# 🎉 Neira Telegram Bot v0.7 — Обновление

## 🧠 Параллельное мышление (Parallel Thinking)

Теперь у Neira **отдельный контекст для каждого чата**! Она помнит историю диалога с каждым пользователем независимо.

### Новые возможности:
- ✅ Отдельная история для каждого chat_id
- ✅ Сохранение до 50 последних сообщений
- ✅ Автоматическое сохранение в `neira_chat_contexts.json`
- ✅ Метаданные пользователей (username, first_name)

### Новые команды:
```bash
/context              # Показать последние 10 сообщений диалога
/clear_context        # Очистить историю этого чата
```

---

## � Расширенное управление памятью (NEW!)

Теперь можно **точечно управлять памятью** Neira без полной очистки!

### Основные возможности:

#### 🔍 Поиск и просмотр
```bash
/memory                    # Последние 10 записей
/memory stats              # Детальная статистика по типам и категориям
/memory search <текст>     # Найти записи содержащие текст
```

#### 🗑️ Частичное удаление
```bash
/memory delete last <N>         # Удалить последние N записей (1-15)
/memory delete text <слово>     # Удалить записи содержащие слово
/memory delete old <дней>       # Удалить записи старше N дней
/memory delete category <cat>   # Удалить всю категорию
```

#### 🧹 Обслуживание
```bash
/memory dedupe             # Удалить дубликаты (>95% схожесть)
/memory backup             # Создать полный бэкап памяти
```

### Практические применения:

**Выход из галлюцинации:**
```bash
# Neira начала галлюцинировать про "квантовый процессор"
/memory search "квантовый процессор"
→ Найдено 12 записей

/memory delete text "квантовый процессор"
→ 🗑️ Удалено записей: 12
```

**Быстрая отмена:**
```bash
# Neira только что запомнила что-то неправильное
/memory delete last 3
→ Откатываем последние 3 записи
```

**Регулярная чистка:**
```bash
/memory dedupe             # Удалить дубликаты
/memory delete old 60      # Удалить записи старше 2 месяцев
```

---

## �🔐 Улучшенная авторизация (Enhanced Auth System)

Теперь можно добавлять пользователей не только по `user_id`, но и по **username** или **ссылке**!

### Поддерживаемые форматы:
```bash
/admin add 123456789           # По user_id (как раньше)
/admin add @username           # По username
/admin add username            # Без @
/admin add t.me/username       # По ссылке на профиль
/admin add https://t.me/username
```

### Улучшенные команды администратора:
```bash
/admin users          # Показывает обе системы авторизации
/admin add <identifier>   # Гибкий формат добавления
/admin stats          # Статистика параллельного мышления
```

### Статистика `/admin stats`:
- 🗨️ Количество активных чатов
- 💬 Всего сообщений
- 👥 Уникальных пользователей
- 📊 Топ-5 самых активных чатов

---

## 🐛 Исправленные ошибки

### 1. **Ошибки типов в brain_enhancer.py**
- ❌ Было: `search_index: Dict[str, List[Tuple[str, str, float]]]`
- ✅ Стало: Убрана аннотация типов + `# type: ignore`
- **Причина**: Индекс хранит `(key, text, source)` а не `(key, text, score)`

### 2. **WindowsSelectorEventLoopPolicy warnings**
- ❌ Было: Pylance не видит Windows-specific класс
- ✅ Стало: Добавлен `# type: ignore` для импортов
- **Причина**: Класс существует в runtime но Pylance его не распознаёт

---

## 📋 Архитектура изменений

### Параллельное мышление
```python
# parallel_thinking.py
@dataclass
class ChatContext:
    chat_id: int
    user_id: int
    messages: List[Dict[str, Any]]
    username: Optional[str]
    first_name: Optional[str]

class ParallelMindSystem:
    def get_or_create_context(chat_id, user_id, ...)
    def add_message(chat_id, role, content)
    def get_context_history(chat_id)
    def clear_context(chat_id)
    def get_stats()
```

### Улучшенная авторизация
```python
# enhanced_auth.py
@dataclass
class AuthorizedUser:
    user_id: int
    username: Optional[str]
    name: Optional[str]

class EnhancedAuthSystem:
    def parse_user_identifier(identifier: str)
    def add_user(user_id, username, name)
    def is_authorized(user_id, username)
    def get_all_users()
```

### Интеграция в telegram_bot.py
```python
# Инициализация систем
parallel_mind = ParallelMindSystem()
auth_system = EnhancedAuthSystem()

# В chat_handler:
1. Создать/получить контекст чата
2. Сохранить сообщение пользователя
3. Обработать запрос
4. Сохранить ответ Neira

# В require_auth:
- Проверка через auth_system.is_authorized()
- Поддержка обеих систем (старой и новой)
```

---

## 🔄 Обратная совместимость

✅ Старая система авторизации (`AUTHORIZED_USERS`) продолжает работать  
✅ Новая система работает параллельно  
✅ `/admin users` показывает обе системы  
✅ Проверка авторизации использует обе системы

---

## 🚀 Что дальше?

### Возможные улучшения:
1. **Миграция пользователей** — скрипт переноса из старой системы в новую
2. **Контекстная память** — интеграция истории диалога в BrainEnhancer
3. **Персонализация** — разная личность Neira для разных пользователей
4. **Экспорт контекста** — сохранение истории в файл
5. **Лимиты сообщений** — настройка глубины истории по пользователям

---

## 📝 Changelog

### v0.7 (2024)
- ➕ Добавлена система параллельного мышления
- ➕ **Расширенное управление памятью** (поиск, частичное удаление, бэкапы)
- ➕ Улучшенная авторизация с поддержкой username
- ➕ Команды `/context` и `/clear_context`
- ➕ Команды `/memory search/delete/dedupe/backup`
- ➕ Команда `/admin stats` для мониторинга
- 🐛 Исправлены ошибки типов в brain_enhancer.py
- 🐛 Убраны Pylance warnings в test_organ_telegram.py

### v0.6
- ➕ Самообучение через теги `#создай_орган`
- ➕ Автоматическая загрузка новых органов (CellWatcher)
- ➕ Защита от зацикливания памяти

---

## 🎯 Использование

### Для пользователей:
```bash
/help                 # Полный список команд
/context             # Посмотреть историю диалога
/clear_context       # Начать с чистого листа
```

### Для администраторов:
```bash
/admin stats         # Статистика параллельного мышления
/admin add @username # Добавить пользователя гибко
/admin users         # Список всех авторизованных
```

---

**Powered by Neira's self-growth system** 🧬
