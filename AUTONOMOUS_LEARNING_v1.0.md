# 🎓 Автономное обучение Neira v1.0

## 📋 Содержание
- [Концепция](#концепция)
- [Защита от галлюцинаций](#защита-от-галлюцинаций)
- [Архитектура](#архитектура)
- [Использование](#использование)
- [Whitelist источников](#whitelist-источников)
- [Система карантина](#система-карантина)
- [Конфигурация](#конфигурация)

---

## 🎯 Концепция

**Проблема:** Neira простаивает когда нет активных диалогов. Можно ли использовать это время для самообучения?

**Решение:** Система автономного обучения — Neira учится в фоновом режиме из проверенных источников с многоуровневой защитой от галлюцинаций.

### Принципы:
1. ✅ **Только надёжные источники** — whitelist проверенных ресурсов
2. ✅ **Карантинная зона** — все знания проходят проверку перед добавлением в память
3. ✅ **Мультисорсная верификация** — минимум 2+ источника для подтверждения
4. ✅ **Автоматическое отклонение** — при обнаружении противоречий
5. ✅ **Human-in-the-loop** — администратор проверяет спорные случаи

---

## 🛡️ Защита от галлюцинаций

### 1. Whitelist надёжных источников
```python
CURATED_SOURCES = [
    # Официальная документация (100% доверие)
    Python.org, MDN Web Docs
    
    # Научные источники (90% доверие)
    Wikipedia (ru/en), arXiv.org
    
    # Open Source (90% доверие)
    GitHub README (только топовые репозитории)
    
    # Q&A (70% доверие)
    Stack Overflow (только accepted answers)
]
```

### 2. Blacklist ненадёжных доменов
```python
BLACKLIST_PATTERNS = [
    ".*forum.*",           # Форумы (неконтролируемый UGC)
    ".*medium.com.*",      # Субъективные статьи
    ".*reddit.com.*",      # User-generated content
    ".*quora.com.*",       # Субъективные ответы
    ".*\.xyz$",            # Подозрительные домены
    ".*\.tk$",             # Бесплатные домены
]
```

### 3. Проверка на противоречия с памятью
```python
# Перед добавлением в карантин:
for memory_entry in existing_memories:
    if check_contradiction(new_fact, memory_entry.text):
        ❌ Отклонить (противоречит существующим знаниям)
```

### 4. Паттерны галлюцинаций
Используется детектор из `immune_system.py`:
- Финансовые галлюцинации (цены без контекста)
- Игровые механики
- Физически невозможное
- Абсолютные утверждения без подтверждения

### 5. Карантинная зона
```
Новые знания → Проверка источника → Валидация → Карантин (24ч) → Память
                    ↓                    ↓              ↓
                Whitelist?         Противоречия?   Confidence >= 70%?
```

### 6. Минимальный порог confidence
- **0.7** (70%) — минимум для автоодобрения
- **0.9** (90%) — высокая уверенность (автоматическое добавление в память)
- **< 0.7** — требует ручной проверки администратором

---

## 🏗️ Архитектура

### Компоненты:

#### 1. **SourceWhitelist** — Управление источниками
- Whitelist проверенных источников
- Blacklist ненадёжных доменов
- Уровни доверия (0.0-1.0)

#### 2. **KnowledgeValidator** — Валидатор знаний
- Проверка на противоречия
- Детект галлюцинаций
- Оценка confidence

#### 3. **LearningCurriculum** — Учебный план
Приоритетные темы:
1. **Самоосознание** (приоритет 1): AI, LLM, neural networks
2. **Текущий стек** (приоритет 2): Python, Telegram API, asyncio
3. **Общие знания** (приоритет 3): наука, технологии, культура
4. **Технологии** (приоритет 4): Python 3.13, AI новости

#### 4. **QuarantineEntry** — Запись в карантине
```python
{
    "id": "abc123",
    "text": "Факт для проверки",
    "source_url": "https://ru.wikipedia.org/...",
    "source_trust": 0.9,
    "category": "learned",
    "confidence": 0.85,
    "status": "pending",  # pending / approved / rejected
    "contradictions": []
}
```

#### 5. **AutonomousLearningSystem** — Основная система
- Определение idle-режима (по умолчанию: 30 минут без активности)
- Фоновый цикл обучения (каждый час)
- Управление карантином
- Статистика

---

## 💻 Использование

### Telegram команды:

#### 🎓 Запуск обучения
```bash
/learn_auto start
```
→ Neira начнёт учиться в фоновом режиме когда не занята диалогами

#### 🛑 Остановка
```bash
/learn_auto stop
```

#### 📊 Статистика
```bash
/learn_auto stats
```
Показывает:
- Статус (работает / остановлено)
- Количество сессий обучения
- Изучено / отклонено фактов
- Состояние карантина
- Защита (whitelist/blacklist)

#### 🔬 Карантин
```bash
/learn_auto quarantine
```
Показывает факты ожидающие проверки:
```
🔬 Карантин знаний (5):

1. ✅ [90%] abc123
   Python: динамически типизированный язык программирования...
   📍 https://ru.wikipedia.org/wiki/Python...

2. ⚠️ [65%] def456
   AI может превзойти человека в...
   📍 https://en.wikipedia.org/wiki/AI...
```

#### ✅ Одобрение из карантина
```bash
/learn_auto approve abc123
```
→ Факт добавляется в память

#### ❌ Отклонение
```bash
/learn_auto reject def456
```
→ Факт удаляется из карантина

---

## 🌐 Whitelist источников

### Официальная документация (доверие: 100%)
- **Python.org** — `https://docs.python.org/.*`
- **MDN Web Docs** — `https://developer.mozilla.org/.*`

### Научные (доверие: 90%)
- **Wikipedia (RU)** — `https://ru.wikipedia.org/wiki/.*`
- **Wikipedia (EN)** — `https://en.wikipedia.org/wiki/.*`
- **arXiv.org** — `https://arxiv.org/abs/.*`

### Open Source (доверие: 90%)
- **GitHub README** — `https://github.com/.*/README.md`

### Q&A (доверие: 70%)
- **Stack Overflow** — `https://stackoverflow.com/questions/.*`

### Как добавить источник?

Редактируй [autonomous_learning.py](autonomous_learning.py):
```python
CuratedSource(
    name="Ваш источник",
    url_pattern=r"https?://your-site\.com/.*",
    trust_level=0.9,  # 0.0-1.0
    categories=["category1", "category2"],
    rate_limit=10  # запросов в час
)
```

---

## 🧪 Система карантина

### Жизненный цикл факта:

#### 1. Извлечение из источника
```python
fact = extract_wikipedia_summary("Python")
# → "Python: динамически типизированный язык..."
```

#### 2. Проверка источника
```python
trusted, trust_level, source_name = SourceWhitelist.is_trusted(url)
# → (True, 0.9, "Wikipedia (Russian)")
```

#### 3. Валидация
```python
approved, confidence, contradictions = validator.verify_fact(
    fact, url, trust_level
)
# → (True, 0.85, [])
```

#### 4. Добавление в карантин
```python
quarantine_entry = QuarantineEntry(
    id="abc123",
    text=fact,
    source_url=url,
    source_trust=0.9,
    confidence=0.85,
    status="pending"
)
```

#### 5. Автоодобрение или ручная проверка
- **Если confidence >= 0.9 и нет противоречий** → автоматически в память
- **Если confidence < 0.9** → ждёт ручной проверки администратором

---

## ⚙️ Конфигурация

### Параметры инициализации:

```python
autonomous_learning_system = AutonomousLearningSystem(
    memory_system=neira_wrapper.memory,
    idle_threshold_minutes=30,      # Минут без активности = idle
    admin_telegram_id=123456789     # ID администратора для уведомлений
)
```

### Настройки валидации:

```python
# В KnowledgeValidator:
min_sources = 2          # Минимум источников для подтверждения
min_confidence = 0.7     # Минимальная уверенность (70%)
```

### Расписание обучения:

```python
# В _learning_loop():
await asyncio.sleep(3600)  # Проверка каждый час
```

---

## 📈 Примеры использования

### Сценарий 1: Запуск автономного обучения
```bash
Пользователь: /learn_auto start

Neira: 🎓 Автономное обучение запущено!

Буду учиться в фоновом режиме когда не занята диалогами.
Все новые знания проходят проверку и карантин.

Используй /learn_auto stats для статистики.
```

### Сценарий 2: Проверка статистики
```bash
Пользователь: /learn_auto stats

Neira: 📊 Статистика автономного обучения

🏃 Статус: Работает
💤 В режиме ожидания

Обучение:
  • Сессий: 5
  • Изучено фактов: 12
  • Отклонено: 3
  • Источников проверено: 15

Карантин:
  • Всего: 4
  • Ожидают проверки: 2
  • Высокая уверенность: 2
  • Требуют ревью: 2

Защита:
  • Whitelist: 7 источников
  • Blacklist: 8 паттернов
  • Одобрено из карантина: 10
  • Отклонено: 1
```

### Сценарий 3: Ручная проверка карантина
```bash
Пользователь: /learn_auto quarantine

Neira: 🔬 Карантин знаний (2):

1. ✅ [92%] f3a9bc
   Python: динамически типизированный язык программирования высокого уровня...
   📍 https://ru.wikipedia.org/wiki/Python...

2. ⚠️ [68%] 2d4ef1
   Искусственный интеллект может превзойти человека в творческих задачах...
   📍 https://en.wikipedia.org/wiki/Artificial_intelligence...

💡 Используй /learn_auto approve <id> для одобрения

Пользователь: /learn_auto approve f3a9bc

Neira: ✅ Факт f3a9bc одобрен и добавлен в память
```

---

## 🔧 Интеграция с кодом

### Пример использования в Python:

```python
from autonomous_learning import AutonomousLearningSystem
from memory_system import MemorySystem

# Инициализация
memory = MemorySystem()
learning = AutonomousLearningSystem(memory, idle_threshold_minutes=30)

# Запуск
await learning.start_autonomous_learning()

# В основном цикле приложения отмечаем активность:
learning.mark_activity()  # При каждом сообщении пользователя

# Проверка карантина
stats = learning.get_quarantine_stats()
print(f"В карантине: {stats['total']}")

# Ручное одобрение
learning.manual_approve("abc123")

# Остановка
await learning.stop_autonomous_learning()
```

---

## 🚀 Дальнейшие улучшения

### Запланировано:
- [ ] Интеграция с GitHub API (изучение trending репозиториев)
- [ ] RSS-фиды для новостей (с фильтрацией)
- [ ] Peer-review между несколькими моделями
- [ ] Автоматические тесты для проверки новых знаний
- [ ] Веб-интерфейс для управления карантином
- [ ] Метрики quality-score для источников
- [ ] A/B тестирование новых знаний

---

## 📚 Связанные документы
- [ADVANCED_MEMORY_v0.7.md](ADVANCED_MEMORY_v0.7.md) — расширенное управление памятью
- [TELEGRAM_v0.7_UPDATE.md](TELEGRAM_v0.7_UPDATE.md) — обновления Telegram бота
- [QUICKSTART.md](QUICKSTART.md) — быстрый старт

---

## ❓ FAQ

### Q: Может ли Neira случайно "загаллюцинировать" факты?
**A:** Нет. Все факты проходят 5 уровней проверки:
1. Whitelist источников
2. Проверка на противоречия с памятью
3. Детект паттернов галлюцинаций
4. Оценка confidence
5. Карантин + ручная проверка

### Q: Сколько времени знания находятся в карантине?
**A:** Факты с confidence >= 90% переносятся в память автоматически. Остальные ждут ручной проверки администратором.

### Q: Можно ли настроить свои источники?
**A:** Да! Редактируй `CURATED_SOURCES` в `autonomous_learning.py`.

### Q: Как часто запускается обучение?
**A:** Каждый час проверяется idle-режим. Если Neira не занята 30+ минут, запускается сессия обучения.

### Q: Можно ли отключить автоодобрение?
**A:** Да, установи `min_confidence = 1.0` в `KnowledgeValidator`. Тогда все факты потребуют ручной проверки.

---

**Создано:** 12 декабря 2025  
**Версия:** 1.0  
**Автор:** Neira Development Team
