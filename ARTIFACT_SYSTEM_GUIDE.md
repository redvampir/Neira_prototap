# Neira Artifact System — UI Generation

## 🎨 Концепция

Neira теперь может создавать **живые интерфейсы** (артефакты) прямо во время диалога. Как Claude Artifacts, но с самообучением и игровым фокусом.

---

## ✨ Основные возможности

### 1. Генерация UI через чат

Просто попроси Neira создать интерфейс:

```
Создай UI инвентаря для RPG
Сделай интерфейс для платформера с жизнями
Создай игровое поле для головоломки
```

Neira автоматически:
- Выберет подходящий шаблон
- Сгенерирует HTML/CSS/JS
- Отобразит в Artifact Viewer
- Сохранит в `artifacts/`

### 2. Игровые шаблоны

**Встроенные шаблоны:**

#### `rpg_inventory` — RPG Инвентарь
- Сетка 5x4 (20 слотов)
- Счётчик золота
- Hover эффекты
- Стилизация под фэнтези

#### `platformer_hud` — Платформер HUD
- Жизни (сердечки)
- Счётчик монет
- Таймер
- Номер уровня

#### `puzzle_board` — Головоломка 3x3
- Игровое поле (сетка)
- Счётчик ходов
- Кнопка сброса
- Интерактивные плитки

### 3. Artifact Viewer

**Управление (правая панель):**
- 🎨 **Открыть** — кнопка в header
- 🔄 **Обновить** — перезагрузить артефакт
- ⛶ **Во весь экран** — открыть в новом окне
- 💾 **Экспорт** — скачать как standalone HTML
- ✕ **Закрыть** — скрыть панель

**Безопасность:**
- Артефакты запускаются в `<iframe sandbox>`
- Изоляция от основного UI
- Content Security Policy

---

## 🔧 API Endpoints

### POST `/api/artifacts/generate`

Генерация артефакта

```json
{
  "task": "Создай UI инвентаря",
  "template": "rpg_inventory",  // опционально
  "data": {}                     // опционально
}
```

**Response:**
```json
{
  "id": "abc123",
  "html": "<div>...</div>",
  "css": ".class {...}",
  "js": "function() {...}",
  "template_used": "RPG Inventory",
  "created_at": "2025-12-23T06:00:00"
}
```

### GET `/api/artifacts`

Список всех артефактов

### GET `/api/artifacts/{artifact_id}`

Получить артефакт по ID

### GET `/api/templates`

Список доступных шаблонов

---

## 📁 Структура файлов

```
/prototype
├── ui_code_cell.py             # Ячейка генерации UI
├── neira_ui_templates.json     # Шаблоны (автосоздаётся)
├── artifacts/                  # Сохранённые артефакты
│   ├── abc123.json            # Метаданные
│   └── abc123.html            # Standalone HTML
├── backend/
│   ├── api.py                 # Artifact endpoints
│   └── neira_wrapper.py       # UICodeCell integration
└── frontend/
    ├── index_8001.html        # Artifact Viewer UI
    ├── app.js                 # Artifact rendering
    └── style.css              # Artifact styles
```

---

## 🎮 Примеры использования

### Пример 1: Простой запрос

**Пользователь:**
> Создай UI инвентаря для RPG

**Neira:**
> 🎨 Создаю UI артефакт...
> ✅ Артефакт создан: abc123

*Артефакт автоматически открывается в правой панели*

### Пример 2: Через API

```powershell
$body = @{
    task = "Создай HUD для платформера"
    template = "platformer_hud"
} | ConvertTo-Json

Invoke-RestMethod -Uri 'http://127.0.0.1:8001/api/artifacts/generate' `
    -Method Post `
    -Body $body `
    -ContentType 'application/json'
```

### Пример 3: Программный доступ

```javascript
// В браузере (через NeiraDebug)
const artifact = {
    id: "test123",
    html: "<div>Hello</div>",
    css: "div { color: red; }",
    js: "console.log('Hi');"
};

window.NeiraDebug.showArtifact(artifact);
```

---

## 🧠 Обучение Neira (Phase 2)

### Training Mode (в разработке)

Будущая система обучения:

1. **Ручная оценка** — пользователь ставит рейтинг артефакту (1-5 ⭐)
2. **Метрики качества** — автоматический анализ (CSS валидность, performance)
3. **Pattern extraction** — успешные решения сохраняются в `neira_knowledge.json`
4. **Template evolution** — новые шаблоны создаются из лучших артефактов

### Файл обучения: `neira_knowledge.json`

```json
{
  "ui_patterns": [
    {
      "name": "inventory_grid",
      "rating": 4.8,
      "usage_count": 15,
      "code_snippet": "...",
      "learned_from": "artifact_abc123"
    }
  ]
}
```

---

## 🛠️ Расширение системы

### Добавление нового шаблона

Отредактируй `neira_ui_templates.json`:

```json
{
  "my_template": {
    "name": "My Template",
    "category": "game",
    "description": "Описание",
    "html": "<div>...</div>",
    "css": ".class {...}",
    "js": "function() {...}"
  }
}
```

Или через Python:

```python
cell = UICodeCell(neira_instance)
cell.templates["my_template"] = {...}
cell._save_templates(cell.templates)
```

### Интеграция с LLM (TODO)

Метод `_adapt_template()` в `UICodeCell` — здесь можно добавить генерацию через LLM:

```python
async def _adapt_template(self, template, task, data):
    prompt = f"Адаптируй этот UI под задачу: {task}\n{template['html']}"
    response = await self.neira.process(prompt)
    # Parse response и extract HTML/CSS/JS
    return {"html": ..., "css": ..., "js": ...}
```

---

## 📊 Статистика и метрики

**Автоматически tracked:**
- Количество созданных артефактов
- Используемые шаблоны (popularity)
- Время создания
- Размер кода (HTML/CSS/JS bytes)

**В будущем:**
- User ratings (1-5 stars)
- Load time metrics
- Interaction analytics
- A/B testing разных версий UI

---

## 🔐 Безопасность

### Sandbox ограничения

Артефакты запускаются с атрибутом `sandbox`:
```html
<iframe sandbox="allow-scripts allow-same-origin"></iframe>
```

**Разрешено:**
- JavaScript execution
- localStorage access (same-origin)

**Запрещено:**
- Forms submission
- Pop-ups
- Top navigation
- Downloads (без user gesture)

### Content Security Policy

```http
Content-Security-Policy: default-src 'self'; script-src 'unsafe-inline';
```

---

## 🎯 Roadmap

### Phase 1: ✅ Базовая инфраструктура
- [x] UICodeCell с шаблонами
- [x] Backend API endpoints
- [x] Artifact Viewer UI
- [x] WebSocket integration
- [x] File persistence

### Phase 2: 🚧 Self-Learning
- [ ] Rating system (user feedback)
- [ ] Quality metrics (автоанализ)
- [ ] Pattern extraction
- [ ] Knowledge base integration
- [ ] Template evolution

### Phase 3: 💭 Advanced Generation
- [ ] LLM-powered adaptation
- [ ] Multi-file artifacts
- [ ] Asset management (images, fonts)
- [ ] Live editing mode
- [ ] Collaborative artifacts

### Phase 4: 🌐 Sharing & Export
- [ ] Artifact gallery
- [ ] Public sharing (optional)
- [ ] Embed code generation
- [ ] npm package export
- [ ] GitHub integration

---

## 💡 Tips & Tricks

1. **Быстрый доступ:** Используй горячую клавишу (TODO) для открытия Artifact Viewer
2. **Export:** Все артефакты можно скачать как standalone HTML
3. **Debugging:** Открой артефакт во весь экран и используй DevTools
4. **Templates:** Просмотри все шаблоны через `/api/templates`
5. **History:** Все артефакты сохраняются в `artifacts/` с timestamp

---

## ❓ FAQ

**Q: Можно ли редактировать артефакт после создания?**
A: Пока нет (Phase 3). Можно экспортировать HTML и редактировать вручную.

**Q: Сколько артефактов можно создать?**
A: Без ограничений (только размер диска).

**Q: Работает ли offline?**
A: Да, все шаблоны локальные. LLM нужен только для адаптации (Phase 3).

**Q: Можно ли использовать свои CSS frameworks?**
A: Да, добавь CDN ссылки в шаблон:
```html
<link rel="stylesheet" href="https://cdn.jsdelivr.net/...">
```

**Q: Как удалить артефакт?**
A: Удали файлы из `artifacts/` папки вручную.

---

## 🎮 Игра-примерОткрой Desktop UI и скажи:

```
Создай игру "крестики-нолики" с UI
```

Neira сгенерирует:
- Игровое поле 3x3
- Логику хода (X/O)
- Определение победителя
- Кнопку "Новая игра"

Всё это **за секунды**, полностью рабочее!

---

**Создано с ❤️ by Neira Team**
**Версия: 1.0-alpha**
**Дата: 2025-12-23**
