# Neira Frontend - Simple HTML UI

Простой веб-интерфейс для Neira AI Assistant. Работает с WebSocket для real-time стриминга.

## 🚀 Быстрый старт

### Локальное тестирование

1. **Запустить backend:**
   ```bash
   # запускать из корня репозитория
   uvicorn backend.api:app --host 127.0.0.1 --port 8000
   ```
   Backend будет доступен на `http://localhost:8000`

   Если порт `8000` занят:
   ```bash
   uvicorn backend.api:app --host 127.0.0.1 --port 8001
   ```

2. **Открыть frontend:**
   ```bash
   # Просто откройте index.html в браузере
   # (или index_8001.html, если backend запущен на 8001)
   open index.html
   # Или используйте простой HTTP сервер
   python -m http.server 3000
   ```
   Frontend будет доступен на `http://localhost:3000`

3. **Проверить подключение:**
   - В правом верхнем углу должен быть зелёный индикатор "Connected"
   - Если красный "Disconnected" — проверьте, что backend запущен

---

## 📦 Деплой на бесплатный хостинг

### Вариант 1: Vercel (рекомендуется)

**Vercel** — бесплатный хостинг для статических сайтов с автодеплоем из GitHub.

#### Шаги:

1. **Создать аккаунт на Vercel:**
   - Перейти на https://vercel.com
   - Зарегистрироваться через GitHub

2. **Подготовить репозиторий:**
   ```bash
   # Убедитесь что frontend закоммичен
   git add frontend/
   git commit -m "Add frontend"
   git push origin main
   ```

3. **Импортировать проект:**
   - На Vercel нажать "New Project"
   - Выбрать ваш репозиторий `Neira_prototap`
   - В настройках указать:
     - **Root Directory**: `frontend`
     - **Build Command**: оставить пустым (статика)
     - **Output Directory**: оставить пустым

4. **Настроить переменные окружения:**
   - В настройках проекта → Environment Variables
   - Добавить:
     ```
     NEIRA_WS_URL=wss://your-backend.com/ws/chat
     NEIRA_API_URL=https://your-backend.com/api
     ```
   - ⚠️ **ВАЖНО**: Нужно задеплоить backend отдельно (см. ниже)

5. **Деплой:**
   - Vercel автоматически задеплоит при каждом push в GitHub
   - Ваш сайт будет доступен на `https://your-project.vercel.app`

---

### Вариант 2: Netlify

**Netlify** — альтернатива Vercel, тоже бесплатный.

#### Шаги:

1. **Создать аккаунт:**
   - Перейти на https://netlify.com
   - Зарегистрироваться через GitHub

2. **Drag & Drop (простейший способ):**
   - Открыть https://app.netlify.com/drop
   - Перетащить папку `frontend/` в окно браузера
   - Готово! Сайт развёрнут

3. **Или через GitHub:**
   - New site from Git → выбрать репозиторий
   - Base directory: `frontend`
   - Build command: (пусто)
   - Publish directory: `frontend`

4. **Настроить переменные:**
   - Site settings → Build & deploy → Environment
   - Добавить `NEIRA_WS_URL` и `NEIRA_API_URL`

---

### Вариант 3: GitHub Pages

**GitHub Pages** — бесплатный хостинг прямо из репозитория.

#### Шаги:

1. **Настроить репозиторий:**
   ```bash
   # Скопировать frontend в корень или отдельную ветку gh-pages
   cp -r frontend/ docs/
   git add docs/
   git commit -m "Add frontend to docs"
   git push
   ```

2. **Включить GitHub Pages:**
   - Репозиторий → Settings → Pages
   - Source: Deploy from branch
   - Branch: `main`, folder: `/docs`
   - Save

3. **Доступ:**
   - Сайт будет доступен на `https://username.github.io/Neira_prototap/`

⚠️ **Ограничение**: GitHub Pages не поддерживает WebSocket напрямую, нужно настроить CORS и использовать внешний backend.

---

## 🔧 Конфигурация

### Изменить адрес backend

Если backend запущен не на `localhost:8000`, измените в `app.js`:

```javascript
const CONFIG = {
    WS_URL: 'ws://YOUR_BACKEND_IP:8000/ws/chat',
    API_URL: 'http://YOUR_BACKEND_IP:8000/api',
};
```

Или задайте переменные окружения в HTML перед подключением `app.js`:

```html
<script>
    window.NEIRA_WS_URL = 'wss://neira-backend.herokuapp.com/ws/chat';
    window.NEIRA_API_URL = 'https://neira-backend.herokuapp.com/api';
</script>
<script src="app.js"></script>
```

---

## 🌐 Деплой Backend

Чтобы frontend на Vercel/Netlify работал, нужно задеплоить backend на облачный сервер.

### Варианты:

#### 1. Heroku (бесплатно до 550 часов/месяц)

```bash
cd backend/
heroku login
heroku create neira-backend

# Создать Procfile:
echo "web: uvicorn api:app --host 0.0.0.0 --port \$PORT" > Procfile

git add Procfile
git commit -m "Add Heroku config"
git push heroku main
```

Ваш backend будет на `https://neira-backend.herokuapp.com`

#### 2. Railway.app (бесплатный план)

```bash
# Установить Railway CLI
npm i -g @railway/cli

# Деплой
cd backend/
railway login
railway init
railway up
```

#### 3. Render.com (бесплатно)

- Создать Web Service на https://render.com
- Подключить репозиторий
- Root directory: `backend`
- Build command: `pip install -r requirements.txt`
- Start command: `uvicorn api:app --host 0.0.0.0 --port $PORT`

#### 4. VPS (полный контроль)

Если у вас есть VPS (например, Oracle Cloud Free Tier):

```bash
# На сервере
git clone https://github.com/your-username/Neira_prototap
cd Neira_prototap/backend

pip install -r requirements.txt
uvicorn api:app --host 0.0.0.0 --port 8000

# Или через systemd (см. backend/README.md)
```

---

## 📱 Возможности

### Что работает:

- ✅ Real-time чат через WebSocket
- ✅ Стриминг ответов с индикацией этапов
- ✅ Статистика моделей и VRAM
- ✅ Информация о памяти и опыте
- ✅ Адаптивный дизайн (десктоп + мобильный)
- ✅ Тёмная тема
- ✅ Автоподключение при разрыве соединения

### Что будет добавлено:

- ⏳ История сессий (сохранение в localStorage)
- ⏳ Настройки (выбор моделей, temperature)
- ⏳ Экспорт диалогов
- ⏳ Поддержка изображений (через qwen3-vl)
- ⏳ Markdown рендеринг в сообщениях
- ⏳ Подсветка кода

---

## 🐛 Отладка

### Проблема: "Disconnected" в статусе

**Причины:**
1. Backend не запущен → запустите `python api.py` в папке `backend/`
2. Неверный URL → проверьте `CONFIG.WS_URL` в `app.js`
3. CORS блокировка → убедитесь что `allow_origins=["*"]` в `api.py`

**Проверка:**
```bash
# Проверить что backend доступен
curl http://localhost:8000/api/health

# Проверить WebSocket
wscat -c ws://localhost:8000/ws/chat
```

### Проблема: Сообщения не отправляются

**Откройте консоль браузера (F12):**
- Должны быть логи: `WebSocket connected`
- Если есть ошибки — скопируйте и проверьте

**Debug функции:**
```javascript
// В консоли браузера
window.NeiraDebug.sendTestMessage("Привет")
window.NeiraDebug.getStats()
window.NeiraDebug.reconnect()
```

---

## 📂 Структура

```
frontend/
├── index.html      # Главная страница (чат + статистика)
├── style.css       # Тёмная тема, адаптивный дизайн
├── app.js          # WebSocket клиент, логика UI
└── README.md       # Эта документация
```

---

## 🎨 Кастомизация

### Изменить тему

В `style.css` измените переменные:

```css
:root {
    --bg-primary: #1a1a1a;        /* Фон основной */
    --bg-secondary: #2a2a2a;      /* Фон карточек */
    --accent-blue: #4a9eff;       /* Акцентный цвет */
}
```

### Добавить новые функции

Скелет для нового API endpoint:

```javascript
// В app.js
async function fetchMemory() {
    const response = await fetch(`${CONFIG.API_URL}/memory?limit=10`);
    const data = await response.json();
    console.log(data);
}
```

---

## 📖 Связанные документы

- Backend API: `../backend/README.md`
- Архитектура: `../DESKTOP_UI_PLAN.md`
- Основной README: `../README.md`

---

## 🆘 Поддержка

Если что-то не работает:

1. Проверьте логи backend: `python api.py`
2. Проверьте консоль браузера (F12)
3. Убедитесь что все зависимости установлены: `pip install -r backend/requirements.txt`
4. Попробуйте тестовый скрипт: `python backend/test_api.py`

Удачи! 🚀
