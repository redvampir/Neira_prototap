# 🚀 Быстрый старт: Автономное обучение

## Установка зависимостей

```bash
pip install aiohttp beautifulsoup4 lxml
```

## Запуск

### 1. Запусти Telegram бота
```bash
python telegram_bot.py
```

### 2. Активируй автономное обучение
В Telegram:
```
/learn_auto start
```

Готово! Neira начнёт учиться сама когда не занята диалогами.

## Проверка работы

### Статистика
```
/learn_auto stats
```

### Карантин (ожидающие проверки факты)
```
/learn_auto quarantine
```

### Одобрить факт
```
/learn_auto approve abc123
```

### Отклонить
```
/learn_auto reject abc123
```

## Защита от галлюцинаций

✅ **Автоматически работает:**
- Whitelist надёжных источников (Wikipedia, Python.org, arXiv)
- Blacklist ненадёжных доменов (форумы, блоги)
- Проверка на противоречия с памятью
- Детект паттернов галлюцинаций
- Карантин перед добавлением в память
- Минимальный порог confidence (70%)

**Факты с confidence >= 90%** добавляются автоматически  
**Факты с confidence < 90%** требуют ручной проверки

## Настройки

### Изменить порог idle (по умолчанию: 30 минут)

Редактируй [telegram_bot.py](telegram_bot.py):
```python
autonomous_learning_system = AutonomousLearningSystem(
    memory_system=neira_wrapper.memory,
    idle_threshold_minutes=60,  # ← измени здесь
    admin_telegram_id=_ADMIN_ID
)
```

### Добавить свой источник

Редактируй [autonomous_learning.py](autonomous_learning.py):
```python
CURATED_SOURCES = [
    # ...existing sources...
    CuratedSource(
        name="Мой источник",
        url_pattern=r"https?://my-site\.com/.*",
        trust_level=0.8,
        categories=["technology"],
        rate_limit=10
    ),
]
```

## Полная документация

См. [AUTONOMOUS_LEARNING_v1.0.md](AUTONOMOUS_LEARNING_v1.0.md)
