# 🧬 Neira Evolution System v0.6

Полная автономная система самосовершенствования Neira.

## Оглавление

- [Обзор](#обзор)
- [Архитектура](#архитектура)
- [Системы](#системы)
- [Команды](#команды)
- [Примеры использования](#примеры-использования)
- [Конфигурация](#конфигурация)
- [FAQ](#faq)

---

## Обзор

Neira v0.6 включает **6 систем самосовершенствования**, которые позволяют ей автономно эволюционировать:

1. **Continuous Learning** — исправляет собственный код при ошибках
2. **Prompt Evolution** — улучшает system prompts через генетические алгоритмы
3. **Cell Factory** — создает специализированные клетки для частых задач
4. **Dynamic Cell Loader** — загружает новые клетки без перезапуска
5. **A/B Testing** — тестирует изменения перед развертыванием
6. **Fine-Tuning Pipeline** — дообучает модель личности на опыте

Все системы координируются через **EvolutionManager** и могут работать автономно.

---

## Архитектура

```
┌─────────────────────────────────────────────────────────┐
│                   EvolutionManager                      │
│         (координатор всех систем эволюции)              │
└──────────────────┬──────────────────────────────────────┘
                   │
    ┌──────────────┼──────────────┐
    │              │              │
    ▼              ▼              ▼
┌─────────┐  ┌──────────┐  ┌────────────┐
│Continuo │  │ Prompt   │  │   Cell     │
│us       │  │Evolution │  │  Factory   │
│Learning │  └──────────┘  └────────────┘
└─────────┘        │              │
    │         ┌────▼────┐    ┌────▼────┐
    │         │   A/B   │    │ Dynamic │
    │         │ Testing │    │  Loader │
    │         └─────────┘    └─────────┘
    │              │
    └──────────┐   │
               ▼   ▼
         ┌──────────────┐
         │  Fine-Tuning │
         │   Pipeline   │
         └──────────────┘
               │
               ▼
         [neira-personality:v2]
```

---

## Системы

### 1. Continuous Learning System

**Назначение:** Автоматическое исправление кода при обнаружении провалов.

**Как работает:**
1. Анализирует Experience на наличие провалов (score < 6, verdict != "ПРИНЯТ")
2. При 3+ провалах в одном типе задачи → запускается
3. Генерирует патч для исправления проблемы
4. Применяет изменения с бэкапом в `backups/code_evolution/`
5. Валидирует синтаксис Python
6. При ошибке — откатывает изменения

**Что модифицирует:**
- `cells.py` — system prompts клеток
- `main.py` — логика оркестратора
- `code_cell.py`, `web_cell.py` — специализированные клетки

**Файлы:**
- `continuous_learning.py` — основная логика
- `neira_evolution.json` — лог эволюции
- `backups/code_evolution/` — бэкапы

**Пример:**
```python
# После 3 провалов в задачах типа "код"
# Continuous Learning анализирует проблему:
# "AnalyzerCell неправильно определяет сложность кода"

# Генерирует патч:
# Файл: cells.py
# Цель: AnalyzerCell.system_prompt
# Решение: Добавить инструкции по оценке сложности

# Применяет улучшенный промпт и логирует
```

---

### 2. Prompt Evolution System

**Назначение:** Эволюция system prompts через генетические алгоритмы и A/B тесты.

**Как работает:**
1. Версионирует каждый system prompt (v1, v2, v3...)
2. При низкой производительности (avg_score < 7.5) → запускает эволюцию
3. Генерирует новый вариант:
   - **Mutation** — мутирует промпт (усиление строгости, добавление примеров)
   - **Crossover** — скрещивает два успешных промпта
4. Новая версия тестируется на реальных задачах (минимум 5 тестов)
5. Сравнивает с текущей версией по метрикам
6. Активирует лучшую версию автоматически

**Метрики:**
- `avg_score` — средняя оценка (1-10)
- `success_rate` — процент успешных задач
- `avg_confidence` — средняя уверенность модели

**Файлы:**
- `prompt_evolution.py` — основная логика
- `neira_prompts_history.json` — версии промптов

**Пример:**
```python
# У AnalyzerCell производительность 6.5/10

# Эволюция генерирует мутацию:
# Базовый промпт: "Ты — аналитик. Определи тип задачи."
# Мутация "усилить строгость":
# → "Ты — строгий аналитик. ТОЧНО определи тип задачи.
#    Проверь: код/вопрос/творчество. Оценка сложности 1-5."

# Новая версия: analyzer_v20241209_153022
# Тестируется 5 раз → avg_score: 8.2/10
# Активируется как лучшая
```

---

### 3. Cell Factory

**Назначение:** Автоматическое создание специализированных клеток для частых задач.

**Как работает:**
1. Сканирует Experience на повторяющиеся паттерны (минимум 3 повтора)
2. Извлекает паттерн (первые 2 слова запроса)
3. Генерирует спецификацию клетки через LLM
4. Создает Python файл по шаблону в `generated/`
5. Валидирует синтаксис и импорт
6. Регистрирует в `neira_cell_registry.json`
7. Статус: `active: false` (требуется активация)

**Паттерны:**
- "напиши код" → `CodeGeneratorCell`
- "объясни концепцию" → `ConceptExplainerCell`
- "найди информацию" → `InfoFinderCell`

**Файлы:**
- `cell_factory.py` — фабрика
- `generated/*.py` — сгенерированные клетки
- `neira_cell_registry.json` — реестр клеток

**Пример:**
```python
# Паттерн "напиши код" повторился 5 раз

# Cell Factory создает:
# Файл: generated/code_generator_20241209_153045.py
# Класс: CodeGeneratorCell
# system_prompt: "Ты — генератор кода. Пиши чистый,
#                 работающий код по описанию."

# Регистрируется в реестре (неактивна)
# Активация: /activate-cell code_generator
```

---

### 4. Dynamic Cell Loader

**Назначение:** Динамическая загрузка клеток из `generated/` без перезапуска.

**Как работает:**
1. При старте Neira сканирует `generated/`
2. Читает реестр `neira_cell_registry.json`
3. Загружает только **активные** клетки
4. Создает экземпляры с MemoryCell
5. Регистрирует в `loaded_cells`
6. **Hot reload:** проверяет изменения файлов и перезагружает

**Возможности:**
- Загрузка без `import` в main.py
- Обновление клеток "на лету"
- Изоляция через отдельные модули

**Файлы:**
- `dynamic_cell_loader.py` — загрузчик

**Пример:**
```python
# При старте Neira:
# 📚 ДИНАМИЧЕСКАЯ ЗАГРУЗКА КЛЕТОК
# Найдено активных клеток: 2
# ✅ Загружена клетка: code_generator (CodeGeneratorCell)
# ✅ Загружена клетка: concept_explainer (ConceptExplainerCell)
# ✅ Загружено клеток: 2/2

# Hot reload (при изменении файла):
# 🔄 Перезагрузка клетки: code_generator
# ✅ Клетка перезагружена: code_generator
```

---

### 5. A/B Testing Framework

**Назначение:** Тестирование вариантов промптов/клеток перед развертыванием.

**Как работает:**
1. Создается тест с 2+ вариантами
2. Запросы распределяются по алгоритму:
   - **Round-robin** — поровну между вариантами
   - **Epsilon-greedy** — 80% лучший, 20% случайный
   - **UCB** — баланс исследования/эксплуатации
3. Собирается статистика (минимум 10 тестов на вариант)
4. Статистический анализ (t-test упрощенный)
5. Автоматическое определение победителя

**Метрики:**
- Score (1-10)
- Confidence (0-1)
- Latency (ms)
- Success rate (%)

**Файлы:**
- `ab_testing.py` — фреймворк
- `neira_ab_tests.json` — тесты

**Пример:**
```python
# Тест двух промптов для AnalyzerCell
# Вариант A: старый промпт
# Вариант B: эволюционированный промпт

# 15 тестов распределены через epsilon-greedy
# Результаты:
# A: score 6.8, success 70%, latency 320ms
# B: score 8.4, success 90%, latency 280ms

# 🏆 ПОБЕДИТЕЛЬ: Вариант B
# Улучшение: +1.6 score, +20% success, -40ms latency
# Статистически значимо: ✅
```

---

### 6. Fine-Tuning Pipeline

**Назначение:** Автоматическое дообучение модели `neira-personality` на успешных диалогах.

**Как работает:**
1. Фильтрует Experience: score >= 8, verdict = "ПРИНЯТ"
2. Экспортирует диалоги в JSONL формат
3. Генерирует Ollama Modelfile с personality prompt
4. Запускает `ollama create neira-personality:v2`
5. Регистрирует версию в `neira_model_versions.json`
6. Требует активацию: `/activate-model v2`

**Триггер:** 50+ успешных примеров накоплено

**Версионирование:**
- `v1_20241201` — базовая на mistral:7b-instruct
- `v2_20241209` — дообученная на 87 диалогах
- `v3_20241215` — дообученная на 156 диалогах

**Файлы:**
- `finetuning_pipeline.py` — pipeline
- `training_data/*.jsonl` — данные для обучения
- `models/Modelfile.*` — Ollama Modelfiles
- `neira_model_versions.json` — версии

**Пример:**
```bash
# 🎓 ЗАПУСК FINE-TUNING PIPELINE
# 📊 Экспортировано примеров: 87
#    Файл: training_data/training_data_20241209_153120.jsonl

# 📝 Создан Modelfile: models/Modelfile.neira-v2_20241209
# 🎓 Создание модели: neira-personality:v2_20241209
# ✅ Модель создана: neira-personality:v2_20241209

# 🎉 FINE-TUNING ЗАВЕРШЕН
#    Модель: neira-personality:v2_20241209
#    Статус: требуется тестирование
```

---

## Команды

### Общие команды

```bash
/evolution stats       # Статистика всех систем эволюции
/evolution log [system] # Лог эволюции (cls/cells/models/all)
/evolution cycle       # Запустить автоматический цикл эволюции
/evolution help        # Полная справка
```

### Continuous Learning

```bash
# Автоматически триггерится при 3+ провалах
# Ручной запуск недоступен (безопасность)

# Просмотр лога:
/evolution log cls     # Последние 10 записей эволюции кода
```

### Prompt Evolution

```bash
# Ручная эволюция промпта:
/evolve-prompt analyzer    # Эволюционировать промпт AnalyzerCell

# Просмотр версий:
/evolution log prompts     # История версий промптов
```

### Cell Factory

```bash
# Создание клетки вручную:
/create-cell "обработка JSON файлов"

# Активация клетки:
/activate-cell json_processor

# Просмотр реестра:
/evolution log cells
```

### Dynamic Loader

```bash
# Загружается автоматически при старте
# Просмотр загруженных:
/evolution log loader
```

### A/B Testing

```bash
# Создание теста (в коде):
from ab_testing import ABTestingFramework
framework.create_test(
    "Analyzer prompts",
    [("prompt_v1", "Старый"), ("prompt_v2", "Новый")]
)

# Просмотр результатов:
/ab-results test_20241209_153045
```

### Fine-Tuning

```bash
# Запуск обучения:
/train-model           # Автоматически если 50+ примеров

# Просмотр версий:
/evolution log models

# Активация версии:
/activate-model v2_20241209
```

---

## Примеры использования

### Сценарий 1: Автоматическая эволюция

**Ситуация:** Neira плохо справляется с задачами типа "код" (3 провала подряд)

**Что происходит:**
1. **Continuous Learning** обнаруживает паттерн провалов
2. Анализирует проблему: "CodeCell неправильно генерирует классы"
3. Генерирует патч для улучшения промпта
4. Применяет изменения → `cells.py` обновлен
5. **Prompt Evolution** создает улучшенную версию промпта
6. **A/B Testing** тестирует старую vs новую версию (15 тестов)
7. Новая версия побеждает → активируется
8. При накоплении 50+ примеров → **Fine-Tuning** дообучает модель

**Результат:** Neira самостоятельно исправила проблему, без вмешательства пользователя.

---

### Сценарий 2: Создание специализированной клетки

**Ситуация:** Пользователь часто просит "объясни концепцию X"

**Что происходит:**
1. Паттерн "объясни концепцию" повторился 5 раз
2. **Cell Factory** обнаруживает паттерн
3. Генерирует `ConceptExplainerCell`:
   ```python
   class ConceptExplainerCell(Cell):
       name = "concept_explainer"
       system_prompt = """Ты — преподаватель.
       Объясняй концепции простым языком
       с примерами и аналогиями."""
   ```
4. Сохраняет в `generated/concept_explainer_20241209.py`
5. Регистрирует в реестре (неактивна)
6. Пользователь активирует: `/activate-cell concept_explainer`
7. **Dynamic Loader** загружает клетку
8. Следующий запрос "объясни рекурсию" → использует новую клетку
9. **A/B Testing** сравнивает с базовой ExecutorCell
10. Новая клетка лучше → остается активной

**Результат:** Neira создала специализированную клетку под частую задачу.

---

### Сценарий 3: Fine-tuning модели личности

**Ситуация:** Накопилось 87 успешных диалогов (score >= 8)

**Что происходит:**
1. **Fine-Tuning Pipeline** триггерится автоматически
2. Экспортирует диалоги в JSONL:
   ```json
   {"instruction": "Ты — Neira...", "input": "Как дела?", "output": "..."}
   ```
3. Генерирует Modelfile:
   ```dockerfile
   FROM mistral:7b-instruct
   SYSTEM """Ты — Neira, живая программа...
   [personality prompt из Experience]"""
   ```
4. Создает модель: `ollama create neira-personality:v2`
5. Пользователь тестирует новую версию:
   ```bash
   # Временно переключиться на v2:
   MODEL_PERSONALITY="neira-personality:v2" python main.py
   ```
6. **A/B Testing** сравнивает v1 vs v2 (20 диалогов)
7. v2 лучше (score 8.9 vs 7.8) → активируется
8. Обновляет `cells.py`: `MODEL_PERSONALITY = "neira-personality:v2"`

**Результат:** Модель личности стала лучше отражать накопленный опыт.

---

## Конфигурация

### Настройка порогов

**Continuous Learning** (`continuous_learning.py`):
```python
MIN_FAILURES_TO_TRIGGER = 3      # Минимум провалов для запуска
MIN_SCORE_THRESHOLD = 6          # Оценка ниже которой = провал
MODIFIABLE_FILES = ["cells.py", "main.py", ...]  # Что можно менять
```

**Prompt Evolution** (`prompt_evolution.py`):
```python
MIN_TESTS_PER_VARIANT = 5        # Минимум тестов для оценки
MUTATION_STRENGTH = 0.3          # Сила мутации (0.0-1.0)
CROSSOVER_RATE = 0.5             # Вероятность кроссовера
```

**Cell Factory** (`cell_factory.py`):
```python
MIN_PATTERN_OCCURRENCES = 3      # Минимум повторений для создания клетки
GENERATED_CELLS_DIR = "generated" # Папка для клеток
```

**Fine-Tuning** (`finetuning_pipeline.py`):
```python
MIN_TRAINING_SAMPLES = 50        # Минимум примеров для обучения
```

### Отключение систем

Если нужно отключить определенные системы, закомментируйте импорты в `evolution_manager.py`:

```python
# Отключить Continuous Learning:
# from continuous_learning import ContinuousLearningSystem
CLS_AVAILABLE = False

# Отключить Fine-Tuning:
# from finetuning_pipeline import FineTuningPipeline
FTP_AVAILABLE = False
```

---

## FAQ

### Q: Как часто запускается автоэволюция?

A: Автоматически **не запускается**. Только по команде `/evolution cycle` или при достижении порогов:
- Continuous Learning: 3+ провала в одном типе
- Prompt Evolution: avg_score < 7.5 после 5+ тестов
- Cell Factory: 3+ повторения паттерна
- Fine-Tuning: 50+ успешных примеров

### Q: Безопасно ли автоматическое изменение кода?

A: **Да**, есть несколько уровней защиты:
1. **Бэкапы:** Каждое изменение сохраняется в `backups/code_evolution/`
2. **Валидация:** Проверка синтаксиса Python перед применением
3. **Rollback:** Автоматический откат при ошибках
4. **Whitelist:** Модифицируются только файлы из `MODIFIABLE_FILES`
5. **Логирование:** Все изменения записываются в `neira_evolution.json`

Но для критических систем рекомендуется review изменений перед перезапуском Neira.

### Q: Как откатить неудачные изменения?

A:
```bash
# 1. Найти бэкап в backups/code_evolution/
ls -lt backups/code_evolution/

# 2. Восстановить файл:
cp backups/code_evolution/20241209_153045_cells.py cells.py

# 3. Перезапустить Neira
python main.py
```

### Q: Сколько занимает fine-tuning?

A: Зависит от количества примеров:
- 50 примеров: ~2-5 минут
- 100 примеров: ~5-10 минут
- 500 примеров: ~20-30 минут

Ollama создает модель локально, не требует GPU обязательно (но быстрее с GPU).

### Q: Можно ли экспортировать сгенерированные клетки?

A: Да, они в `generated/*.py`. Можешь:
1. Скопировать в другие проекты
2. Версионировать через git
3. Делиться с другими пользователями Neira

### Q: Как посмотреть что изменилось в коде?

A:
```bash
# Сравнить с бэкапом:
diff cells.py backups/code_evolution/20241209_153045_cells.py

# Или посмотреть лог эволюции:
/evolution log cls
```

### Q: A/B тесты влияют на пользовательский опыт?

A: **Минимально**. Варианты распределяются через epsilon-greedy (80% лучший, 20% тестовый), поэтому большинство запросов идут на проверенную версию. Тестовые варианты отличаются незначительно (мутации промптов).

### Q: Можно ли откатить версию модели?

A:
```python
# Посмотреть версии:
/evolution log models

# Активировать старую:
/activate-model v1_20241201

# Обновить cells.py вручную:
MODEL_PERSONALITY = "neira-personality:v1_20241201"
```

### Q: Что если эволюция сломает систему?

A: **Критические файлы не трогаются**:
- `experience.py` — система опыта
- `model_manager.py` — управление VRAM
- `evolution_manager.py` — сам менеджер

Модифицируются только:
- `cells.py` — промпты клеток (безопасно)
- `code_cell.py`, `web_cell.py` — специализированные клетки

В худшем случае:
1. Откатить из бэкапа
2. Или переклонировать репозиторий
3. Или удалить `neira_evolution.json` для сброса

---

## Архитектурные решения

### Почему не используется ML для мутаций?

**Ответ:** Используется **LLM как meta-optimizer**. Вместо обучения отдельной модели для генерации промптов, мы используем ту же Mistral/Qwen для:
- Анализа провалов
- Генерации патчей
- Мутации промптов
- Создания спецификаций клеток

Это проще, быстрее и не требует дополнительных моделей.

### Почему не используется reinforcement learning?

**Ответ:** RL требует:
- Тысячи итераций для обучения
- Сложную инфраструктуру (replay buffer, value network)
- Долгое время обучения

Вместо этого используем **bandit алгоритмы** (epsilon-greedy, UCB) для A/B тестов:
- Простая реализация
- Быстрая адаптация
- Достаточно для выбора между 2-3 вариантами

### Почему fine-tuning через Ollama Modelfile?

**Ответ:** Ollama пока не поддерживает полноценный fine-tuning через API. Modelfile — это способ создания **адаптированной модели** с custom system prompt и параметрами.

Для реального fine-tuning потребуется:
1. Экспортировать модель из Ollama
2. Использовать LoRA/QLoRA через Hugging Face
3. Импортировать обратно в Ollama

Это планируется в будущих версиях.

---

## Метрики и мониторинг

### Ключевые метрики

**Continuous Learning:**
- `total_attempts` — попыток самоулучшения
- `successful` — успешных изменений
- `rollbacks` — откатов
- `files_modified` — измененных файлов

**Prompt Evolution:**
- `total_versions` — версий промптов
- `active_versions` — активных версий
- `avg_improvement` — среднее улучшение score

**Cell Factory:**
- `total_cells` — создано клеток
- `active_cells` — активных клеток
- `patterns_covered` — покрытых паттернов
- `avg_uses` — среднее использование

**Fine-Tuning:**
- `total_versions` — версий модели
- `training_samples` — примеров обучения
- `avg_score_improvement` — улучшение после обучения

### Просмотр метрик

```bash
/evolution stats       # Все метрики сразу
/evolution log cls     # Детали Continuous Learning
/evolution log cells   # Детали Cell Factory
```

---

## Roadmap

### v0.7 (планируется)

- [ ] **Meta-Learning:** Обучение на истории эволюции
  - Какие паттерны изменений работают лучше
  - Предсказание успешности патчей

- [ ] **Distributed Evolution:** Эволюция на нескольких инстансах
  - Sharing генетического материала между Neira
  - Federated learning для личности

- [ ] **Vision Evolution:** Эволюция для multimodal задач
  - Адаптация для qwen3-vl
  - Генерация клеток для обработки изображений

- [ ] **Full Fine-Tuning:** Реальное дообучение через LoRA
  - Интеграция с Hugging Face
  - Автоматический экспорт/импорт моделей

- [ ] **Interactive Evolution:** Feedback от пользователя
  - Thumbs up/down для вариантов
  - Human-in-the-loop для критических изменений

---

## Контрибуция

Хочешь улучшить систему эволюции?

1. Fork репозиторий
2. Создай новую систему в отдельном файле (например, `reward_learning.py`)
3. Интегрируй через `EvolutionManager`
4. Добавь тесты
5. Submit PR

**Идеи для новых систем:**
- Reward Learning — обучение с подкреплением
- Neural Architecture Search — поиск архитектуры клеток
- Curriculum Learning — постепенное усложнение задач
- Transfer Learning — перенос знаний между доменами

---

## Лицензия

MIT License — делай что хочешь, но на свой риск 😉

Система эволюции может изменять код автономно. Используй бэкапы и version control!

---

**Neira v0.6 — первая по-настоящему автономная AI система! 🧬🚀**
