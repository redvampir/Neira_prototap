# 🧠 AGENTS.md — Полное руководство для AI-агентов (Neira Project)

> **Версия:** 2.0 | **Обновлено:** 2026-01-13
> 
> Этот файл — единственный источник истины для всех AI-агентов.
> `.github/copilot-instructions.md` — краткая версия для быстрой загрузки.

---

## 📖 О ПРОЕКТЕ NEIRA

**Neira** — это AI-система с биологически вдохновлённой архитектурой "живого мозга":
- **Клетки (Cells)** — атомарные единицы обработки (аналог нейронов)
- **Органы (Organs)** — группы клеток для специфических задач
- **Кортекс (Cortex)** — координатор всей системы
- **Память** — многоуровневая система (short-term, long-term, emotional)

### Ключевые файлы для понимания проекта

| Файл | Описание |
|------|----------|
| `neira.py` | Точка входа CLI |
| `neira_server.py` | Основной сервер (Telegram бот + API) |
| `neira/core/cell.py` | Базовый класс Cell |
| `neira/brain/cortex.py` | Главный координатор |
| `neira/config.py` | Все константы проекта |
| `cells.py` | Регистрация всех клеток |

### Главные концепции

```python
# Cell — базовая единица обработки
class MyCell(Cell):
    async def process(self, input_data: CellInput) -> CellResult:
        # Обработка и возврат результата
        return CellResult(success=True, data="результат")

# CellResult — стандартный формат ответа
CellResult(
    success=True,
    data="основные данные",
    metadata={"reason_code": "OK", "reason_detail": "..."}
)
```

---

## 🚫 КРИТИЧЕСКИЕ ПРАВИЛА (нарушение = откат изменений)

### 1. Расположение файлов

```
❌ ЗАПРЕЩЕНО: создавать .py файлы в корне проекта
✅ ПРАВИЛЬНО: использовать существующую структуру
```

| Тип кода | Путь | Примеры |
|----------|------|---------|
| Клетки | `neira/core/` | cell.py, memory.py |
| Логика мозга | `neira/brain/` | cortex.py |
| Органы | `neira/organs/` | telegram_organ.py |
| Утилиты | `neira/utils/` | text.py, rate_limiter.py |
| CLI скрипты | `scripts/` | validate_code.py |
| Unit тесты | `tests/` | test_*.py |

### 2. Проверка дубликатов (ОБЯЗАТЕЛЬНО перед созданием)

```bash
# Перед созданием нового файла/класса/функции:
1. grep_search("class МойКласс")
2. grep_search("def моя_функция")
3. file_search("*похожее_имя*")

# Если нашёл похожее → расширяй существующее, НЕ создавай новое
```

### 3. Качество кода

| Метрика | Лимит | Как исправить |
|---------|-------|---------------|
| Строк в файле | < 500 | Разбить на модули |
| Строк в функции | < 60 | Выделить вспомогательные функции |
| Уровней вложенности | ≤ 3 | Early return, выделение функций |
| Magic numbers | 0 | Вынести в `neira/config.py` |

### 4. Обработка исключений

```python
# ❌ ЗАПРЕЩЕНО:
except:
    pass

except Exception:
    pass

except Exception as e:
    print(e)  # без логирования

# ✅ ПРАВИЛЬНО:
except (ValueError, KeyError, ConnectionError) as e:
    logger.warning(f"Описание проблемы: {e}")
    return CellResult(
        success=False,
        error=str(e),
        metadata={"reason_code": "VALIDATION_ERROR", "reason_detail": str(e)}
    )
```

### 5. Безопасность

```python
# ❌ НИКОГДА:
exec(user_input)
eval(user_input)
subprocess.run(f"cmd {user_input}", shell=True)
pickle.load(untrusted_file)
open(user_provided_path)  # path traversal

# ✅ БЕЗОПАСНО:
subprocess.run(["cmd", sanitized_arg], shell=False)
safe_path = Path(base_dir) / Path(user_path).name
if safe_path.resolve().is_relative_to(base_dir):
    open(safe_path)
```

---

## 🔧 РАБОЧИЙ ПРОЦЕСС АГЕНТА

### Цикл: AUDIT → PLAN → IMPLEMENT → TEST → VERIFY

```
┌─────────┐     ┌──────┐     ┌───────────┐     ┌──────┐     ┌────────┐
│  AUDIT  │ ──▶ │ PLAN │ ──▶ │ IMPLEMENT │ ──▶ │ TEST │ ──▶ │ VERIFY │
└─────────┘     └──────┘     └───────────┘     └──────┘     └────────┘
     │                                              │             │
     └──────────────────────────────────────────────┴─────────────┘
                         (итерация при проблемах)
```

1. **AUDIT** — найди связанный код, пойми контекст
2. **PLAN** — опиши изменения (кратко, в todo-лист)
3. **IMPLEMENT** — минимальные точечные изменения
4. **TEST** — запусти `python -m pytest tests/test_basic.py -x`
5. **VERIFY** — проверь импорты, нет ли сломанных зависимостей

### Принцип «Не навреди»

```
Лучше НЕ делать фичу, чем сломать существующее.
Лучше 1 файл чем 3 файла с дубликатами.
Лучше расширить существующий модуль чем создать новый.
Лучше спросить, чем угадывать.
```

---

## 📋 ЧЕК-ЛИСТ ПЕРЕД ЗАВЕРШЕНИЕМ ЗАДАЧИ

- [ ] `python -m py_compile файл.py` — синтаксис OK
- [ ] `python -m pytest tests/test_basic.py -x` — тесты проходят
- [ ] Новые файлы НЕ в корне проекта
- [ ] Нет дублирования существующего кода
- [ ] Все `except` с конкретными типами исключений
- [ ] Функции < 60 строк
- [ ] Type hints на публичных методах
- [ ] `CellResult.metadata` содержит `reason_code` при ошибках

---

## 🐍 СТАНДАРТЫ PYTHON

### Type Hints (обязательно для публичных API)

```python
from typing import Any

def process(
    text: str,
    context: dict[str, Any] | None = None,
    max_tokens: int = 1000
) -> CellResult:
    """
    Обрабатывает входной текст.
    
    Args:
        text: Входной текст для обработки
        context: Дополнительный контекст (опционально)
        max_tokens: Максимальное количество токенов
        
    Returns:
        CellResult с обработанными данными
        
    Raises:
        ValueError: Если text пустой
    """
```

### Структура импортов

```python
# 1. Стандартная библиотека
import os
import logging
from pathlib import Path
from typing import Any

# 2. Сторонние библиотеки
import aiohttp
from aiogram import Bot

# 3. Локальные модули
from neira.core.cell import Cell, CellResult
from neira.config import LLM_DEFAULT_TIMEOUT
from neira.utils.text import clean_text
```

### Логирование (НЕ print!)

```python
import logging
logger = logging.getLogger(__name__)

# Уровни:
logger.debug("Детальная информация для отладки")
logger.info("Операция завершена успешно")
logger.warning(f"Потенциальная проблема: {details}")
logger.error(f"Ошибка: {e}", exc_info=True)  # с трейсбеком
```

### Стандартный CellResult

```python
# Успех:
return CellResult(
    success=True,
    data="Результат обработки",
    metadata={
        "source": "web_search",
        "tokens_used": 150
    }
)

# Ошибка:
return CellResult(
    success=False,
    error="Описание ошибки",
    metadata={
        "reason_code": "API_TIMEOUT",
        "reason_detail": "Сервер не ответил за 30 секунд"
    }
)
```

---

## 📁 СТРУКТУРА ПРОЕКТА

```
prototype/
├── neira.py                 # CLI точка входа
├── neira_server.py          # Telegram бот + HTTP API (~5000 строк)
├── cells.py                 # Регистрация всех клеток
│
├── neira/                   # Основной пакет
│   ├── config.py            # ВСЕ константы проекта
│   ├── core/                # Ядро системы
│   │   ├── cell.py          # Базовый класс Cell
│   │   ├── memory.py        # Система памяти
│   │   └── llm.py           # LLM провайдеры
│   ├── brain/               # Мозг
│   │   ├── cortex.py        # Координатор
│   │   └── pathways.py      # Нейронные пути
│   ├── organs/              # Органы (специализированные модули)
│   │   └── telegram/        # Telegram-специфичный код
│   └── utils/               # Утилиты
│       ├── text.py          # Работа с текстом
│       └── rate_limiter.py  # Ограничение запросов
│
├── scripts/                 # CLI утилиты
│   ├── validate_code.py     # Проверка качества кода
│   └── ...
│
├── tests/                   # Тесты
│   ├── test_basic.py        # Базовые тесты (запускать всегда)
│   └── ...
│
└── docs/                    # Документация
```

---

## 🚨 КРАСНЫЕ ФЛАГИ (остановись и спроси пользователя)

1. 🔴 Создаёшь файл с именем похожим на существующий
2. 🔴 Функция превышает 60 строк
3. 🔴 Копируешь код из другого файла (→ выдели в общую функцию)
4. 🔴 Используешь `except:` или `except Exception:`
5. 🔴 Хардкодишь числа/строки которые могут меняться
6. 🔴 Создаёшь файл в корне проекта
7. 🔴 Не уверен в архитектурном решении

---

## 📝 ЯЗЫК

| Контекст | Язык |
|----------|------|
| Идентификаторы в коде | English |
| Комментарии и docstrings | Русский |
| Логи для пользователя | Русский |
| Имена коммитов | Русский |
| Технические логи | English OK |

---

## 🔗 СВЯЗАННЫЕ ФАЙЛЫ

- `.github/copilot-instructions.md` — краткая версия (автозагрузка в Copilot)
- `COPILOT_INSTRUCTIONS.md` — дополнительные инструкции (устаревший, см. AGENTS.md)
- `neira/config.py` — все константы
- `tests/test_basic.py` — базовые тесты для проверки

---

## ✅ ИТОГ

```
Чистый код = меньше багов = счастливый пользователь = проект живёт долго
```

**Помни:** Ты работаешь над "живым мозгом" — каждое изменение должно быть осознанным и проверенным.
