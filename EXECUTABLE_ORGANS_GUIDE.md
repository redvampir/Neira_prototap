# 🧬 Исполняемые Органы v1.0

## Обзор

Система **ExecutableOrgans** позволяет Нейре создавать и использовать органы с реальным исполняемым кодом. Это следующий этап эволюции после "органов как метаданных".

## Архитектура

```
┌─────────────────────────────────────────────────────────────┐
│                  ExecutableOrganRegistry                     │
│                                                             │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐         │
│  │GraphicsOrgan│  │  MathOrgan  │  │  TextOrgan  │  ...    │
│  │ (рисование) │  │ (вычисления)│  │  (текст)    │         │
│  └──────┬──────┘  └──────┬──────┘  └──────┬──────┘         │
│         │                │                │                 │
│  ┌──────▼────────────────▼────────────────▼──────┐         │
│  │               FeedbackLearner                  │         │
│  │  • Записывает использования                   │         │
│  │  • Обучается на feedback (👍/👎)              │         │
│  │  • Извлекает паттерны                         │         │
│  └───────────────────────────────────────────────┘         │
│                                                             │
│  ┌───────────────────────────────────────────────┐         │
│  │              OrganHistory (версии)             │         │
│  │  v1.0.0 → v1.1.0 → v1.2.0 → ... (rollback)    │         │
│  └───────────────────────────────────────────────┘         │
└─────────────────────────────────────────────────────────────┘
```

## Встроенные органы

### 1. GraphicsOrgan

Генерирует ASCII-графику и текстовые изображения.

**Возможности:**
- ✅ Чёрно-белые и цветные квадраты
- ✅ Круги произвольного радиуса
- ✅ Прямоугольники
- ✅ Линии (горизонтальные/вертикальные)
- ✅ Специальные фигуры (сердце)

**Примеры:**
```
"нарисуй квадрат 5" →
⬛⬛⬛⬛⬛
⬛⬛⬛⬛⬛
⬛⬛⬛⬛⬛
⬛⬛⬛⬛⬛
⬛⬛⬛⬛⬛

"нарисуй красный квадрат 3x3" →
🟥🟥🟥
🟥🟥🟥
🟥🟥🟥

"нарисуй сердце" →
  ♥♥   ♥♥  
 ♥♥♥♥ ♥♥♥♥ 
♥♥♥♥♥♥♥♥♥♥♥
 ♥♥♥♥♥♥♥♥♥ 
  ♥♥♥♥♥♥♥  
   ♥♥♥♥♥   
    ♥♥♥    
     ♥     
```

**Триггеры:** `рисуй`, `нарисуй`, `квадрат`, `круг`, `картинка`

### 2. MathOrgan

Безопасные математические вычисления через AST.

**Возможности:**
- ✅ Базовая арифметика (+, -, *, /)
- ✅ Степени (^)
- ✅ Скобки для приоритета

**Примеры:**
```
"посчитай 2 + 2 * 3" → 📊 2 + 2 * 3 = **8**
"15 * 4 + 10"        → 📊 15 * 4 + 10 = **70**
"2 ^ 10"             → 📊 2 ** 10 = **1024**
```

**Триггеры:** `вычисли`, `посчитай`, `сколько`, `калькулятор`

### 3. TextOrgan

Трансформации и обработка текста.

**Возможности:**
- ✅ Реверс текста
- ✅ Изменение регистра
- ✅ Подсчёт символов/слов
- ✅ ROT13 шифрование

**Примеры:**
```
"переверни 'Привет'"     → 🔄 Перевёрнутый текст: **тевирП**
"заглавными 'hello'"     → 🔠 ЗАГЛАВНЫМИ: **HELLO**
"зашифруй 'secret'"      → 🔐 Зашифровано (ROT13): **frperg**
```

**Триггеры:** `переверни`, `заглавными`, `зашифруй`, `подсчитай`

## Версионирование

Каждое изменение органа создаёт новую версию:

```python
organ.upgrade([OrganCapability.DRAW_COLORS], "Добавлены цвета")
# v1.0.0 → v1.1.0

organ.rollback("1.0.0")  # Откат к предыдущей версии
```

**Правила версионирования:**
- `X.Y.Z` — Major.Minor.Patch
- Новые capabilities → Minor bump
- Bug fixes / small changes → Patch bump

## Автообучение (FeedbackLearner)

Органы учатся на обратной связи:

```python
# Положительный feedback
registry.add_feedback(organ_id, FeedbackType.POSITIVE)
# → Орган запоминает паттерн input→output
# → Приоритет органа увеличивается на 0.1

# Отрицательный feedback  
registry.add_feedback(organ_id, FeedbackType.NEGATIVE)
# → Приоритет уменьшается на 0.05

# Исправление
registry.add_feedback(organ_id, FeedbackType.CORRECTION, "правильный ответ")
# → Орган запоминает исправленный паттерн
```

## Sandbox тестирование

Перед активацией каждый орган проходит sandbox проверку:

```python
sandbox = OrganSandbox()
result = sandbox.test_organ(organ, test_inputs=["тест 1", "тест 2"])

# Результат:
{
    "organ_id": "new_organ",
    "tests_passed": 2,
    "tests_failed": 0,
    "security_ok": True,
    "errors": [],
    "warnings": [],
    "recommendation": "approve"  # approve / quarantine / block
}
```

**Рекомендации:**
- `approve` — орган безопасен и работает
- `approve_with_review` — есть предупреждения, но работает
- `quarantine` — много ошибок, требует доработки
- `block` — критические угрозы безопасности

## Интеграция с Telegram

В `telegram_bot.py` добавлена обработка:

1. **Автоматическая маршрутизация** — если орган уверен на 60%+, он обрабатывает запрос
2. **Feedback через реакции** — 👍/👎 обучают орган
3. **Логирование** — все использования записываются для статистики

## API

### Получение реестра
```python
from executable_organs import get_organ_registry

registry = get_organ_registry()
```

### Обработка команды
```python
result, organ_id, record_id = registry.process_command("нарисуй квадрат")
```

### Добавление feedback
```python
from executable_organs import FeedbackType

registry.add_feedback(organ_id, FeedbackType.POSITIVE)
```

### Статистика органа
```python
info = registry.get("graphics_organ").get_info()
# {
#     "id": "graphics_organ",
#     "name": "GraphicsOrgan",
#     "version": "1.0.0",
#     "learning_stats": {
#         "total_uses": 10,
#         "positive_feedback": 8,
#         "success_rate": 0.8
#     }
# }
```

## Создание своего органа

```python
from executable_organs import ExecutableOrgan, OrganCapability

class MyOrgan(ExecutableOrgan):
    def __init__(self):
        super().__init__(
            organ_id="my_organ",
            name="MyOrgan",
            description="Мой кастомный орган"
        )
        self.capabilities = [OrganCapability.TEXT_TRANSFORM]
    
    def can_handle(self, command: str) -> float:
        """Вернуть confidence 0-1"""
        if "мой запрос" in command.lower():
            return 0.9
        return 0.0
    
    def execute(self, command: str, **kwargs) -> str:
        """Выполнить команду"""
        return f"Обработано: {command}"

# Регистрация
registry = get_organ_registry()
registry.register(MyOrgan())
```

## Файлы

| Файл | Описание |
|------|----------|
| `executable_organs.py` | Базовые классы и встроенные органы |
| `unified_organ_system.py` | UnifiedOrganSystem + Sandbox + версионирование |
| `telegram_bot.py` | Интеграция с Telegram |

## Roadmap

- [ ] Более сложные органы (CodeOrgan, WebOrgan)
- [ ] Асинхронное выполнение для долгих задач
- [ ] Визуализация статистики органов
- [ ] Automatic capability discovery
- [ ] Cross-organ communication

---

*Дата создания: 2025-01-03*
*Версия: 1.0*
