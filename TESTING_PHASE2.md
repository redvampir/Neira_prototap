# 🧪 Инструкция для тестирования Phase 2

## Предварительные требования

1. **Backend запущен:**
   ```powershell
   cd f:\Нейронки\prototype\backend
   python main.py
   ```
   Убедись, что видишь:
   ```
   [INFO] UICodeCell initialized (3 templates loaded)
   INFO:     Uvicorn running on http://0.0.0.0:8001
   ```

2. **Frontend открыт:**
   - Открой `http://localhost:8001` в браузере
   - Или `frontend/index_8001.html` (если backend на 8001)

---

## ✅ Тест 1: Генерация + Рейтинг

### Шаг 1: Создать артефакт
В чате Neira напиши:
```
Создай интерфейс RPG инвентаря
```

**Ожидаемое:**
- Neira ответит "🎨 Создан артефакт: {id}"
- Справа откроется Artifact Viewer с превью
- В header покажется Template: `rpg_inventory`

### Шаг 2: Оценить артефакт
1. Наведи курсор на звёзды (⭐) — они станут золотыми и увеличатся
2. Кликни на 5-ю звезду
3. **Ожидаемое:**
   - Все 5 звёзд заполнятся (★★★★★)
   - Справа появится "5"
   - В консоли браузера (F12): `Rating successful: {artifact_id: "...", rating: 5}`

### Шаг 3: Проверить файлы
```powershell
# 1. Рейтинг сохранён
cat artifacts/{artifact_id}.json | Select-String rating
# Output: "rating": 5, "rated_at": "2025-01-15T..."

# 2. Компоненты извлечены
cat neira_ui_components.json | Select-String components
# Output: должны быть css_inventory-grid, js_renderInventory, etc.
```

---

## ✅ Тест 2: Component Library (автоэкстракция)

### Шаг 1: Генерировать ещё один артефакт
```
Создай HUD для платформера
```

### Шаг 2: Оценить на 5⭐

### Шаг 3: Проверить библиотеку
```powershell
Get-Content neira_ui_components.json | ConvertFrom-Json | Select-Object -ExpandProperty components | Measure-Object
# Output: Count > 0 (количество извлечённых компонентов)
```

**Ожидаемое:**
- Компоненты из двух артефактов объединены
- Сортировка по рейтингу (5⭐ компоненты первыми)
- Нет дубликатов по `name`

---

## ✅ Тест 3: Resonance-based Generation

### Подготовка: Установить resonance
```powershell
# Временно изменить neira.heart.resonance (если есть)
# Или проверить дефолтное значение (0.5)
```

### Вариант A: Низкий resonance (<0.3)
1. Установи `neira.heart.resonance = 0.2` (в коде или через API)
2. Создай артефакт: "Создай интерфейс головоломки"
3. **Ожидаемое:**
   - Цвета консервативные (серый #7f8c8d, синий #3498db)
   - Нет анимаций

### Вариант B: Высокий resonance (>0.7)
1. Установи `neira.heart.resonance = 0.9`
2. Создай артефакт: "Создай интерфейс инвентаря"
3. **Ожидаемое:**
   - Яркие цвета (фиолетовый #9b59b6, красный #e74c3c)
   - CSS содержит `@keyframes pulse`

### Проверка:
```powershell
# Посмотри CSS в artifacts/{id}.json
cat artifacts/{id}.json | Select-String "@keyframes|#9b59b6|#e74c3c"
```

---

## ✅ Тест 4: Experience.py Integration

### Предусловие: experience.py работает

### Шаг 1: Оценить артефакт на 4⭐

### Шаг 2: Проверить лог
```powershell
# В консоли backend должно быть:
# [INFO] Experience added: action_type=ui_generation, success=True, reward=4
```

### Шаг 3: Проверить neira_experience.json
```powershell
Get-Content neira_experience.json | ConvertFrom-Json | Select-Object -ExpandProperty experiences -Last 1
```

**Ожидаемое:**
```json
{
  "action_type": "ui_generation",
  "success": true,
  "reward": 4,
  "context": {
    "artifact_id": "...",
    "template": "rpg_inventory",
    "rating": 4
  }
}
```

---

## 🚨 Возможные проблемы

### Проблема: "UICodeCell not available"
**Решение:**
```powershell
# Перезапусти backend
cd backend
python main.py
```

### Проблема: Звёзды не кликаются
**Решение:**
1. Открой DevTools (F12)
2. Проверь Console на ошибки
3. Убедись, что `elements.starsContainer` не null

### Проблема: Компоненты не извлекаются
**Решение:**
1. Проверь рейтинг (должен быть 5⭐)
2. Проверь backend console на `[WARNING] Component extraction failed`
3. Убедись, что в артефакте есть CSS классы (не пустые шаблоны)

---

## 📊 Метрики успеха

- ✅ Рейтинг сохраняется в JSON
- ✅ `neira_ui_components.json` содержит >0 компонентов
- ✅ Resonance влияет на цвета CSS
- ✅ Experience.py записывает события
- ✅ UI отзывчив (hover, click работают)

---

## 🎉 Что дальше?

После успешного тестирования Phase 2:
1. **Phase 2.1:** UI для Component Library (просмотр библиотеки в frontend)
2. **Phase 3:** LLM-powered adaptation (использование модели для кастомизации)
3. **Phase 4:** Sharing & Export (публичная галерея артефактов)

---

**Удачи! 🚀**
