# 🔒 Безопасность Neira

> Аутентификация, авторизация и защита системы

## 📋 Содержание

- [Обзор](#-обзор)
- [Настройка пароля](#-настройка-пароля)
- [Защищённые команды](#-защищённые-команды)
- [Аудит безопасности](#-аудит-безопасности)
- [Органы защиты](#-органы-защиты)

---

## 🎯 Обзор

Neira имеет многоуровневую систему безопасности:

| Уровень | Компонент | Описание |
|---------|-----------|----------|
| 1 | Аутентификация | Пароль администратора |
| 2 | Авторизация | Декораторы `@require_auth` |
| 3 | Изоляция | Sandbox для артефактов |
| 4 | Защита памяти | Фильтрация галлюцинаций |

---

## 🔑 Настройка пароля

### Переменная окружения

```env
# Пароль администратора (ОБЯЗАТЕЛЬНО измените!)
NEIRA_ADMIN_PASSWORD=your_strong_password_here
```

### Примеры сильных паролей

```
MyN3iraB0t!Secur3#2024
T3l3gramAdmin!98765
Pr0t3ct_N3ira$2024
```

### ⚠️ НЕ используйте

- ❌ `0:11111111` (старый пароль)
- ❌ `password`, `123456`, `admin`
- ❌ Простые слова из словаря

---

## 🛡️ Защищённые команды

### Только для администратора

| Команда | Описание | Защита |
|---------|----------|--------|
| `/code` | Чтение исходного кода | `@require_auth` + `is_admin()` |
| `/clear` | Сброс памяти | `@require_auth` |
| `/admin` | Панель администратора | `is_admin()` |
| `/settings` | Настройки системы | `@require_auth` |

### Декоратор @require_auth

```python
@require_auth
async def code_command(update, context):
    user_id = update.effective_user.id
    
    if not is_admin(user_id):
        await update.message.reply_text("⛔ Только для администратора")
        return
    
    # ... логика команды
```

---

## 📊 Аудит безопасности v0.7.2

### Исправленные уязвимости

| # | Уязвимость | Критичность | Статус |
|---|------------|-------------|--------|
| 1 | `/code` без авторизации | 🔴 Критическая | ✅ |
| 2 | Хардкод пароля | 🔴 Критическая | ✅ |
| 3 | `/clear` без защиты | 🟠 Высокая | ✅ |
| 4 | Path traversal в `/code read` | 🟠 Высокая | ✅ |
| 5 | Утечка `.env` | 🔴 Критическая | ✅ |
| 6 | Rate limiting отсутствовал | 🟡 Средняя | ✅ |
| 7 | CORS слишком открытый | 🟡 Средняя | ✅ |
| 8 | Логирование паролей | 🟠 Высокая | ✅ |

---

## 🧬 Органы защиты

См. подробнее: [ORGAN_SECURITY.md](ORGAN_SECURITY.md)

### ImmuneSystem (иммунная система)

- Детект галлюцинаций
- Фильтрация вредоносного контента
- Мониторинг аномалий

### MemoryProtection (защита памяти)

- Блокировка технического мусора
- Обнаружение зацикливаний
- Версионный контроль памяти

---

## ⚙️ Конфигурация

```env
# Безопасность
NEIRA_ADMIN_PASSWORD=your_strong_password
NEIRA_MAX_AUTH_ATTEMPTS=5
NEIRA_AUTH_TIMEOUT=300

# Изоляция
NEIRA_SANDBOX_ENABLED=true
NEIRA_ARTIFACT_CSP=strict

# Логирование
NEIRA_AUDIT_LOG=true
NEIRA_LOG_SENSITIVE=false
```

---

## 🚨 Действия при инциденте

### Если подозреваете компрометацию

1. **Немедленно:**
   ```bash
   # Остановить бота
   ./stop.sh
   ```

2. **Сменить пароль:**
   ```env
   NEIRA_ADMIN_PASSWORD=new_very_strong_password
   ```

3. **Проверить логи:**
   ```bash
   grep -i "admin\|auth\|error" logs/neira.log
   ```

4. **Перезапустить:**
   ```bash
   ./start.sh
   ```

---

## 🔗 Связанные документы

- [Система органов](ORGAN_SECURITY.md)
- [Архитектура](../architecture/ARCHITECTURE.md)
- [Главная документация](../README.md)
