# 🚀 Neira v0.7 — Что нового?

## TL;DR

Три главных улучшения:

1. **🧠 Параллельное мышление** — отдельный контекст для каждого чата
2. **💾 Умное управление памятью** — удаление по словам/датам, поиск, дедупликация
3. **🔐 Гибкая авторизация** — добавление по @username, не только user_id

---

## Быстрый старт

### Параллельное мышление

```bash
# Neira помнит контекст каждого чата отдельно
User A: Мой любимый цвет синий
User B: Мой любимый цвет красный

User A: Какой мой любимый цвет?
→ Neira: Синий! (не перепутает с User B)

# Управление контекстом
/context         # Посмотреть историю
/clear_context   # Очистить и начать заново
```

---

### Управление памятью

```bash
# Просмотр
/memory                      # Последние записи
/memory stats                # Детальная статистика
/memory search "Python"      # Поиск по слову

# Удаление (для выхода из галлюцинаций)
/memory delete last 5        # Удалить последние 5 записей
/memory delete text "bug"    # Удалить все упоминания бага
/memory delete old 30        # Удалить записи старше месяца

# Обслуживание
/memory dedupe               # Удалить дубликаты
/memory backup               # Создать бэкап
```

**Пример: выход из галлюцинации**
```
Neira начала галлюцинировать про "квантовый процессор"

/memory search "квантовый процессор"
→ Найдено 8 записей

/memory delete text "квантовый процессор"
→ Удалено 8 записей ✅
```

---

### Улучшенная авторизация

```bash
# Теперь можно добавлять пользователей разными способами
/admin add @username               # По username
/admin add t.me/username          # По ссылке
/admin add 123456789              # По user_id (как раньше)

# Проверка
/admin users
→ Показывает username и имена

# Статистика параллельного мышления
/admin stats
→ Сколько активных чатов, топ пользователей
```

---

## Полная документация

- [TELEGRAM_v0.7_UPDATE.md](TELEGRAM_v0.7_UPDATE.md) — все изменения v0.7
- [MEMORY_MANAGEMENT.md](MEMORY_MANAGEMENT.md) — подробное руководство по памяти
- [TELEGRAM_PARALLEL_THINKING.md](TELEGRAM_PARALLEL_THINKING.md) — параллельное мышление
- [TESTING_v0.7.md](TESTING_v0.7.md) — как тестировать новые функции

---

## Примеры использования

### Сценарий 1: Регулярная чистка памяти

```bash
# Еженедельное обслуживание
/memory backup              # Сначала бэкап
/memory stats               # Посмотреть "до"
→ Всего: 347 записей

/memory dedupe              # Удалить дубликаты
→ Удалено: 34 дубликата

/memory delete old 60       # Удалить старше 2 месяцев
→ Удалено: 89 записей

/memory stats               # Посмотреть "после"
→ Всего: 224 записи ⬇️ 123
```

---

### Сценарий 2: Работа с контекстами

```bash
# В личном чате
User: Обсуждаем приватный проект X
/context
→ История личного чата

# В групповом чате
User (в группе): Обсуждаем публичную задачу Y
/context
→ История группового чата (отдельная!)

# Очистка после завершения темы
/clear_context
→ Начинаем новую тему с чистого листа
```

---

### Сценарий 3: Исследование памяти

```bash
# Что Neira помнит обо мне?
/memory search preference
→ Мои предпочтения

/memory search skill
→ Что Neira умеет

/memory search "мой проект"
→ Всё про конкретный проект

# Анализ
/memory stats
→ Сколько всего, по каким категориям
```

---

## Roadmap (что планируется)

### Память:
- [ ] Восстановление из бэкапа (`/memory restore`)
- [ ] Пометка важных записей (`/memory pin`)
- [ ] Экспорт в читаемый формат
- [ ] Автоочистка по настраиваемым правилам

### Контексты:
- [ ] Именованные контексты (переключение между темами)
- [ ] Экспорт истории диалога
- [ ] Настройка глубины истории на пользователя

### Авторизация:
- [ ] Группы пользователей (admin, user, guest)
- [ ] Права доступа к командам
- [ ] Логирование действий администраторов

---

## Миграция со старой версии

### Для пользователей:
✅ Ничего не требуется — всё работает автоматически!

### Для администраторов:
```bash
# Старая система авторизации продолжает работать
# Но рекомендуется мигрировать на новую:

# 1. Посмотреть старых пользователей
/admin users
→ Видим и старых (по user_id) и новых

# 2. Постепенно добавлять в новую систему
/admin add @username1
/admin add @username2

# 3. Новая система даёт больше информации
/admin users
→ Теперь видим username и имена
```

---

## Обратная совместимость

- ✅ Старые команды работают как раньше
- ✅ AUTHORIZED_USERS продолжает работать
- ✅ Старые файлы памяти автоматически мигрируются
- ✅ Можно использовать обе системы авторизации одновременно

---

## Технические детали

### Новые файлы:
```
neira_chat_contexts.json        # Контексты чатов
neira_authorized_users.json     # Новая система авторизации
backups/memory_backup_*.json    # Бэкапы памяти
```

### Новые модули:
```python
parallel_thinking.py            # Система параллельного мышления
enhanced_auth.py               # Улучшенная авторизация
memory_system.py (MemoryManager)  # Менеджер памяти
```

### Новые зависимости:
Нет! Все новые функции используют только стандартную библиотеку Python.

---

## Частые вопросы

**Q: Потеряется ли старая память?**  
A: Нет! Вся память сохраняется, новые команды — это дополнительные возможности управления.

**Q: Нужно ли перенастраивать бота?**  
A: Нет, всё работает автоматически. Новые функции доступны сразу.

**Q: Безопасно ли удаление памяти?**  
A: Используйте `/memory backup` перед массовыми удалениями. Удаление необратимо.

**Q: Сколько контекстов может хранить Neira?**  
A: Неограниченно! Каждый чат = отдельный контекст. По умолчанию 50 сообщений на чат.

**Q: Можно ли восстановить удалённую память?**  
A: Только из бэкапа. Планируется команда `/memory restore` в следующей версии.

---

## Известные проблемы

1. **Восстановление бэкапа:** Пока только вручную (копирование файлов)
2. **Глубина контекста:** Фиксированная (50 сообщений), планируется настройка
3. **Username resolution:** Для добавления по @username бот должен видеть пользователя в чате

---

## Благодарности

Спасибо за идеи и тестирование:
- Система частичного удаления памяти — по запросу пользователей
- Поиск по слову — для выхода из галлюцинаций
- Бэкапы — для безопасного экспериментирования

---

**Версия:** 0.7  
**Дата:** Декабрь 2024  
**Статус:** Стабильный релиз  

Подробнее: [TELEGRAM_v0.7_UPDATE.md](TELEGRAM_v0.7_UPDATE.md)
