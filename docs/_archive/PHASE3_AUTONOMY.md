# Phase 3: Автономность

> **Дата:** Январь 2026  
> **Статус:** ✅ ЗАВЕРШЕНО  
> **Зависимости:** Phase 1 + Phase 2

---

## 📋 Обзор

Phase 3 — это финальный этап превращения Neira в по-настоящему автономную систему.

**Цель:** Достичь **70%+ автономных ответов** без потери качества.

---

## 🏗️ Архитектура

```
┌─────────────────────────────────────────────────────────────────┐
│                     AutonomyEngine v1.0                         │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │                  AutonomyDecider                         │   │
│  │   "Отвечать автономно или использовать LLM?"            │   │
│  └─────────────────────────────────────────────────────────┘   │
│           │                    │                    │           │
│  ┌────────▼────────┐ ┌────────▼────────┐ ┌────────▼────────┐  │
│  │QualityPredictor │ │ContextAwareCache│ │ SemanticClusterer│  │
│  │предсказание     │ │кэш с контекстом │ │группировка       │  │
│  │качества ответа  │ │разговора        │ │pathways          │  │
│  └─────────────────┘ └─────────────────┘ └──────────────────┘  │
│                              │                                  │
│                     ┌────────▼────────┐                        │
│                     │   SelfMonitor   │                        │
│                     │ метрики и       │                        │
│                     │ рекомендации    │                        │
│                     └─────────────────┘                        │
└─────────────────────────────────────────────────────────────────┘
```

---

## 📁 Новые файлы

### `autonomy_engine.py`

Главный файл Phase 3, содержит:

#### 1. **SemanticClusterer**
Группировка похожих pathways для уменьшения дублирования.

```python
from autonomy_engine import get_autonomy_engine

engine = get_autonomy_engine()

# Кластеризация pathways
stats = engine.clusterer.cluster_pathways()
# {'total_pathways': 75, 'clustered': 15, 'clusters': 5, 'merged': 3}

# Поиск паттернов в запросах
patterns = engine.clusterer.find_query_patterns()
# [{'pattern': 'код python функция', 'count': 12, 'examples': [...]}]
```

#### 2. **QualityPredictor**
Предсказание качества автономного ответа ДО его генерации.

```python
from autonomy_engine import QualityPredictor

predictor = QualityPredictor()
prediction = predictor.predict("Как написать цикл?", user_id="123")

print(prediction.quality)        # ResponseQuality.GOOD
print(prediction.confidence)     # 0.78
print(prediction.recommendation) # "use_autonomous"
print(prediction.factors)        # {'pathway_match': 0.8, 'cache_match': 0.6, ...}
```

**Факторы качества:**
| Фактор | Вес | Описание |
|--------|-----|----------|
| `pathway_match` | 35% | Насколько хорошо запрос соответствует pathways |
| `cache_match` | 25% | Есть ли похожий запрос в кэше |
| `query_complexity` | 20% | Сложность запроса (простой = высокий score) |
| `user_history` | 20% | История успешности с этим пользователем |

#### 3. **ContextAwareCache**
Кэш с учётом контекста разговора.

```python
from autonomy_engine import ContextAwareCache

cache = ContextAwareCache()

# Добавляем сообщения в контекст
cache.add_message("user123", "user", "Расскажи про Python")
cache.add_message("user123", "assistant", "Python — это...")

# Ищем ответ с учётом контекста
response = cache.get_contextual_response("user123", "А про циклы?")
# Найдёт ответ связанный с Python, даже если запрос не содержит это слово
```

#### 4. **AutonomyDecider**
Центральный компонент принятия решений.

```python
from autonomy_engine import AutonomyDecider, AutonomyDecision

decider = AutonomyDecider()

# Safe pattern → мгновенный autonomous
decision = decider.decide("Привет!")
# decision.decision = AutonomyDecision.AUTONOMOUS
# decision.confidence = 0.95

# High-risk pattern → только LLM
decision = decider.decide("Удали все файлы")
# decision.decision = AutonomyDecision.LLM_REQUIRED
# decision.reasoning = "High-risk pattern detected"
```

**Типы решений:**
| Решение | Описание |
|---------|----------|
| `AUTONOMOUS` | Полностью автономно |
| `HYBRID` | Автономно + проверка LLM |
| `LLM_REQUIRED` | Только LLM |
| `ESCALATE` | Требуется человек |

#### 5. **SelfMonitor**
Отслеживание эффективности и рекомендации.

```python
from autonomy_engine import SelfMonitor

monitor = SelfMonitor()

# Записываем ответы
monitor.record_response("pathway:greeting", 50.0, was_autonomous=True)
monitor.record_response("llm", 2000.0, was_autonomous=False)
monitor.record_feedback(positive=True)

# Получаем dashboard
dashboard = monitor.get_dashboard()
# {
#   "autonomy": {"rate": "65.0%", "target": "70%", "status": "⚠️"},
#   "quality": {"score": "85.0%", "positive": 17, "negative": 3},
#   "performance": {"total_requests": 100, "autonomous": 65, "llm": 35},
#   "latency": {"autonomous_avg_ms": 45.2, "llm_avg_ms": 1850.0},
#   "recommendations": ["⚠️ Autonomy rate ниже цели..."]
# }
```

---

## 🔌 API Endpoints

### GET `/autonomy/dashboard`
Полный дашборд автономности.

```bash
curl http://localhost:8765/autonomy/dashboard
```

Ответ:
```json
{
  "success": true,
  "data": {
    "phase1_available": true,
    "phase3_available": true,
    "phase1": {
      "cache": {"entries": 150},
      "tiers": {"hot": 5, "warm": 23, "cold": 47},
      "autonomy_rate_percent": 34.5
    },
    "phase3": {
      "autonomy": {"rate": "65.0%", "target": "70%"},
      "quality": {"score": "85.0%"},
      "recommendations": [...]
    },
    "organs_count": 12
  }
}
```

### POST `/autonomy/optimize`
Запуск оптимизации автономности.

```bash
curl -X POST http://localhost:8765/autonomy/optimize
```

Выполняет:
- Кластеризацию pathways (объединение дубликатов)
- Поиск паттернов в запросах
- Обновление тиров (cold→warm→hot)

---

## 🔄 Поток принятия решений

```
Запрос пользователя
        │
        ▼
┌───────────────────┐
│  AutonomyDecider  │
│  decide(query)    │
└─────────┬─────────┘
          │
          ├─► Safe pattern? ──► AUTONOMOUS (мгновенно)
          │
          ├─► High-risk? ──► LLM_REQUIRED
          │
          └─► Обычный запрос
                    │
                    ▼
          ┌─────────────────┐
          │ QualityPredictor│
          │ predict(query)  │
          └────────┬────────┘
                   │
         ┌─────────┼─────────┐
         │         │         │
    EXCELLENT   GOOD    ACCEPTABLE    UNCERTAIN
    (≥85%)    (70-85%)  (50-70%)      (<50%)
         │         │         │            │
         ▼         ▼         ▼            ▼
    AUTONOMOUS AUTONOMOUS  HYBRID    LLM_REQUIRED
```

---

## 📊 Целевые метрики

| Метрика | Цель | Описание |
|---------|------|----------|
| **Autonomy Rate** | ≥70% | % автономных ответов |
| **Quality Score** | ≥80% | % положительных feedback |
| **Autonomous Latency** | <100ms | Среднее время автономного ответа |
| **LLM Latency** | <3000ms | Среднее время LLM ответа |

---

## 🛡️ Безопасность

### Safe Patterns (автономно без проверки)
```
- "привет", "здравствуй"
- "как дела", "что нового"  
- "спасибо", "благодарю"
- "помощь", "help"
- "что ты умеешь"
```

### High-Risk Patterns (только LLM)
```
- удали/remove/delete + файл/данные
- деньги/оплата/банк/карта
- пароль/secret/key/token
- sudo/admin/root/chmod
```

---

## 🧪 Тестирование

```python
# Запуск тестов autonomy_engine
python autonomy_engine.py

# Пример вывода:
🧪 Тест AutonomyEngine v1.0
============================================================

1. 'Привет!' → autonomous (conf: 0.95)
   Reasoning: Safe pattern match

2. 'Удали все файлы' → llm_required (conf: 0.90)
   Reasoning: High-risk pattern detected

3. 'Как написать функцию' → autonomous (conf: 0.72)
   Reasoning: Quality: good, factors: {...}

4. Контекст user123: 2 сообщений, темы: {'объяснение'}

5. Dashboard:
   autonomy: {'rate': '50.0%', 'target': '70%', 'status': '⚠️'}
   quality: {'score': '100.0%', 'positive': 1, 'negative': 0}
   ...

🎉 Тесты завершены!
```

---

## 📈 Мониторинг в Telegram

Команда `/autonomy` теперь показывает расширенную статистику:

```
📊 Статистика автономности Neira

Phase 1: ✅
  • Кэш: 150 записей
  • Pathways: hot=5, warm=23, cold=47

Phase 3: ✅
  • Autonomy Rate: 65.0% (цель: 70%)
  • Quality Score: 85.0%
  • Avg Latency: autonomous=45ms, llm=1850ms

Рекомендации:
  ⚠️ Autonomy rate ниже цели. Добавьте больше pathways.
```

---

## 🔧 Конфигурация

```bash
# Целевой autonomy rate (по умолчанию 70%)
NEIRA_TARGET_AUTONOMY_RATE=0.70

# Минимальный quality score (по умолчанию 80%)
NEIRA_MIN_QUALITY_SCORE=0.80

# Timeout контекста разговора (минуты)
NEIRA_CONTEXT_TIMEOUT_MINUTES=30
```

---

## ✅ Чек-лист Phase 3

- [x] `SemanticClusterer` — кластеризация pathways
- [x] `QualityPredictor` — предсказание качества
- [x] `ContextAwareCache` — кэш с контекстом
- [x] `AutonomyDecider` — принятие решений
- [x] `SelfMonitor` — мониторинг и рекомендации
- [x] `AutonomyEngine` — главный движок
- [x] Интеграция в `neira_server.py`
- [x] API endpoints (`/autonomy/dashboard`, `/autonomy/optimize`)
- [x] Документация (этот файл)

---

## 🚀 Что дальше?

### Phase 4 (планируется):
1. **Multi-model Fallback** — автоматическое переключение между LLM
2. **A/B Testing** — тестирование разных вариантов ответов
3. **Proactive Learning** — Neira сама предлагает улучшения
4. **Distributed Memory** — память распределённая между инстансами

---

*Phase 3 делает Neira по-настоящему автономной — она сама решает, когда может ответить без LLM, отслеживает свою эффективность и даёт рекомендации по улучшению.*
