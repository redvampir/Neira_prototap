# ⚡ Web UI - Быстрый старт

## 🚀 Запуск одной командой

```bash
# Windows:
start_web_ui.bat

# Linux/Mac:
./start_web_ui.sh
```

---

## ✅ Что исправлено (14.12.2025)

### Проблемы с кнопками - РЕШЕНЫ ✅

1. **Кнопка "Отправить"**
   - ✅ Обработка ошибок
   - ✅ Визуальный feedback: Синяя → 🟠 Обработка... → Синяя
   - ✅ Защита от дублирования запросов

2. **Кнопка "🔄 Обновить"**
   - ✅ Показывает статус: ⏳ Обновление... → ✅ Обновлено → 🔄
   - ✅ Обработка ошибок API
   - ✅ Таймаут и повтор

3. **Общие улучшения**
   - ✅ Автоматический reconnect WebSocket
   - ✅ Понятные сообщения об ошибках
   - ✅ Диагностика в start_web_ui.bat

---

## 🔍 Проверка работоспособности

### 1. Индикатор подключения (правый верхний угол):
- 🟢 **Connected** = всё работает
- 🔴 **Disconnected** = backend не запущен

### 2. Кнопки реагируют:
- Клик по "Отправить" → кнопка становится оранжевой
- Клик по "🔄 Обновить" → появляется "⏳ Обновление..."

### 3. Консоль браузера (F12):
```
✅ Должно быть:
   WebSocket connected
   🧠 Neira Frontend loaded

❌ НЕ должно быть:
   Failed to create WebSocket
   Network error
```

---

## 🐛 Решение проблем

### Кнопка "Отправить" серая и не работает

**Причина:** WebSocket не подключен

**Решение:**
```bash
# 1. Проверь backend запущен
curl http://localhost:8000/

# 2. Если не отвечает - запусти:
cd backend
python api.py

# 3. Обнови страницу (F5)
```

---

### Кнопка "Обновить" показывает "❌ Ошибка"

**Причина:** API /stats не отвечает

**Решение:**
```bash
# 1. Проверь endpoint
curl http://localhost:8000/api/stats

# 2. Посмотри логи backend
# В окне "Neira Backend" должно быть:
# INFO: "GET /api/stats HTTP/1.1" 200 OK

# 3. Если 404 или 500 - перезапусти backend
```

---

### "Failed to fetch stats" в консоли

**Причина:** Backend не запущен или CORS блокирует

**Решение:**
```bash
# 1. Убедись backend запущен:
curl http://localhost:8000/

# 2. Проверь CORS в backend/api.py:
# Должно быть: allow_origins=["*"]

# 3. Попробуй через localhost:
cd frontend
python -m http.server 8080
# Открой: http://localhost:8080/
```

---

## 📚 Полная документация

Смотри [WEB_UI_GUIDE.md](WEB_UI_GUIDE.md) для:
- Ручного запуска по шагам
- Всех API endpoints
- Конфигурации и кастомизации
- Мобильной версии
- Отладки

---

## 💡 Быстрые проверки

### Backend запущен?
```bash
curl http://localhost:8000/
# Должен вернуть: {"status":"ok",...}
```

### WebSocket работает?
Открой консоль (F12):
```javascript
window.NeiraDebug.reconnect()
// Должно появиться: WebSocket connected
```

### Статистика обновляется?
```javascript
window.NeiraDebug.getStats()
// Должны обновиться цифры в правой панели
```

---

## 🎯 Изменённые файлы

- [frontend/app.js](frontend/app.js) - обработка кнопок
- [frontend/style.css](frontend/style.css) - стили processing
- [start_web_ui.bat](start_web_ui.bat) - диагностика

---

## 🆘 Всё ещё не работает?

1. **Перезапусти всё по порядку:**
   ```bash
   # 1. Ollama
   ollama serve
   
   # 2. Backend
   cd backend
   python api.py
   
   # 3. Frontend (обнови F5)
   ```

2. **Жёсткое обновление страницы:**
   - Windows: `Ctrl+F5`
   - Mac: `Cmd+Shift+R`

3. **Проверь порт 8000 свободен:**
   ```bash
   netstat -ano | findstr :8000
   ```

4. **Логи:**
   - Backend: окно "Neira Backend"
   - Frontend: консоль браузера (F12)
   - Ollama: терминал с `ollama serve`
