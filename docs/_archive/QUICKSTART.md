# ⚡ Быстрый старт Neira v0.8

## 🌐 Выбор режима работы

Нейра поддерживает **3 режима** работы с LLM:

### 1️⃣ Локальный (Ollama only)
```bash
start.sh  # Linux/Mac
start.bat # Windows
```
- ✅ Бесплатно и приватно
- ✅ Работает без интернета
- ❌ Требует GPU (4+ GB VRAM)

### 2️⃣ Гибридный (Ollama + Cloud)
```bash
start_hybrid.bat  # Рекомендуется!
```
- ✅ Локально когда можно, облако как fallback
- ✅ Высокая надёжность
- ⚠️ Требует API ключи для fallback

### 3️⃣ Cloud-only (без Ollama)
```bash
start_cloud_only.bat
```
- ✅ Не требует GPU/VRAM
- ✅ Работает на любом ПК
- ❌ Требует API ключи и интернет

---

## 🚀 Первый запуск

### Вариант А: С Ollama (локально)

```bash
# 1. Установка Ollama
# Windows: скачай с https://ollama.com/download
# Linux/Mac: curl -fsSL https://ollama.com/install.sh | sh

# 2. Установка зависимостей
pip install -r requirements.txt

# 3. Запуск
./start.sh      # Linux/Mac
start.bat       # Windows
```

### Вариант Б: Без Ollama (cloud-only)

```bash
# 1. Создай .env с API ключами
GROQ_API_KEY=gsk_...              # Бесплатно: https://console.groq.com/keys
OPENAI_API_KEY=sk-...             # Опционально: https://platform.openai.com/api-keys
ANTHROPIC_API_KEY=sk-ant-...      # Опционально для Claude

# 2. Установка зависимостей
pip install -r requirements.txt

# 3. Запуск
start_cloud_only.bat  # Windows
# Linux/Mac: export PROVIDER_PRIORITY=groq,openai && python telegram_bot.py
```

---

## 📋 Провайдеры LLM

| Провайдер | Стоимость | Скорость | Качество | API Key |
|-----------|-----------|----------|----------|---------|
| **Ollama** | 🆓 Бесплатно | ⚡ Быстро | ⭐⭐⭐ | Не требуется |
| **Groq** | 🆓 Бесплатно | ⚡⚡⚡ Очень быстро | ⭐⭐⭐⭐ | `GROQ_API_KEY` |
| **OpenAI** | 💰 $0.002/1K tokens | ⚡⚡ Средне | ⭐⭐⭐⭐⭐ | `OPENAI_API_KEY` |
| **Claude** | 💰 $0.003/1K tokens | ⚡⚡ Средне | ⭐⭐⭐⭐⭐ | `ANTHROPIC_API_KEY` |

**Рекомендация:** Используй гибридный режим (Ollama + Groq) — бесплатно и надёжно!

---

## 💡 Конфигурация провайдеров

### .env файл
```bash
# Приоритет провайдеров (слева направо)
PROVIDER_PRIORITY=ollama,groq,openai,claude

# Режим работы
NEIRA_MODE=hybrid  # или cloud, или local

# API ключи (только для облачных провайдеров)
GROQ_API_KEY=gsk_your_key_here
OPENAI_API_KEY=sk-your_key_here
ANTHROPIC_API_KEY=sk-ant-your_key_here

# Telegram бот
BOT_TOKEN=your_telegram_bot_token
```

---

## 🎯 Первые команды

```bash
# Справка
/help

# Проверка провайдеров
/providers

# Проверка системы
/stats

# Простой запрос
Привет! Что ты умеешь?

# Память и опыт
/memory
/experience
```

---

## 🐛 Проблемы?

### "Ни один LLM провайдер не доступен"
```bash
# Проверь:
1. Ollama запущен? → tasklist | find "ollama"
2. API ключи в .env? → cat .env
3. Интернет доступен? → ping google.com

# Решение:
start_hybrid.bat  # Автоматически проверит всё
```

### Ollama ошибка "out of memory"
```bash
# Переключись на cloud:
start_cloud_only.bat

# Или освободи VRAM:
taskkill /f /im ollama.exe
start.bat
```

### "API key not configured"
```bash
# Создай .env файл:
echo GROQ_API_KEY=gsk_... > .env
echo OPENAI_API_KEY=sk-... >> .env

# Перезапусти:
start_cloud_only.bat
```
```bash
# Проверьте версию
python3 --version  # Должна быть 3.10+

# Установите зависимости
pip install requests
```

---

## 📚 Документация

- **QUICKSTART.md** ← вы здесь (быстрый старт)
- **SETUP.md** — подробная инструкция по установке
- **EVOLUTION.md** — система эволюции v0.6
- **README.md** — общая информация о проекте

---

## 🎓 Системные требования

| Компонент | Минимум | Рекомендуется |
|-----------|---------|---------------|
| Python | 3.10+ | 3.11+ |
| RAM | 8 GB | 16 GB |
| Диск | 10 GB | 20 GB |
| ОС | Linux, macOS, Windows (WSL) | Linux/macOS |

---

## ✅ Готово!

После запуска `./start.sh` вы увидите:

```
╔══════════════════════════════════════════════════════════════╗
║           NEIRA v0.6 — AI ассистент с эволюцией             ║
╚══════════════════════════════════════════════════════════════╝

Введи запрос или /help для справки
>
```

Начинайте работать! 🚀
