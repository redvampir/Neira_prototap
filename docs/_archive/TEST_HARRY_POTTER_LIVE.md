# 🎮 Harry Potter Game — Live Test Guide

## ✅ Backend уже запущен!

Backend работает на **http://localhost:8001**

## 🧪 Как протестировать

### Вариант 1: HTML WebSocket Client (рекомендуется)

1. **Открой файл в браузере**:
   ```
   f:\Нейронки\prototype\test_websocket.html
   ```
   
2. **Нажми "🔌 Подключиться"**

3. **Отправь запрос** (кнопка уже настроена на TicTacToe, но можно изменить):
   
   Открой DevTools (F12) → Console → Выполни:
   ```javascript
   const request = {
       content: "Создай мини-игру в стиле Гарри Поттера. Нужна смесь аркады и квеста с UI, управлением клавишами WASD/стрелки и встроенным чатом для общения с Нейрой. Игрок перемещается по Хогвартсу (простой 2D вид сверху), собирает магические артефакты (иконки на поле) и решает загадки. Минимум: игровое поле 10x10, персонаж-точка, 3-5 артефактов, счетчик очков, чат-окно справа для вопросов Нейре.",
       use_memory: true
   };
   ws.send(JSON.stringify(request));
   ```

4. **Наблюдай за логами**:
   - `[CELL:ui_code_cell]` — Cell Router сработал ✅
   - `artifact` — HTML игра сгенерирована ✅
   - Артефакт откроется автоматически

### Вариант 2: Python WebSocket Client

1. **Открой новый PowerShell** (не закрывай backend!)

2. **Запусти тест**:
   ```bash
   cd f:\Нейронки\prototype
   python test_harry_potter.py
   ```

3. **Результат**:
   - Streaming ответ от Neira
   - HTML сохранится в `harry_potter_game.html`
   - Открой в браузере: `start harry_potter_game.html`

### Вариант 3: cURL (быстрая проверка)

```bash
curl -X POST http://localhost:8001/chat \
  -H "Content-Type: application/json" \
  -d "{\"content\":\"Создай простую игру в стиле Гарри Поттера\",\"use_memory\":true}"
```

## 🔍 Что проверять

### Cell Router работает правильно:
- ✅ Response содержит `[CELL:ui_code_cell]`
- ✅ `artifact` type в WebSocket message
- ✅ HTML файл генерируется

### UICodeCell выбирает template:
- Проверь логи backend терминала:
  ```
  [UICodeCell.generate_ui] Generating UI...
  [UICodeCell._select_template] Template 'rpg_inventory' selected (score=X)
  ```

### Игра работает:
- **UI/UX**: Игровое поле видно
- **Управление**: Клавиши WASD/стрелки двигают персонажа  
- **Чат с Neira**: Поле ввода + кнопка отправки
- **Артефакты**: Объекты на поле
- **Счётчик**: Очки/прогресс

## 🐛 Если что-то не работает

### Backend не отвечает:
```bash
# Проверь что backend запущен:
curl http://localhost:8001/

# Должен вернуть:
# {"status":"ok","service":"Neira API",...}
```

### WebSocket не подключается:
- Проверь консоль браузера (F12 → Console)
- Должно быть: `WebSocket connection to 'ws://localhost:8001/ws/chat' failed`
- **Решение**: Перезапусти backend

### Артефакт не генерируется:
- Проверь backend логи на наличие:
  ```
  [process_stream] Cell Router decided: should_use=True
  [UICodeCell.generate_ui] Template 'X' selected
  ```
- Если нет — модель не генерирует `[CELL:...]` directive

### Модель генерирует код вместо артефакта:
- **Ожидаемо**: Модель может генерировать Python код после `[CELL:ui_code_cell]`
- **Cell Router обрабатывает**: Извлекает directive и обрезает лишнее
- **Проверь**: Артефакт всё равно должен появиться

## 📊 Ожидаемый результат

```
✅ WebSocket connected
✅ Request sent: "Создай игру в стиле Гарри Поттера..."
✅ Cell Router detected: [CELL:ui_code_cell]
✅ UICodeCell selected template: rpg_inventory (или custom)
✅ Artifact generated: 15KB+ HTML
✅ Game opened in browser
✅ Controls work (WASD/arrows)
✅ Chat with Neira active
```

## 🚀 Next Steps

Если всё работает:
1. **Тестируй разные запросы**: "Создай интерфейс для...", "Сделай игру..."
2. **Проверь resonance адаптацию**: UICodeCell должен менять CSS на основе 0-1 scale
3. **Добавь новые templates** в `ui_code_cell_templates.json`
4. **Улучши training dataset** для лучшего fine-tuning

---

**Backend запущен и ждёт твоих запросов!** 🎯

Terminal: `Terminal ID: 0217aa27-8064-49ad-bd5e-2d870e6c0981`
