# 🧪 Инструкция по тестированию Neira v0.7

## Быстрый тест функционала

### 1. Проверка исправления ошибок
```bash
# Запустить Pylance проверку (в VS Code)
# Не должно быть ошибок в:
- brain_enhancer.py
- test_organ_telegram.py
- telegram_bot.py
```

### 2. Тест параллельного мышления

#### Запуск бота:
```bash
python telegram_bot.py
```

#### В Telegram (от двух разных пользователей):
```
Пользователь А:
  1. Привет, я Алиса
  2. Запомни: мой любимый цвет синий
  3. /context

Пользователь Б:
  1. Привет, я Боб
  2. Запомни: мой любимый цвет красный
  3. /context

Пользователь А (снова):
  4. Какой мой любимый цвет?
  → Должен ответить "синий" (не перепутает с Бобом!)

Пользователь Б (снова):
  4. Какой мой любимый цвет?
  → Должен ответить "красный"
```

**Ожидаемый результат:**
- ✅ У каждого пользователя своя история
- ✅ Neira не путает контексты разных чатов
- ✅ `/context` показывает только сообщения этого чата

---

### 3. Тест улучшенной авторизации

#### В Telegram (как администратор):
```bash
# Тест 1: Добавление по username
/admin add @testuser
→ ✅ Пользователь @testuser добавлен

# Тест 2: Добавление по ссылке
/admin add t.me/testuser2
→ ✅ Пользователь добавлен

# Тест 3: Добавление по user_id (старая система)
/admin add 123456789
→ ✅ Пользователь 123456789 добавлен

# Тест 4: Просмотр всех пользователей
/admin users
→ Показывает обе системы:
  • 123456789 (старая система)
  • 987654321 @testuser — Алиса
  • 555444333 @testuser2 — Боб

# Тест 5: Статистика
/admin stats
→ Показывает:
  🗨️ Активных чатов: 2
  💬 Всего сообщений: 8
  👥 Уникальных пользователей: 2
  Топ-5 активных чатов...
```

---

### 4. Тест новых команд пользователя

```bash
# Тест 1: Просмотр контекста
Пользователь: /context
→ Показывает последние 10 сообщений диалога

# Тест 2: Очистка контекста
Пользователь: /clear_context
→ 🗑️ Контекст диалога очищен!

# Проверка очистки:
Пользователь: /context
→ 📭 История диалога пуста
```

---

### 5. Тест самообучения (регрессия)

Убедиться что старый функционал работает:
```bash
Пользователь: #создай_орган орган для игры в шахматы
→ 🧬 Начинаю создавать новый орган...
→ [фоновое создание]
→ ✅ Орган создан: chess_cell.py
```

---

## Проверка файлов

### Создаваемые файлы:
```bash
# Контексты чатов
ls neira_chat_contexts.json
→ должен содержать структуру:
{
  "123456": {
    "chat_id": 123456,
    "user_id": 111,
    "username": "testuser",
    "messages": [...]
  }
}

# Авторизованные пользователи
ls neira_authorized_users.json
→ должен содержать:
{
  "987654321": {
    "user_id": 987654321,
    "username": "testuser",
    "name": "Алиса"
  }
}
```

---

## Проверка логов

### В консоли бота должно быть:
```
[INFO] Загружено контекстов: 2
[INFO] Авторизовано пользователей: 3 (новая система)
[DEBUG] Создан новый контекст для чата 123456
[DEBUG] Добавлено сообщение в чат 123456: user
[DEBUG] Добавлено сообщение в чат 123456: assistant
```

---

## Чек-лист перед релизом

- [ ] Нет ошибок Pylance в ключевых файлах
- [ ] Параллельное мышление работает (разные чаты не путаются)
- [ ] Контекст сохраняется после перезапуска бота
- [ ] `/context` показывает правильную историю
- [ ] `/clear_context` очищает только текущий чат
- [ ] `/admin add` принимает @username и ссылки
- [ ] `/admin users` показывает обе системы
- [ ] `/admin stats` показывает статистику
- [ ] Старая авторизация (AUTHORIZED_USERS) продолжает работать
- [ ] Самообучение через #создай_орган не сломалось

---

## Известные ограничения

1. **Глубина истории**: 50 сообщений на чат (можно увеличить в parallel_thinking.py)
2. **Обратная совместимость**: Старая система AUTHORIZED_USERS не синхронизируется с новой
3. **Username resolution**: Нужен user_id для добавления по username (бот должен видеть пользователя)

---

## Rollback план

Если что-то сломалось:
```bash
# 1. Восстановить старую версию
git checkout HEAD~1 telegram_bot.py

# 2. Удалить новые файлы
rm parallel_thinking.py
rm enhanced_auth.py
rm neira_chat_contexts.json
rm neira_authorized_users.json

# 3. Перезапустить
python telegram_bot.py
```

---

## Следующие шаги

После успешного тестирования:
1. Обновить `QUICKSTART.md` с новыми командами
2. Добавить примеры в `TELEGRAM.md`
3. Создать migration скрипт для переноса AUTHORIZED_USERS → новая система
4. Рассмотреть интеграцию контекста в BrainEnhancer

---

**Happy testing!** 🚀
