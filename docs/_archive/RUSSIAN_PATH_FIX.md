# 🔧 Решение проблемы русских путей в Windows

## Проблема
Многие инструменты (OpenCode, npm, некоторые Python пакеты) не работают с кириллицей в путях:
- `F:\Нейронки\prototype` ❌ Кириллица вызывает ошибки

Важно: исправление пути (junction/subst) **не устанавливает** `opencode`/OpenCode. Оно лишь даёт инструментам путь **без кириллицы**, чтобы они не падали.

## Решения

### ✅ Вариант 1: Junction (символическая ссылка) — РЕКОМЕНДУЕТСЯ

**Создать симлинк без кириллицы:**

```batch
# Запустить от имени администратора (если попросит)
fix_russian_path.bat
```

После этого:
```powershell
cd /d C:\neira_work  # Теперь это тот же проект, но без кириллицы
<ваша_команда> --port 27418
```

**Преимущества:**
- ✅ Не нужно копировать файлы
- ✅ Все изменения синхронизируются автоматически
- ✅ Занимает 0 байт дополнительно
- ✅ Работает для всех инструментов

**Как это работает:**
- `C:\neira_work` → junction → `F:\Нейронки\prototype`
- Windows перенаправляет все операции автоматически
- Для приложений это выглядит как обычная папка

### ✅ Вариант 2: Subst (виртуальный диск)

```batch
# Создать виртуальный диск N: без кириллицы
subst N: F:\Нейронки\prototype
cd /d N:\
<ваша_команда> --port 27418
```

**Удалить виртуальный диск:**
```batch
subst N: /D
```

**Недостаток:** Нужно создавать при каждой перезагрузке Windows

### ✅ Вариант 3: Автозапуск через PowerShell Profile

**Добавить в профиль PowerShell:**

```powershell
# Открыть профиль
notepad $PROFILE

# Добавить в конец файла:
function neira {
    $env:PYTHONUTF8 = "1"
    $env:PYTHONIOENCODING = "utf-8"
    Set-Location "F:\Нейронки\prototype"
}

# Создать виртуальный диск автоматически
if (-not (Test-Path N:\)) {
    subst N: "F:\Нейронки\prototype" 2>$null
}
```

**Использование:**
```powershell
neira  # Быстрый переход + настройка кодировок
cd N:\  # Альтернатива без кириллицы
```

## Для конкретно OpenCode

Точное значение “OpenCode/opencode” зависит от того, **какой проект** вы имеете в виду и как он устанавливается (GitHub release, cargo, pipx, npm и т.д.).

### 1) Быстрая диагностика: установлен ли `opencode` вообще

```powershell
Get-Command opencode -ErrorAction SilentlyContinue
where.exe opencode 2>$null
```

Если ничего не найдено — команда не установлена/не в PATH.

### 2) Обход кириллицы (после junction/subst)

**После создания junction:**

```powershell
cd C:\neira_work
<команда_opencode_или_другая> --port 27418
```

**Или с виртуальным диском:**

```powershell
subst O: F:\Нейронки\prototype
cd O:\
<команда_opencode_или_другая> --port 27418
```

### 3) Как понять, что это за “OpenCode”

- Если у вас есть ссылка на репозиторий/инструкцию — откройте README: там будет точная команда запуска и установка.
- Если вы видели команду вида `... --port 39468`, чаще всего это сервер, который **поставляется отдельным бинарём** (и его нужно установить/добавить в PATH).

## Проверка результата

```powershell
# Должны показывать одинаковое содержимое
dir F:\Нейронки\prototype
dir C:\neira_work  # Если создали junction
dir N:\  # Если создали subst
```

## PowerShell UTF-8 Fix (дополнительно)

Если PowerShell всё равно глючит с кириллицей:

```powershell
# Установить UTF-8 для текущей сессии
chcp 65001
[Console]::OutputEncoding = [System.Text.Encoding]::UTF8
$env:PYTHONUTF8 = "1"

# Или добавить в $PROFILE для автозапуска
```

## Рекомендация для будущих проектов

**Избегайте кириллицы в путях проектов:**
- ✅ `C:\Projects\neira` 
- ✅ `D:\Dev\ai-assistant`
- ❌ `F:\Нейронки\prototype`
- ❌ `C:\Пользователи\Имя\Проекты`

Это сэкономит часы отладки странных ошибок.

## Удаление junction (если понадобится)

```batch
# Удалить junction (НЕ удаляет исходные файлы!)
rmdir C:\neira_work

# Проверить что это junction а не обычная папка:
dir C:\ | findstr JUNCTION
```

## FAQ

**Q: Безопасно ли использовать junction?**  
A: Да, это стандартная функция NTFS. Удаление junction не затронет исходные файлы.

**Q: Можно ли создать несколько junction?**  
A: Да, хоть 10 штук на разные диски.

**Q: Работает ли с VS Code?**  
A: Да, VS Code отлично работает с junction. Просто откройте `C:\neira_work`

**Q: Как удалить junction?**  
A: `rmdir C:\neira_work` — это удалит только ссылку, не файлы!

---

**🎯 TL;DR:**
1. Запусти `fix_russian_path.bat` от админа
2. Используй `C:\neira_work` вместо `F:\Нейронки\prototype`
3. Profit! Всё работает без кириллицы.
