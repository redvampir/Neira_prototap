# 🛡️ ЗАЩИТА ОТ ПЕРЕПОЛНЕНИЯ ПАМЯТИ v2.4

## Проблема
Нейра может накапливать неограниченное количество записей в памяти, что приводит к:
- Замедлению работы
- Росту файлов памяти (neira_memory.json может занимать мегабайты)
- Дубликатам и низкокачественным записям
- Переполнению диска

## Решение

### 1. Автоматическая защита (встроена в MemorySystem)

**Лимиты:**
- Долгосрочная память: 1000 записей максимум
- Краткосрочная память: 100 записей максимум
- Семантическая память: 500 записей
- Эпизодическая память: 300 записей
- Минимальная уверенность: 0.3 (записи ниже удаляются)

**Как работает:**
- Проверка лимитов при загрузке системы
- Автоматическая очистка каждые 10 новых записей
- Удаление записей с низкой уверенностью
- Приоритизация по: уверенность → частота доступа → свежесть

**Настройка** (в `memory_system.py`):
```python
self.max_long_term = 1000        # Максимум долгосрочных
self.max_semantic = 500          # Максимум знаний
self.max_episodic = 300          # Максимум событий
self.min_confidence_keep = 0.3   # Минимальная уверенность
self.auto_cleanup_enabled = True # Вкл/выкл автоочистку
```

### 2. Ручная очистка (memory_cleanup.py)

**Использование:**
```bash
python memory_cleanup.py
```

**Опции:**
1. **Полная очистка (сохранить важное)** - удаляет всё кроме топ-50 записей с confidence > 0.8
2. **Полная очистка (удалить всё)** - стирает всю память (создаёт бэкап)
3. **Применить лимиты (оптимизация)** - умная очистка с сохранением структуры
4. **Только статистика** - показывает текущее состояние

**Что делает оптимизация:**
- Удаляет дубликаты
- Фильтрует по уверенности (confidence >= 0.3)
- Применяет лимиты по типам
- Сохраняет баланс между типами памяти

### 3. Бэкапы

**Автоматически создаются:**
- При полной очистке
- Папка: `memory_backups/`
- Формат: `neira_memory_backup_YYYYMMDD_HHMMSS.json`

**Восстановление:**
```bash
# Найти последний бэкап
ls memory_backups/ | sort

# Восстановить
cp memory_backups/neira_memory_backup_20251214_061717.json neira_memory.json
```

## Мониторинг

### Проверка размера памяти
```python
from memory_system import MemorySystem

ms = MemorySystem()
stats = ms.get_memory_stats()
print(f"Total records: {stats['total']}")
print(f"By type: {stats['by_type']}")
```

### Показать статистику
```bash
python -c "
import json
with open('neira_memory.json', 'r', encoding='utf-8') as f:
    mem = json.load(f)
print(f'Memory size: {len(mem)} records')
print(f'File size: {len(json.dumps(mem)) / 1024:.2f} KB')
"
```

## Рекомендации

### Когда очищать:
- **Еженедельно**: Применять оптимизацию (опция 3)
- **При замедлении**: Если Нейра тормозит - проверить размер памяти
- **При переобучении**: Если Нейра "зациклилась" - полная очистка (опция 1)
- **Перед важным тестом**: Чистый старт (опция 2)

### Безопасная работа:
1. Всегда создаётся бэкап перед очисткой
2. Важные записи (confidence > 0.8) сохраняются
3. Автоматическая защита работает в фоне
4. Можно отключить автоочистку: `auto_cleanup_enabled = False`

## Квантование vs Очистка

**Почему НЕ квантование:**
- Квантование сжимает модель, но не решает проблему роста данных
- Память - это данные, а не веса модели
- Нужен контроль объёма данных, а не точности вычислений

**Наш подход:**
- Умная приоритизация записей
- Автоматическое забывание неважного
- Сохранение только ценной информации
- Баланс между объёмом и качеством

## Примеры

### Очистка перед тестированием
```bash
# Полная очистка с сохранением важного
echo 1 | python memory_cleanup.py
```

### Ежедневная оптимизация
```bash
# Применить лимиты
echo 3 | python memory_cleanup.py
```

### Экстренная очистка
```bash
# Удалить всё
echo 2 | python memory_cleanup.py
```

## Технические детали

### Алгоритм приоритизации:
1. **Уверенность (confidence)** - главный критерий
2. **Частота доступа (access_count)** - используемые записи важнее
3. **Свежесть (timestamp)** - новые записи предпочтительнее старых
4. **Тип памяти** - долгосрочная важнее краткосрочной

### Защита от зацикливания:
- Проверка последних 50 записей на дубликаты
- Автоматическое объединение похожих записей
- Контроль за повторяющимися паттернами

### Метрики качества:
- Уникальность: % уникальных записей
- Средняя уверенность: среднее confidence
- Использование лимита: текущий_размер / max_размер

## FAQ

**Q: Как часто срабатывает автоочистка?**  
A: Каждые 10 новых записей проверяются лимиты.

**Q: Можно ли отключить автоматическую очистку?**  
A: Да, установите `auto_cleanup_enabled = False` в memory_system.py.

**Q: Безопасно ли удалять всю память?**  
A: Да, всегда создаётся бэкап. Можно восстановить из `memory_backups/`.

**Q: Как понять, что пора чистить память?**  
A: Запустите `python memory_cleanup.py` и выберите опцию 4 (статистика).

**Q: Что будет если память переполнится?**  
A: Автоматически удалятся старые записи с низкой уверенностью.

## Обновления

**v2.4 (14.12.2025)**
- ✅ Автоматическая защита от переполнения
- ✅ Умная приоритизация записей
- ✅ Автоматические бэкапы
- ✅ Инструмент ручной очистки
- ✅ Мониторинг и статистика

**Следующие шаги:**
- [ ] Web-интерфейс для управления памятью
- [ ] Визуализация графа знаний
- [ ] Экспорт важных воспоминаний
- [ ] Умное слияние похожих записей
