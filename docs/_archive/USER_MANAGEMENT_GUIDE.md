# Инструкция по управлению пользователями в Telegram боте Neira

## Обзор системы авторизации

Бот Neira использует **трехуровневую систему доступа**:

1. **Open** - доступен всем (небезопасно)
2. **Whitelist** - только авторизованные пользователи
3. **Admin Only** - только администратор

По умолчанию установлен режим **Whitelist**.

## Как добавить нового пользователя

### Шаг 1: Получить идентификатор пользователя

Пользователь должен отправить боту любое сообщение. Бот ответит:
```
🔐 Требуется авторизация

Твой user_id: `123456789`
```

Или попросите пользователя выполнить команду `/myid` (если бот запущен).

### Шаг 2: Добавить пользователя (только администратор)

Используйте одну из команд:

```
/admin add 123456789
/admin add @username
/admin add https://t.me/username
```

**Примеры:**
- `/admin add 1044358267` - добавить по user_id
- `/admin add @alena_user` - добавить по username
- `/admin add https://t.me/alena_user` - добавить по ссылке

### Шаг 3: Проверить добавление

```
/admin users
```

Бот покажет список всех авторизованных пользователей.

## Как пользователь может установить своё имя

После авторизации пользователь может:

```
/myname Алёна
```

Бот запомнит имя и будет использовать его в разговорах.

## Удаление пользователя

```
/admin remove 123456789
/admin remove @username
/admin remove https://t.me/username
```

## Режимы доступа

### Изменить режим (только администратор):

```
/admin mode open        # Доступ всем
/admin mode whitelist   # Только авторизованные
/admin mode admin_only  # Только админ
```

### Проверить текущий режим:

```
/admin
```

## Авторизация администратора

Если вы администратор и забыли пароль:

1. Установите переменную `NEIRA_ADMIN_PASSWORD` в файле `.env`
2. Или используйте команду `/auth 0 <ваш_пароль>`

## Проблемы и решения

### "Не могу установить имя новому пользователю"

**Причина:** Пользователь не авторизован.

**Решение:**
1. Добавьте пользователя командой `/admin add`
2. После добавления он сможет использовать `/myname`

### "Пользователь добавлен, но не может писать боту"

**Причина:** Режим доступа не whitelist.

**Решение:** Установите `/admin mode whitelist`

### "Не знаю user_id пользователя"

**Решение:**
- Попросите пользователя написать боту любое сообщение
- Бот покажет его user_id
- Или используйте username: `/admin add @username`

## Безопасность

- **Никогда не делитесь паролем администратора**
- **Регулярно проверяйте список пользователей:** `/admin users`
- **Удаляйте неактивных пользователей:** `/admin remove <id>`
- **Используйте сложные пароли** для админа (минимум 10 символов)

## Пример добавления пользователя Алёна

```
1. Пользователь пишет боту: "Привет"
2. Бот отвечает: "Твой user_id: `1044358267`"
3. Админ выполняет: `/admin add 1044358267`
4. Бот подтверждает: "✅ Пользователь 1044358267 добавлен"
5. Пользователь может: `/myname Алёна`
6. Теперь Алёна авторизована и может общаться с ботом
```