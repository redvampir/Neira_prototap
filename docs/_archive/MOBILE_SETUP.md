# 📱 Инструкция: Как играть по сети с телефона

## 🎯 Суть проблемы

**Почему localhost не работает с телефона:**
- `localhost` = "этот компьютер"
- Телефон — это другое устройство
- Нужен IP-адрес компьютера в локальной сети

## ✅ Пошаговая инструкция

### Шаг 1: Запустить сервер на компьютере

```bash
python start_multiplayer.py
```

Вы увидите примерно такое:

```
============================================================
🎮 HARRY POTTER MULTIPLAYER SERVER
============================================================

✅ Сервер запущен!

📍 Локальный IP: 192.168.1.100
📍 Порт: 8003

🖥️  На этом компьютере используй:
   ws://localhost:8003/game

📱 На телефоне/другом устройстве используй:
   ws://192.168.1.100:8003/game

🌐 Для доступа с телефона открой в браузере:
   http://192.168.1.100:8003/

⚠️  Убедись, что телефон в той же WiFi сети!
============================================================
```

### Шаг 2: На телефоне открыть браузер

**Важно:** Телефон должен быть подключён к той же WiFi, что и компьютер!

В браузере на телефоне набрать адрес, который показал сервер:
```
http://192.168.1.100:8003/
```

(Вместо `192.168.1.100` будет ВАШ локальный IP)

### Шаг 3: Войти в игру

1. **Имя игрока**: любое (например: "Гермиона")
2. **Room ID**: hogwarts (или любое другое - главное чтобы одинаковое у всех)
3. **Адрес сервера**: уже заполнен автоматически!
4. Нажать "Войти в игру"

### Шаг 4: Играть!

- **На компьютере**: клавиши WASD или стрелки
- **На телефоне**: сенсорные кнопки (свайпы) или экранные кнопки

## 🔧 Если не работает

### Проблема 1: "Не могу подключиться"

**Решение:**
1. Проверить что телефон в той же WiFi сети
2. Проверить что ввели правильный IP (тот что показал сервер)
3. Проверить Windows Firewall:

```powershell
# В PowerShell от имени администратора:
New-NetFirewallRule -DisplayName "Harry Potter Game" -Direction Inbound -LocalPort 8003 -Protocol TCP -Action Allow
```

### Проблема 2: "Не знаю свой IP"

**Узнать IP вручную:**

```powershell
ipconfig
```

Ищите строку `IPv4 Address` в разделе вашего WiFi адаптера.
Обычно выглядит как: `192.168.X.X` или `10.0.X.X`

### Проблема 3: "Сервер не запускается"

Проверить что порт 8003 свободен:

```powershell
# Убить процесс на порту 8003:
Get-NetTCPConnection -LocalPort 8003 -ErrorAction SilentlyContinue | 
    Select-Object -ExpandProperty OwningProcess | 
    ForEach-Object { Stop-Process -Id $_ -Force }
```

## 🎮 Управление на телефоне

HTML страница адаптивная и будет работать на телефоне!

**Управление:**
- Свайпы в разные стороны для движения
- ИЛИ подключить Bluetooth геймпад
- ИЛИ использовать виртуальные кнопки на экране

## 🌐 Альтернатива: Через файл

Если HTTP не работает, можно скинуть файл на телефон:

1. Скопировать `harry_potter_mobile.html` на телефон
2. Открыть в браузере Chrome/Safari
3. В поле "Адрес сервера" вручную вписать:
   ```
   ws://192.168.X.X:8003/game
   ```
   (где X.X - ваш IP)

## 🔒 Безопасность

**Важно:** Игра доступна всем в вашей локальной сети!

Чтобы ограничить доступ:
- Использовать пароли на комнаты (Room ID как пароль)
- Запускать сервер только когда играете
- Не открывать порт в роутере (не пробрасывать наружу)

## 📊 Проверка подключения

**На компьютере проверить что сервер слушает:**

```powershell
netstat -an | findstr 8003
```

Должно быть:
```
TCP    0.0.0.0:8003    0.0.0.0:0    LISTENING
```

## 🎯 Быстрая диагностика

| Проблема | Решение |
|----------|---------|
| "localhost не работает с телефона" | Используй IP вместо localhost |
| "Не могу подключиться" | Проверь WiFi и firewall |
| "Не знаю IP" | `ipconfig` в PowerShell |
| "Порт занят" | Убей процесс или смени порт |
| "Телефон не видит сервер" | Проверь что оба в одной WiFi |

## 🚀 Запуск одной командой

Создал скрипт `start_multiplayer.py` который:
- ✅ Автоматически определяет IP
- ✅ Показывает все нужные адреса
- ✅ Запускает сервер на всех интерфейсах
- ✅ Выводит инструкции для подключения

Просто запусти:
```bash
python start_multiplayer.py
```

И следуй инструкциям на экране!
