# 📱 Удалённый доступ к Нейре

Инструкция по настройке доступа к Нейре с телефона или другого устройства через интернет.

## 🔐 Твои учётные данные (захардкожены)

```
Логин:  0
Пароль: 11111111
```

> **Символика:** 8 единиц в двоичном = 255 → при переполнении бита = 0

Эти данные захардкожены в коде и не могут быть изменены или перехвачены через переменные окружения.

---

## 🚀 Способ 1: Tailscale (рекомендуется)

**Tailscale** создаёт приватную VPN-сеть между твоими устройствами. Самый безопасный вариант.

### Шаг 1: Установка на компьютер

1. Скачай Tailscale: https://tailscale.com/download
2. Установи и создай аккаунт (можно через Google/GitHub)
3. Нажми "Connect" в трее Windows

### Шаг 2: Установка на телефон

**Android:**
1. Google Play → "Tailscale" → Установить
2. Войди в тот же аккаунт

**iPhone:**
1. App Store → "Tailscale" → Установить  
2. Войди в тот же аккаунт

### Шаг 3: Запуск Нейры

На компьютере запусти:

```batch
.\start_tailscale.bat
```

Скрипт покажет Tailscale IP (например `100.64.0.1`)

### Шаг 4: Подключение с телефона

1. Убедись что Tailscale включен на телефоне
2. Открой браузер
3. Введи адрес: `http://100.64.0.1:8000` (подставь свой IP)
4. Введи логин `0` и пароль `11111111`

**Готово!** 🎉

---

## 🌐 Способ 2: ngrok (публичный доступ)

**ngrok** создаёт публичный URL для твоего localhost. Проще настроить, но менее безопасно.

### Шаг 1: Установка ngrok

1. Зарегистрируйся: https://ngrok.com (бесплатно)
2. Скачай ngrok: https://ngrok.com/download
3. Распакуй `ngrok.exe` в папку (например `C:\ngrok\`)
4. Добавь в PATH или положи в папку с Нейрой

### Шаг 2: Настройка токена

1. Зайди на https://dashboard.ngrok.com/get-started/your-authtoken
2. Скопируй свой authtoken
3. В терминале выполни:

```powershell
ngrok config add-authtoken YOUR_TOKEN_HERE
```

### Шаг 3: Запуск

```batch
.\start_remote_secure.bat
```

Скрипт покажет публичный URL (например `https://abc123.ngrok.io`)

### Шаг 4: Подключение с телефона

1. Открой браузер на телефоне
2. Введи ngrok URL из консоли
3. Введи логин `0` и пароль `11111111`

---

## 🔧 Ручной запуск (без скриптов)

Если скрипты не работают, запусти вручную:

### Терминал 1: Ollama
```powershell
ollama serve
```

### Терминал 2: Backend
```powershell
cd backend
$env:NEIRA_AUTH_ENABLED = "true"
python -m uvicorn api:app --host 0.0.0.0 --port 8000
```

### Терминал 3: Туннель (выбери один)

**ngrok:**
```powershell
ngrok http 8000
```

**Tailscale (просто узнай IP):**
```powershell
tailscale ip -4
```

---

## 📱 Мобильный интерфейс

После подключения ты увидишь адаптированный интерфейс:

- **Кнопка 📊** в header — показать/скрыть статистику
- **Компактный input** — оптимизирован для клавиатуры телефона
- **Сообщения** — стиль мессенджера с округлёнными углами
- **Safe area** — поддержка iPhone notch/Dynamic Island

### Добавление на Home Screen

**Android Chrome:**
1. Меню (⋮) → "Добавить на главный экран"
2. Назови "Neira"
3. Теперь можно запускать как приложение

**iPhone Safari:**
1. Кнопка "Поделиться" (□↑)
2. "На экран Домой"
3. Назови "Neira"

---

## 🛡️ Безопасность

### Что защищено:

| Угроза | Защита |
|--------|--------|
| Перехват пароля | SHA-256 хеш в коде |
| Подмена через env | Захардкоженный профиль имеет приоритет |
| Timing attack | `secrets.compare_digest()` |
| MITM (ngrok) | HTTPS автоматически |
| Чужие устройства (Tailscale) | Только твоя VPN-сеть |

### Рекомендации:

1. **Используй Tailscale** — доступ только с твоих устройств
2. **ngrok** — удобно, но URL публичный (хоть и случайный)
3. **Не делись URL** — даже с паролем, лучше перестраховаться
4. **Выключай когда не используешь** — Ctrl+C в терминале

---

## ❓ Решение проблем

### "Не могу подключиться"

1. Проверь что Ollama запущена: `curl http://localhost:11434/api/tags`
2. Проверь что backend запущен: `curl http://localhost:8000/api/health`
3. Проверь firewall Windows — разреши Python/uvicorn

### "401 Unauthorized"

- Логин точно `0` (цифра ноль)
- Пароль точно `11111111` (8 единиц)
- Проверь что `NEIRA_AUTH_ENABLED=true`

### "Tailscale не видит устройство"

1. На обоих устройствах один аккаунт Tailscale
2. Оба устройства "Connected" в статусе
3. `tailscale ping <ip>` для проверки связи

### "ngrok показывает ошибку"

1. Проверь authtoken: `ngrok config check`
2. Бесплатный лимит — 1 туннель одновременно
3. Перезапусти ngrok

---

## 📊 Сравнение способов

| Критерий | Tailscale | ngrok |
|----------|-----------|-------|
| **Безопасность** | ⭐⭐⭐⭐⭐ | ⭐⭐⭐ |
| **Простота** | ⭐⭐⭐ | ⭐⭐⭐⭐ |
| **Скорость** | ⭐⭐⭐⭐⭐ | ⭐⭐⭐ |
| **Стабильный URL** | ✅ (IP) | ❌ (меняется) |
| **Бесплатно** | ✅ | ✅ (с лимитами) |
| **Работает без интернета** | ❌ | ❌ |

**Мой совет:** Используй **Tailscale** для постоянного доступа, **ngrok** для быстрого теста.

---

## 🎯 Быстрый старт (TL;DR)

```powershell
# 1. Запусти на компе
.\start_tailscale.bat

# 2. Установи Tailscale на телефон, войди в тот же аккаунт

# 3. Открой в браузере телефона
http://<tailscale-ip>:8000

# 4. Логин: 0 | Пароль: 11111111
```

**Готово!** Теперь можешь общаться с Нейрой откуда угодно 🚀
