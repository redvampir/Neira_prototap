# 🌐 Web UI для Нейры - Руководство по запуску

## ✅ Что исправлено (14 декабря 2025)

### Проблемы с кнопками:
1. ✅ **Кнопка "Отправить"** - добавлена обработка ошибок и визуальный feedback
2. ✅ **Кнопка "Обновить"** - теперь показывает статус (⏳ → ✅ → 🔄)
3. ✅ **Состояние processing** - кнопка подсвечивается оранжевым при обработке
4. ✅ **Автоматический reconnect** - при разрыве соединения
5. ✅ **Улучшенная диагностика** - больше информации об ошибках

---

## 🚀 Быстрый запуск

### Windows:
```bash
# Просто запусти:
start_web_ui.bat
```

### Linux/Mac:
```bash
# Сделай исполняемым (один раз):
chmod +x start_web_ui.sh

# Запусти:
./start_web_ui.sh
```

Скрипт автоматически:
- ✅ Проверит и запустит Ollama
- ✅ Установит зависимости
- ✅ Запустит backend API
- ✅ Откроет frontend в браузере

---

## 🖥️ Ручной запуск (если скрипт не работает)

### 1. Запусти Ollama
```bash
# Новое окно терминала:
ollama serve
```

### 2. Запусти Backend API
```bash
# Новое окно терминала:
cd backend
python api.py

# Или через uvicorn напрямую:
uvicorn api:app --reload --host 0.0.0.0 --port 8000
```

### 3. Открой Frontend
```
Открой в браузере:
file:///f:/Нейронки/prototype/frontend/index.html

# Или через localhost (если есть веб-сервер):
http://localhost:8000/frontend/index.html
```

---

## 🎯 Проверка работоспособности

### 1. Backend API работает?
Открой в браузере: http://localhost:8000/

Должен вернуться JSON:
```json
{
  "status": "ok",
  "service": "Neira API",
  "version": "0.5.0",
  "timestamp": "..."
}
```

### 2. WebSocket подключается?
В консоли браузера (F12) должно быть:
```
WebSocket connected
🧠 Neira Frontend loaded
```

### 3. Кнопки работают?
- **"Отправить"** - при клике должна стать оранжевой с текстом "Обработка..."
- **"🔄 Обновить"** - при клике показывает "⏳ Обновление..." → "✅ Обновлено"
- **Индикатор подключения** - должен быть зелёным

---

## 🐛 Решение проблем

### Кнопка "Отправить" не работает

**Симптомы:**
- Клик по кнопке ничего не делает
- Кнопка серая и disabled

**Решения:**
1. **Проверь индикатор подключения** (справа вверху)
   - Должен быть зелёным
   - Если красный → backend не запущен или WebSocket не подключается

2. **Проверь консоль браузера (F12)**
   ```
   Ищи ошибки:
   - "Failed to create WebSocket"
   - "WebSocket disconnected"
   - "Network error"
   ```

3. **Перезапусти backend**
   ```bash
   # Закрой окно backend и запусти заново
   cd backend
   python api.py
   ```

4. **Обнови страницу** (F5 или Ctrl+R)

---

### Кнопка "Обновить" не работает

**Симптомы:**
- Клик не обновляет статистику
- Показывает "❌ Ошибка"

**Решения:**
1. **Проверь backend API**
   ```
   Открой: http://localhost:8000/api/stats
   Должен вернуться JSON со статистикой
   ```

2. **Проверь консоль**
   ```
   Ищи: "Failed to fetch stats: ..."
   ```

3. **Убедись что Neira запущена**
   ```bash
   # Backend должен подключиться к Neira
   # Проверь логи в окне backend
   ```

---

### WebSocket не подключается

**Симптомы:**
- Красный индикатор подключения
- "Disconnected" в статусе
- В консоли: "WebSocket error"

**Решения:**
1. **Проверь backend запущен**
   ```bash
   # Должен быть запущен на порту 8000
   curl http://localhost:8000/
   ```

2. **Проверь CORS**
   ```
   Backend должен разрешать подключения от file://
   (уже настроено в api.py: allow_origins=["*"])
   ```

3. **Попробуй через localhost**
   ```bash
   # Вместо открытия file:// используй простой HTTP сервер
   cd frontend
   python -m http.server 8080
   
   # Открой: http://localhost:8080/
   ```

---

### "Failed to fetch stats"

**Причина:** Backend API не отвечает на `/api/stats`

**Решения:**
1. **Проверь backend логи**
   ```
   В окне backend ищи ошибки при GET /api/stats
   ```

2. **Проверь NeiraWrapper**
   ```python
   # В backend/neira_wrapper.py должен быть метод:
   def get_stats(self):
       return self.neira.get_stats()
   ```

3. **Попробуй вручную**
   ```bash
   curl http://localhost:8000/api/stats
   ```

---

## 📱 Мобильная версия

Web UI полностью адаптивен для мобильных устройств:
- ✅ Адаптивная раскладка
- ✅ Тач-управление
- ✅ Виртуальная клавиатура
- ✅ Скрываемая панель статистики (кнопка 📊)

### Использование на мобильном:
1. Убедись что backend запущен на компьютере
2. Найди IP адрес компьютера:
   ```bash
   ipconfig  # Windows
   ifconfig  # Linux/Mac
   ```
3. Открой на мобильном:
   ```
   http://192.168.x.x:8000/frontend/index.html
   ```

---

## 🔧 Конфигурация

### Изменить порт backend:
```python
# backend/api.py, в конце файла:
uvicorn.run(
    "api:app",
    host="0.0.0.0",
    port=8888,  # <-- ИЗМЕНИТЬ ЗДЕСЬ
    reload=True
)
```

### Изменить URL backend:
```javascript
// frontend/app.js, в начале:
const CONFIG = {
    WS_URL: 'ws://localhost:8888/ws/chat',     // <-- ИЗМЕНИТЬ
    API_URL: 'http://localhost:8888/api',      // <-- ИЗМЕНИТЬ
    STATS_REFRESH_INTERVAL: 5000,
};
```

---

## 🎨 Кастомизация UI

### Изменить тему:
```css
/* frontend/style.css, в начале: */
:root {
    --bg-primary: #1a1a1a;      /* Основной фон */
    --bg-secondary: #2a2a2a;    /* Вторичный фон */
    --accent-blue: #4a9eff;     /* Акцентный цвет */
    --accent-green: #4ade80;    /* Успех */
    --accent-orange: #fb923c;   /* Обработка */
}
```

### Отключить авто-обновление статистики:
```javascript
// frontend/app.js, закомментируй:
// setInterval(fetchStats, CONFIG.STATS_REFRESH_INTERVAL);
```

---

## 📊 API Endpoints

### REST API:
```
GET  /                    - Health check
GET  /api/health         - Detailed health
GET  /api/status         - Neira status
GET  /api/stats          - System statistics
GET  /api/memory         - Recent memory
GET  /api/experience     - Personality data
POST /api/chat           - Non-streaming chat
POST /api/memory/clear   - Clear memory
```

### WebSocket:
```
WS /ws/chat              - Streaming chat

# Формат сообщений:
Client → Server: {"message": "привет"}
Server → Client: {
    "type": "stage|content|error|done",
    "stage": "analysis|planning|execution|verification",
    "content": "...",
    "metadata": {...}
}
```

### Интерактивная документация:
```
http://localhost:8000/docs
```

---

## 🔒 Безопасность

### Включить авторизацию:
```bash
# В .env добавь:
NEIRA_AUTH_ENABLED=true
NEIRA_USERNAME=admin
NEIRA_PASSWORD=your_secure_password
```

### Захардкоженный админ:
```
Логин: 0
Пароль: 11111111
```

---

## 💡 Полезные команды

### Проверить что backend запущен:
```bash
curl http://localhost:8000/
```

### Проверить WebSocket:
```bash
# Используй websocat (установи: cargo install websocat)
websocat ws://localhost:8000/ws/chat
```

### Логи backend:
```bash
# В окне backend видны все запросы
# Уровень логирования: INFO
```

### Отладка в браузере:
```javascript
// Откройте консоль (F12) и используй:
window.NeiraDebug.sendTestMessage("привет")
window.NeiraDebug.getStats()
window.NeiraDebug.reconnect()
```

---

## 📝 Changelog

### v0.8.1 (14 декабря 2025)
- ✅ Исправлены кнопки: добавлена обработка ошибок
- ✅ Визуальный feedback для всех действий
- ✅ Автоматический reconnect при разрыве соединения
- ✅ Улучшенная диагностика ошибок
- ✅ CSS анимации для состояний кнопок
- ✅ Обновлён start_web_ui.bat с проверками

---

## 🆘 Поддержка

Если проблемы остались:
1. Проверь логи backend (окно "Neira Backend")
2. Проверь консоль браузера (F12)
3. Убедись что Ollama запущена: `curl http://localhost:11434/api/tags`
4. Перезапусти всё по порядку: Ollama → Backend → Frontend

**Полезные логи:**
- Backend: `backend/api.py` выводит все запросы
- Frontend: Консоль браузера (F12) показывает WebSocket события
- Ollama: `ollama serve` показывает обращения к моделям
