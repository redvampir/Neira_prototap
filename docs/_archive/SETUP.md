# 🚀 Установка и запуск Neira v0.6.1

## 📋 Системные требования

- **Python**: 3.10 или выше
- **RAM**: минимум 8 GB (рекомендуется 16 GB для локальных моделей)
- **Диск**: ~10 GB свободного места для моделей
- **ОС**: Linux, macOS, или Windows (с WSL)

## 🔧 Шаг 1: Установка зависимостей Python

```bash
# Создайте виртуальное окружение (рекомендуется)
python3 -m venv venv
source venv/bin/activate  # На Windows: venv\Scripts\activate

# Установите необходимые пакеты
pip install requests
```

**Минимальные зависимости**:
- `requests` — для HTTP запросов к Ollama

## 🤖 Шаг 2: Установка Ollama

Ollama — это локальная платформа для запуска LLM моделей.

### Linux/macOS:
```bash
curl -fsSL https://ollama.com/install.sh | sh
```

### Windows:
Скачайте установщик с https://ollama.com/download

### Проверка установки:
```bash
ollama --version
```

## 📦 Шаг 3: Скачивание моделей

Neira использует несколько моделей для разных задач:

```bash
# Основные локальные модели
ollama pull qwen2.5-coder:7b          # Для работы с кодом (~5 GB)
ollama pull ministral-3:3b       # Для рассуждений (~4.5 GB)

# Проверьте установленные модели
ollama list
```

**Ожидаемый вывод**:
```
NAME                     ID           SIZE    MODIFIED
qwen2.5-coder:7b        abc123...    4.7 GB  2 minutes ago
ministral-3:3b     def456...    4.1 GB  5 minutes ago
```

## ▶️ Шаг 4: Запуск проекта

### 4.1. Запуск Ollama (в отдельном терминале)
```bash
# Запустите Ollama сервер
ollama serve
```

Ollama должен быть запущен на `http://localhost:11434`

### 4.2. Запуск Neira
```bash
# Перейдите в директорию проекта
cd /home/user/Neira_prototap

# Активируйте виртуальное окружение (если используете)
source venv/bin/activate

# Запустите Neira
python3 main.py
```

## 🎯 Шаг 5: Первое тестирование

После запуска вы увидите приглашение:

```
╔══════════════════════════════════════════════════════════════╗
║           НЕЙРА v0.6 — AI ассистент с эволюцией             ║
╚══════════════════════════════════════════════════════════════╝

📊 Загружено опыта: X записей
🧠 Загружено памяти: Y записей
🧬 Система эволюции активирована

Введи запрос или /help для справки
>
```

### Базовые команды для проверки:

```bash
# Справка по командам
> /help

# Проверка моделей
> /models

# Статистика системы
> /stats

# Проверка памяти
> /memory

# Справка по эволюции
> /evolution help
```

### Тестирование основного функционала:

```bash
# 1. Простой запрос
> Привет, расскажи о себе

# 2. Запрос на рассуждение
> Объясни разницу между list и tuple в Python

# 3. Запрос на код
> Напиши функцию для сортировки списка

# 4. Работа с памятью
> Запомни что мой любимый язык - Python
> /memory

# 5. Проверка опыта
> /experience
```

## 🧬 Тестирование новых функций (v0.6.1)

### 1. Code Diff Visualization

Эта функция позволяет увидеть изменения кода, которые внесла система эволюции:

```bash
# Посмотреть список изменений кода
> /evolution list cls

# Если есть записи, посмотреть детали изменения
> /evolution diff cls 0
```

**Примечание**: Для появления записей нужно сначала накопить провалы и запустить автоэволюцию.

### 2. Interactive Voting для промптов

Эта функция позволяет вручную сравнивать разные версии промптов:

```bash
# Сначала нужно создать версии промптов (автоматически при использовании)
# Затем можно начать голосование:

> /vote-start analyzer v1_20251209 v2_20251209 Проанализируй этот код

# Оцените обе версии
> /vote-record analyzer v1_20251209 8 Хороший анализ
> /vote-record analyzer v2_20251209 9 Отличный детальный анализ

# Посмотрите результаты
> /vote-results analyzer v1_20251209 v2_20251209
```

### 3. Автоматическая эволюция

```bash
# Запустить полный цикл эволюции
> /evolution cycle

# Посмотреть статистику
> /evolution stats

# Посмотреть логи
> /evolution log
```

## 🐛 Решение проблем

### Ollama не запускается
```bash
# Проверьте процесс
ps aux | grep ollama

# Перезапустите
killall ollama
ollama serve
```

### Модели не найдены
```bash
# Убедитесь что модели загружены
ollama list

# Если нет, загрузите снова
ollama pull qwen2.5-coder:7b
ollama pull ministral-3:3b
```

### Ошибки импорта Python
```bash
# Убедитесь что все зависимости установлены
pip install requests

# Проверьте версию Python
python3 --version  # Должна быть 3.10+
```

### Ошибка подключения к Ollama
```bash
# Проверьте что Ollama запущен
curl http://localhost:11434/api/tags

# Должен вернуть JSON с моделями
```

## 📁 Структура данных

После первого запуска создаются файлы:

```
Neira_prototap/
├── neira_memory.json           # Краткосрочная память
├── neira_experience.json       # Накопленный опыт
├── neira_evolution.json        # История эволюции кода
├── neira_prompt_versions.json  # Версии промптов
├── neira_prompt_tests.json     # Результаты тестов
├── neira_cell_registry.json    # Реестр клеток
├── backups/                    # Бэкапы кода
│   └── code_evolution/
└── generated/                  # Сгенерированные клетки
```

## 🧪 Полный тест функционала

```bash
# 1. Запустите Neira
python3 main.py

# 2. Проверьте систему
> /models
> /stats
> /evolution stats

# 3. Сделайте несколько запросов
> Что такое Python?
> Напиши Hello World на Python
> Объясни что такое декоратор

# 4. Проверьте опыт
> /experience

# 5. Если накопилось достаточно опыта, запустите эволюцию
> /evolution cycle

# 6. Посмотрите результаты
> /evolution log
> /evolution list cls
```

## 🎓 Дополнительные ресурсы

- **Документация по эволюции**: `EVOLUTION.md`
- **Основная документация**: `README.md`
- **Ollama документация**: https://ollama.com/docs

## ✅ Контрольный список

- [ ] Python 3.10+ установлен
- [ ] Виртуальное окружение создано (опционально)
- [ ] `requests` установлен
- [ ] Ollama установлен и запущен
- [ ] Модели `qwen2.5-coder:7b` и `ministral-3:3b` загружены
- [ ] `main.py` запускается без ошибок
- [ ] Базовые команды работают
- [ ] Система отвечает на запросы

## 🆘 Получить помощь

Если возникли проблемы:

1. Проверьте логи в терминале
2. Убедитесь что Ollama запущен (`ollama serve`)
3. Проверьте доступные модели (`ollama list`)
4. Убедитесь что Python 3.10+ (`python3 --version`)

Удачного тестирования! 🚀
