# 🔒 Обновление безопасности v0.7.1

## ⚠️ Критические изменения безопасности

### Исправлены уязвимости:

1. **Команда /code теперь защищена**
   - ✅ Добавлен декоратор `@require_auth`
   - ✅ Добавлена проверка `is_admin()`
   - ❌ Раньше: любой пользователь мог читать исходный код
   - ✅ Теперь: только администратор

2. **Пароль администратора вынесен в переменную окружения**
   - ❌ Раньше: `_ADMIN_HASH = hashlib.sha256(b"0:11111111").hexdigest()`
   - ✅ Теперь: `_ADMIN_PASSWORD = os.getenv("NEIRA_ADMIN_PASSWORD", "change_me_please")`

---

## 🚀 Инструкция по обновлению

### Шаг 1: Обновите файл .env

Добавьте в ваш файл `.env`:

```bash
# Пароль администратора (придумайте свой сложный пароль!)
NEIRA_ADMIN_PASSWORD=your_strong_password_here
```

**Примеры сильных паролей:**
- `MyN3iraB0t!Secur3#2024`
- `T3l3gramAdmin!98765`
- `Pr0t3ct_N3ira$2024`

⚠️ **НЕ используйте:**
- `0:11111111` (старый пароль)
- `password`, `123456`, `admin`
- Простые слова из словаря

### Шаг 2: Обновите код

Код уже исправлен! Изменения в `telegram_bot.py`:

**Строка 62-65** (было):
```python
_ADMIN_HASH = hashlib.sha256(b"0:11111111").hexdigest()
```

**Теперь:**
```python
_ADMIN_PASSWORD = os.getenv("NEIRA_ADMIN_PASSWORD", "change_me_please")
_ADMIN_HASH = hashlib.sha256(_ADMIN_PASSWORD.encode()).hexdigest()
```

**Строка 1063** (было):
```python
async def code_command(update: Update, context: ContextTypes.DEFAULT_TYPE) -> None:
    """Команды работы с кодом."""
    if not context.args:
```

**Теперь:**
```python
@require_auth
async def code_command(update: Update, context: ContextTypes.DEFAULT_TYPE) -> None:
    """Команды работы с кодом (только для администратора)."""
    user_id = update.effective_user.id
    
    if not is_admin(user_id):
        await update.message.reply_text("⛔ Команда /code доступна только администратору.")
        return
    
    if not context.args:
```

### Шаг 3: Перезапустите бота

```bash
# Linux/Mac
./stop.sh
./start.sh

# Windows
Ctrl+C (остановить)
start_web_ui.bat (запустить)
```

### Шаг 4: Повторная авторизация

1. Отправьте `/auth 0 ваш_новый_пароль`
2. Проверьте доступ: `/code list`

---

## 🔍 Что было уязвимо?

### До исправления:

**Любой пользователь мог:**
```
/code read telegram_bot.py
→ видел старую строку с хардкодом
→ узнавал что старый пароль был в коде
→ получал доступ администратора
```

**Последствия:**
- Чтение всех файлов: `.env`, `telegram_bot.py`, `enhanced_auth.py`
- Доступ к токенам Telegram
- Возможность изменения настроек
- Полный контроль над ботом

### После исправления:

```
/code list
→ ⛔ Команда /code доступна только администратору.
```

```
/code read telegram_bot.py
→ ⛔ Команда /code доступна только администратору.
```

Пароль администратора:
- ✅ Хранится в `.env` (не в git)
- ✅ Не виден в коде
- ✅ Уникален для каждой установки

---

## 📋 Защищённые команды

После обновления требуют авторизации администратора:

| Команда | Описание | Защита |
|---------|----------|--------|
| `/code list` | Список файлов | ✅ `@require_auth` + `is_admin()` |
| `/code read <file>` | Чтение файла | ✅ `@require_auth` + `is_admin()` |
| `/admin <password>` | Авторизация | ✅ Проверка по хешу |
| `/learn_auto start` | Автообучение | ✅ `@require_auth` |
| `/reboot` | Перезагрузка | ✅ `is_admin()` |
| `/logs` | Просмотр логов | ✅ `is_admin()` |

---

## ⚡ Быстрая проверка безопасности

Выполните эти шаги чтобы убедиться что всё защищено:

```bash
# 1. Проверьте что .env содержит пароль
grep NEIRA_ADMIN_PASSWORD .env

# 2. Проверьте что .env НЕ в git
cat .gitignore | grep .env

# 3. Проверьте что код использует переменную окружения
grep "_ADMIN_PASSWORD = os.getenv" telegram_bot.py

# 4. Проверьте защиту команды /code
grep -A3 "def code_command" telegram_bot.py | grep "@require_auth"
```

**Все 4 проверки должны быть успешны! ✅**

---

## 🆘 Что делать если скомпрометирован старый пароль?

Если кто-то узнал старый пароль `0:11111111`:

1. **Немедленно смените пароль в .env:**
   ```bash
   NEIRA_ADMIN_PASSWORD=new_secure_password_here
   ```

2. **Перезапустите бота:**
   ```bash
   ./stop.sh && ./start.sh
   ```

3. **Повторно авторизуйтесь:**
   ```
   /auth 0 новый_пароль
   ```

4. **Смените токен Telegram (если нужно):**
   - Зайдите в [@BotFather](https://t.me/BotFather)
   - `/mybots` → ваш бот → `API Token` → `Revoke current token`
   - Обновите `TELEGRAM_BOT_TOKEN` в `.env`

---

## 📝 Changelog

**v0.7.1** (2024-12-XX)
- 🔒 SECURITY: Команда `/code` требует авторизации администратора
- 🔒 SECURITY: Пароль администратора вынесен в переменную окружения
- 🔒 SECURITY: Добавлена проверка `is_admin()` для критичных команд
- 📝 Добавлен `SECURITY_UPDATE.md`
- 📝 Обновлён `.env.example`

---

## 💡 Рекомендации

### Для разработчиков:

1. **Никогда не храните секреты в коде**
   ```python
   # ❌ ПЛОХО
   PASSWORD = "my_password"
   
   # ✅ ХОРОШО
   PASSWORD = os.getenv("MY_PASSWORD")
   ```

2. **Используйте .gitignore**
   ```
   .env
   .env.local
   *.key
   *.pem
   ```

3. **Проверяйте права доступа**
   ```python
   @require_auth
   async def sensitive_command(...):
       if not is_admin(user_id):
           return
   ```

### Для администраторов:

1. Используйте менеджер паролей (KeePass, 1Password, Bitwarden)
2. Включите двухфакторную аутентификацию в Telegram
3. Регулярно меняйте пароли (раз в 3-6 месяцев)
4. Не публикуйте `.env` в публичных репозиториях

---

## ✅ Итоги

- **Уязвимость:** Команда `/code` была доступна всем → пароль в открытом виде
- **Исправление:** Добавлена авторизация + пароль в `.env`
- **Статус:** 🟢 Защищено
- **Действия:** Обновите `.env` и перезапустите бота

---

**Вопросы?** Проверьте [README.md](README.md) или [SETUP.md](SETUP.md)
