# 🤖 Telegram-бот Neira (v0.8.3)

Этот бот подключает Neira к Telegram: диалог, обучение, управление, vision/Stable Diffusion.

## 🚀 Быстрый старт (чек‑лист)

- [ ] Python 3.10+
- [ ] Установить зависимости:
  - `pip install -r backend/requirements.txt`
  - `pip install -r requirements-telegram.txt`
- [ ] Создать `.env` (по примеру `.env.example`) и заполнить:
  - `TELEGRAM_BOT_TOKEN=...`
  - `NEIRA_ADMIN_PASSWORD=...` (не `change_me_please`)
- [ ] Запустить:
  - `python telegram_bot.py`
- [ ] (Опционально) Самопроверки:
  - `python tools/check_telegram_tools.py`
  - `python tools/check_telegram_connectivity.py`

## 🔐 Безопасность

- `/auth` работает только в личном чате и ограничен по попыткам (защита от brute force).
- Бот пытается удалить сообщение с паролем, но Telegram может не позволить — удаляй вручную.
- Запуск с `NEIRA_ADMIN_PASSWORD=change_me_please` по умолчанию запрещён.
  - Для локальной отладки можно временно установить `NEIRA_ALLOW_DEFAULT_ADMIN_PASSWORD=true` (не использовать в проде).

## 🧭 Режимы доступа

- `open` — отвечает всем
- `whitelist` — только авторизованным пользователям (через `/admin add ...`)
- `admin_only` — только администратору

## ⚙️ Переменные окружения (основные)

- `TELEGRAM_BOT_TOKEN` — токен BotFather (обязательно)
- `NEIRA_ADMIN_PASSWORD` — пароль администратора (обязательно)
- `NEIRA_ALLOW_DEFAULT_ADMIN_PASSWORD` — `true` только для локальной разработки

- `NEIRA_TG_ACCESS` — `open|whitelist|admin_only` (если задана — перекрывает файл настроек)
- `NEIRA_TG_CHANNELS` — ID групп/каналов через запятую, где бот отвечает без whitelist (если задана — перекрывает файл настроек)
- `NEIRA_TG_MENTION_ONLY` — `true|false`, отвечать в группах только на упоминание/ответ
- `NEIRA_TG_SETTINGS_FILE` — путь к JSON с настройками (по умолчанию `neira_tg_settings.json`)

- `NEIRA_TG_AUTH_MAX_ATTEMPTS` — попыток `/auth` в окне (по умолчанию `5`)
- `NEIRA_TG_AUTH_WINDOW_SECONDS` — окно в секундах (по умолчанию `300`)
- `NEIRA_TG_AUTH_BLOCK_SECONDS` — блокировка в секундах (по умолчанию `900`)

- `NEIRA_TG_TYPING_THROTTLE_SECONDS` — троттлинг индикатора typing (по умолчанию `3.0`)
- `NEIRA_TG_RESPONSE_MAX_CHARS` — лимит длины ответа в Telegram (0 = без ограничений)
- `NEIRA_TG_DISABLE_TEMPLATES` — `true|false`, отключить шаблонные/фрагментные ответы Cortex (fallback на legacy)
- `NEIRA_HTTPX_LOG_LEVEL` — шумность логов httpx (`WARNING` по умолчанию)

### 🌐 Сеть и таймауты

- `NEIRA_TG_PROXY_URL` — прокси для Telegram API (например `http://127.0.0.1:8080` или `socks5://127.0.0.1:9050`)
- `NEIRA_TG_BASE_URL` — кастомный Bot API base URL (например локальный `http://127.0.0.1:8081`)

- `NEIRA_TG_CONNECT_TIMEOUT` — таймаут соединения, сек (по умолчанию `15`)
- `NEIRA_TG_READ_TIMEOUT` — таймаут чтения, сек (по умолчанию `30`)
- `NEIRA_TG_WRITE_TIMEOUT` — таймаут записи, сек (по умолчанию `30`)
- `NEIRA_TG_POOL_TIMEOUT` — таймаут пула соединений, сек (по умолчанию `30`)

- `NEIRA_TG_POLLING_TIMEOUT` — long polling timeout для `getUpdates`, сек (по умолчанию `10`)
- `NEIRA_TG_POLLING_BOOTSTRAP_RETRIES` — ретраи `getUpdates` при старте polling (`-1` бесконечно, по умолчанию `-1`)

- `NEIRA_TG_STARTUP_RETRIES` — ретраи при старте (этап `bot.get_me`/`initialize`) (`-1` бесконечно, по умолчанию `-1`)
- `NEIRA_TG_STARTUP_BACKOFF_BASE_SECONDS` — базовая задержка ретрая, сек (по умолчанию `2.0`)
- `NEIRA_TG_STARTUP_BACKOFF_MAX_SECONDS` — максимум задержки ретрая, сек (по умолчанию `60.0`)

## 📁 Файлы данных

- `neira_authorized_users.json` — whitelist пользователей (создаётся/обновляется через `/admin add/remove`)
- `neira_tg_settings.json` — настройки доступа/каналов (создаётся/обновляется через `/admin mode/addchannel/removechannel/thisgroup`)
- `neira_user_profiles.json` — сохранённые имена пользователей (`/myname`)

Примечание: если `NEIRA_TG_ACCESS` или `NEIRA_TG_CHANNELS` заданы в окружении, они имеют приоритет над `neira_tg_settings.json`.

## 🧾 Команды (ключевые)

### Администратор

1) Авторизация (только в личке):
- `/auth 0 <пароль_из_NEIRA_ADMIN_PASSWORD>`

2) Панель:
- `/admin`

3) Управление пользователями:
- `/admin users`
- `/admin add <user_id|@username|t.me/username> [заметка]`
- `/admin remove <user_id|@username|t.me/username>`

4) Управление группами/каналами:
- `/admin channels`
- `/admin addchannel <chat_id>`
- `/admin removechannel <chat_id>`
- `/admin thisgroup` (добавить текущую группу/канал)
- `/admin mode <open|whitelist|admin_only>`

5) Доступ к коду (только админ):
- `/code list`
- `/code read <файл>` — присылает файл документом (без Markdown-инъекций и ограничений по длине сообщения)

### Пользователи

- `/start`, `/help`
- `/stats`, `/memory`, `/learn <тема|URL> ...`, `/learn_auto ...`
- `/imagine ...`, `/vision` (при наличии настроенных моделей)

## 🧩 Типовые проблемы (edge cases)

- `/auth` в группе: команда отклоняется, бот пытается удалить сообщение.
- Не отвечает в группе: проверь `NEIRA_TG_MENTION_ONLY` и что группа добавлена через `/admin thisgroup` или `NEIRA_TG_CHANNELS`.
- Настройки «не сохраняются»: если `NEIRA_TG_ACCESS/NEIRA_TG_CHANNELS` заданы в окружении, они перекрывают `neira_tg_settings.json`.
- Таймаут при запуске (например `telegram.error.TimedOut`, `httpx.ConnectTimeout`): запусти `python tools/check_telegram_connectivity.py`; при блокировках укажи `NEIRA_TG_PROXY_URL` (для SOCKS может понадобиться `pip install httpx[socks]`).
