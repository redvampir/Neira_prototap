# 🎮 Гид по запуску многопользовательской игры Harry Potter

## Что создано

- **multiplayer_server.py** — WebSocket сервер для синхронизации игроков
- **harry_potter_multiplayer.html** — клиент игры с логином и мультиплеером
- **Обучающие примеры** — добавлены в `training_dataset.jsonl` для Нейры

## Быстрый старт

### 1. Запустить сервер

```bash
python multiplayer_server.py
```

Сервер запустится на `ws://localhost:8003/game`

### 2. Открыть игру в браузере

Откройте файл `harry_potter_multiplayer.html` в браузере (можно в нескольких вкладках/окнах)

### 3. Войти в игру

1. Введите имя игрока (например: "Harry", "Hermione", "Ron")
2. Введите ID комнаты (например: "hogwarts")
3. URL сервера уже заполнен: `ws://localhost:8003/game`
4. Нажмите "Войти в игру"

### 4. Играть!

- **Движение**: WASD или стрелки
- **Цель**: Собрать 8 волшебных артефактов
- **Чат**: Общайтесь с другими игроками

## Игра по локальной сети

### На сервере (компьютер, который будет хостить)

1. Узнайте свой локальный IP:
   ```bash
   ipconfig  # Windows
   # Ищите "IPv4 Address" в разделе вашей сети
   ```

2. Запустите сервер:
   ```bash
   python multiplayer_server.py
   ```

3. Сообщите другим игрокам ваш IP (например: `192.168.1.100`)

### На клиентах (другие компьютеры в сети)

1. Откройте `harry_potter_multiplayer.html`
2. В поле "URL сервера" замените `localhost` на IP сервера:
   ```
   ws://192.168.1.100:8003/game
   ```
3. Войдите в ту же комнату

## Архитектура

```
┌─────────────────┐         WebSocket          ┌─────────────────┐
│   Браузер 1     │◄──────────────────────────►│                 │
│ (HTML Client)   │                             │  Python Server  │
└─────────────────┘                             │   (port 8003)   │
                                                 │                 │
┌─────────────────┐         WebSocket          │   GameRoom      │
│   Браузер 2     │◄──────────────────────────►│   - players     │
│ (HTML Client)   │                             │   - artifacts   │
└─────────────────┘                             │   - broadcast() │
                                                 └─────────────────┘
┌─────────────────┐         WebSocket
│   Браузер 3     │◄──────────────────────────►
│ (HTML Client)   │
└─────────────────┘
```

## Механика игры

- **Grid**: 15x15 клеток
- **Артефакты**: 8 волшебных предметов (🪄 💎 📜 🔮 ⚡ 🧪 🦉 🗝️)
- **Игроки**: Каждый игрок получает уникальный цвет
- **Синхронизация**: Real-time через WebSocket
- **Чат**: Сообщения видны всем в комнате

## Как Нейра научилась это создавать

В `training_dataset.jsonl` добавлены 5 примеров:

1. **Базовый multiplayer** — ui_code_cell + WebSocket server
2. **Архитектура** — Starlette server + broadcast система
3. **Чат** — real-time messaging через WebSocket
4. **Рейтинг** — GameRoom с players dict и score tracking
5. **Синхронизация** — server authoritative model

### Переобучение модели

Чтобы Нейра полностью усвоила multiplayer паттерны:

```bash
# 1. Создать новый Modelfile (если нужно обновить prompt)
# 2. Запустить fine-tuning с обновленным dataset
ollama create neira-cell-router:multiplayer -f Modelfile
ollama run neira-cell-router:multiplayer "Создай многопользовательскую игру"
```

## Отладка

### Сервер не запускается

- Проверьте, свободен ли порт 8003:
  ```bash
  netstat -an | findstr 8003
  ```

### Клиент не подключается

- Проверьте console в браузере (F12)
- Убедитесь, что URL сервера правильный
- Проверьте firewall (должен разрешать порт 8003)

### Игроки не видят друг друга

- Убедитесь, что все в одной комнате (одинаковый Room ID)
- Проверьте, что сервер отправляет broadcast (логи в терминале)

## Следующие шаги

- ✅ Multiplayer игра работает
- ✅ Нейра обучена паттернам
- 🔲 Протестировать с 3+ игроками
- 🔲 Добавить звуковые эффекты
- 🔲 Добавить анимации сбора артефактов
- 🔲 Создать комнаты с паролями
