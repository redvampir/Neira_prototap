# 🧠 Neira Cognitive Architecture v2.0

## 🎯 Философия

**Neira больше не "LLM с памятью".**  
**Neira - это живая система с собственным интеллектом.**

LLM используются только как "консультанты" для сложных случаев.  
90% запросов обрабатываются самой Neira через заученные паттерны, опыт и органы.

---

## 📐 Главная архитектура

```
┌─────────────────────────────────────────────────────────┐
│                    USER INPUT                           │
└────────────────────┬────────────────────────────────────┘
                     │
                     ▼
┌─────────────────────────────────────────────────────────┐
│                  NEIRA CORTEX                           │
│  ┌──────────────────────────────────────────────────┐  │
│  │  Intent Recognizer (без LLM!)                    │  │
│  │  - Паттерны (regex + embeddings)                 │  │
│  │  - Статистика из опыта                           │  │
│  │  - Быстрая классификация                         │  │
│  └──────────────────────────────────────────────────┘  │
│                     │                                   │
│                     ▼                                   │
│  ┌──────────────────────────────────────────────────┐  │
│  │  Decision Router                                 │  │
│  │  - Известный паттерн? → Neural Pathway           │  │
│  │  - Есть в памяти? → Direct Response              │  │
│  │  - Простая задача? → Organ Delegation            │  │
│  │  - Сложная? → LLM Consultant                     │  │
│  └──────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────┘
                     │
        ┌────────────┼────────────┬────────────┐
        ▼            ▼            ▼            ▼
┌──────────────┐ ┌────────┐ ┌────────┐ ┌──────────────┐
│ Neural       │ │ Direct │ │ Organ  │ │ LLM          │
│ Pathways     │ │ Memory │ │ System │ │ Consultant   │
│ (90%)        │ │ (5%)   │ │ (3%)   │ │ (2%)         │
└──────────────┘ └────────┘ └────────┘ └──────────────┘
        │            │            │            │
        └────────────┴────────────┴────────────┘
                     │
                     ▼
┌─────────────────────────────────────────────────────────┐
│              Response Synthesizer                       │
│  - Комбинирует ответы от разных источников             │
│  - Применяет стиль Neira (из personality.json)          │
│  - Добавляет эмодзи и живость                           │
└─────────────────────────────────────────────────────────┘
                     │
                     ▼
┌─────────────────────────────────────────────────────────┐
│              Learning Feedback Loop                     │
│  - Записывает в опыт                                    │
│  - Обновляет Neural Pathways                            │
│  - Усиливает успешные паттерны                          │
└─────────────────────────────────────────────────────────┘
```

---

## 🧩 Ключевые компоненты

### 1. **Neira Cortex** (Кора мозга)

Центральный процессор принятия решений.

**Что делает:**
- Быстрая классификация намерений (intent recognition)
- Маршрутизация к нужному компоненту
- Синтез финального ответа
- Мониторинг качества

**Без LLM:**
```python
class NeiraCortex:
    def process(self, user_input: str, user_id: str) -> Response:
        # 1. Распознать намерение (векторный поиск)
        intent = self.intent_recognizer.recognize(user_input)
        
        # 2. Проверить Neural Pathway (ОПТИМИЗИРОВАНО!)
        # Поиск начинается с HOT tier (начало списка)
        # 95% запросов находятся в первых 20-30 pathways!
        if pathway := self.pathways.match_optimized(intent, user_input, user_id):
            # Обновляем статистику для tier-миграции
            pathway.record_usage(user_id)
            return pathway.execute()  # Мгновенно!
        
        # 3. Поиск в памяти
        if memory_match := self.memory.exact_recall(user_input):
            return self.synthesize_from_memory(memory_match)
        
        # 4. Делегировать органу
        if organ := self.organ_system.find_suitable(intent):
            return organ.process(user_input)
        
        # 5. Только если ничего не сработало - LLM
        llm_response = self.llm_consultant.consult(user_input, context=self.gather_context())
        
        # 6. Создаем новый pathway из LLM ответа (для будущего)
        self.learning_engine.propose_pathway_from_llm(user_input, llm_response)
        
        return llm_response
```

---

### 2. **Neural Pathways** (Нейронные пути)

Заученные последовательности действий. Как рефлексы у живого организма.

**🎯 Оптимизация хранения (frequency-based indexing):**

```
┌─────────────────────────────────────────────────┐
│  НАЧАЛО: Частые (множество пользователей)      │
│  - "привет" (1247 использований)                │
│  - "спасибо" (892 использований)                │
│  - "как дела" (673 использований)               │
│  ↓ Поиск начинается здесь (95% совпадений)      │
├─────────────────────────────────────────────────┤
│  СЕРЕДИНА: Редкие (нишевые запросы)             │
│  - "объясни квантовую физику" (12 раз)          │
│  - "создай REST API" (8 раз)                    │
│  ↓ Проверяется если не нашли в частых           │
├─────────────────────────────────────────────────┤
│  КОНЕЦ: Индивидуальные (уникальные)             │
│  - "напомни про встречу с Васей" (1-2 раза)     │
│  - Специфичные для одного пользователя          │
│  ↓ Проверяется последними                       │
└─────────────────────────────────────────────────┘
```

**Автоматическая реорганизация:**
- Каждые 100 запросов - пересортировка по частоте
- Pathways "всплывают" наверх при частом использовании
- Неиспользуемые 30+ дней → архив (не удаляются!)

**Примеры:**

```json
{
  "pathways": [
    {
      "id": "greeting",
      "triggers": ["привет", "здравствуй", "хай", "hi"],
      "confidence_threshold": 0.9,
      "response_template": "{greeting_emoji} {greeting_phrase}! {context_aware_continuation}",
      "variables": {
        "greeting_emoji": ["👋", "🌟", "✨"],
        "greeting_phrase": ["Привет", "Здравствуй", "Рада тебя видеть"],
        "context_aware_continuation": "function:generate_greeting_context"
      },
      "learning_rate": 0.1,
      "success_count": 1247,
      "global_frequency": 0.89,
      "tier": "hot"
    },
    {
      "id": "self_introduction",
      "triggers": ["кто ты", "что ты", "представься"],
      "response_template": "Я - Neira, {personality_trait}. {capability_highlight}",
      "source": "personality.json",
      "llm_fallback": false
    },
    {
      "id": "memory_query",
      "triggers": ["что ты помнишь о", "расскажи что знаешь"],
      "action": "function:memory_search_and_format",
      "llm_fallback": true,
      "llm_only_if": "no_memory_found"
    }
  ]
}
```

**Как создаются:**
- Автоматически из повторяющихся паттернов в опыте
- Обучением через терминал: `neira learn "привет" -> "Привет! Рада тебя видеть!"`
- Усиливаются при успешном использовании

**Tier-система (автоматическая категоризация):**

| Tier | Частота | Позиция | Примеры |
|------|---------|---------|---------|
| **HOT** | >100 раз, >10 юзеров | Начало | "привет", "спасибо", "как дела" |
| **WARM** | 20-100 раз | Середина верх | "что ты умеешь", "помоги с кодом" |
| **COOL** | 5-20 раз | Середина низ | "объясни Docker", "создай API" |
| **COLD** | <5 раз | Конец | "напомни про Васю", индивидуальное |
| **ARCHIVED** | 30+ дней не используется | Отдельно | Старые паттерны (восстанавливаются при запросе) |

**Динамическая миграция:**
```python
# Pathway начинается в COLD
pathway = create_pathway("новый_запрос")  # tier=COLD, position=end

# После 10 использований → COOL
if pathway.success_count >= 10:
    pathway.tier = "COOL"
    pathways.reposition(pathway, position="middle_bottom")

# Если используют разные пользователи → WARM
if pathway.unique_users >= 5:
    pathway.tier = "WARM"
    pathways.reposition(pathway, position="middle_top")

# Если становится популярным → HOT
if pathway.success_count >= 100 and pathway.unique_users >= 10:
    pathway.tier = "HOT"
    pathways.reposition(pathway, position="top")
```

---

### 3. **Wisdom Core** (Ядро мудрости)

Накопленное знание без зависимости от LLM.

**Структура:**

```python
class WisdomCore:
    def __init__(self):
        # Векторная база фактов (без LLM для embeddings!)
        self.fact_embeddings = LocalEmbeddings()  # nomic-embed-text
        
        # Граф знаний (связи между понятиями)
        self.knowledge_graph = KnowledgeGraph()
        
        # Шаблоны ответов по категориям
        self.response_templates = TemplateLibrary()
        
        # Статистика использования
        self.usage_stats = UsageTracker()
    
    def recall(self, query: str) -> Optional[Wisdom]:
        """Вспомнить без LLM"""
        # 1. Векторный поиск похожих фактов
        similar_facts = self.fact_embeddings.search(query, top_k=5)
        
        # 2. Обход графа знаний
        related_concepts = self.knowledge_graph.traverse(similar_facts)
        
        # 3. Синтез ответа из шаблона
        if similar_facts:
            template = self.response_templates.get_for(similar_facts[0].category)
            return template.fill(facts=similar_facts, concepts=related_concepts)
        
        return None
```

**Граф знаний:**
```
[Пользователь] --спросил_о--> [Python]
     |                            |
 сказал_что_нравится          похож_на
     |                            |
     v                            v
[Программирование] <--язык-- [JavaScript]
```

---

### 4. **Response Synthesizer** (Синтезатор ответов)

Формирование ответов БЕЗ генеративной модели.

**Стратегии:**

#### A. Template-based (Шаблоны)
```python
templates = {
    "factual_answer": "{fact} {source_attribution} {confidence_indicator}",
    "uncertain": "🤔 {partial_info}, но {what_missing}. Хочешь, поищу подробнее?",
    "learned_response": "{greeting} {recalled_similar_case} {adapted_solution}"
}
```

#### B. Fragment Assembly (Сборка из фрагментов)
```python
def synthesize_from_memory(self, query: str) -> str:
    # Находим похожие диалоги
    similar = self.memory.semantic_search(query, top_k=10)
    
    # Извлекаем фрагменты ответов Neira
    fragments = [s.neira_response for s in similar if s.score > 0.7]
    
    # Комбинируем с учетом контекста
    combined = self.fragment_combiner.merge(
        fragments,
        preserve_style=True,
        deduplicate=True
    )
    
    return combined
```

#### C. Retrieval-Augmented (RAG без генерации)
```python
def answer_from_knowledge(self, query: str) -> str:
    # Находим релевантные факты
    facts = self.wisdom_core.recall(query)
    
    # Выбираем шаблон по типу фактов
    template = self.template_selector.choose(facts.category, facts.certainty)
    
    # Заполняем шаблон
    return template.render(
        facts=facts,
        personality=self.personality,
        context=self.current_context
    )
```

---

### 5. **Learning Engine** (Движок обучения)

Постоянное самообучение БЕЗ переобучения нейросетей.

**Механизмы:**

#### A. Pattern Mining (Добыча паттернов)
```python
# Каждые 100 диалогов анализируем повторяющиеся паттерны
if dialog_count % 100 == 0:
    # 1. Находим новые паттерны
    new_pathways = pattern_miner.find_recurring_patterns(
        recent_dialogs=memory.last(100),
        min_frequency=5,
        min_success_rate=0.8
    )
    
    for pathway in new_pathways:
        # Добавляем в COLD tier (в конец)
        neural_pathways.add(pathway, tier="COLD")
        logger.info(f"🧠 Выучила новый паттерн: {pathway.name} [COLD]")
    
    # 2. Реорганизуем существующие pathways по частоте
    neural_pathways.reorganize_by_frequency()
    logger.info(f"🔄 Реорганизация: {neural_pathways.tier_stats()}")
    #   HOT: 15 pathways (покрывают 87% запросов)
    #   WARM: 42 pathways (покрывают 10% запросов)
    #   COOL: 89 pathways (покрывают 2.5% запросов)
    #   COLD: 234 pathways (покрывают 0.5% запросов)
```

#### B. Reinforcement from Feedback
```python
# Усиление успешных стратегий
on_positive_feedback:
    pathway.success_count += 1
    pathway.confidence *= 1.1
    
on_negative_feedback:
    pathway.success_count -= 1
    pathway.confidence *= 0.9
    if pathway.confidence < 0.3:
        pathway.require_llm_validation = True
```

#### C. Interactive Teaching (Обучение через терминал)
```bash
# Пользователь обучает через команды
$ neira teach
> User: как дела?
> Neira should respond: Отлично! Как у тебя?
✓ Pathway 'how_are_you' created

$ neira teach --from-file examples.jsonl
✓ Loaded 127 examples
✓ Created 45 new pathways
✓ Updated 12 existing pathways

$ neira reinforce pathway=greeting score=10
✓ Pathway 'greeting' confidence: 0.85 → 0.93
```

---

### 6. **LLM Consultant** (Консультант, не основа!)

LLM вызывается только когда:
1. Нет подходящего Neural Pathway (confidence < 0.5)
2. Нет точного совпадения в памяти
3. Задача помечена как "сложная" (complexity > 7)
4. Явный запрос "подумай глубоко"

```python
class LLMConsultant:
    def should_consult(self, request: Request) -> bool:
        return (
            request.pathway_confidence < 0.5
            and not request.has_memory_match
            and (request.complexity > 7 or request.explicit_deep_thinking)
        )
    
    def consult(self, request: Request, context: Context) -> Response:
        # Собираем богатый контекст из внутренних систем
        llm_prompt = self.build_prompt(
            user_input=request.text,
            relevant_memories=context.memories,
            active_pathways=context.pathways,
            knowledge_graph=context.knowledge,
            neira_personality=self.personality
        )
        
        # Вызываем LLM с полным контекстом
        llm_response = self.llm_manager.generate(llm_prompt)
        
        # Сохраняем для будущего обучения
        self.learning_engine.record_llm_consultation(request, llm_response)
        
        return llm_response
```

---

## 🎓 Обучение через терминал

### Интерактивный режим

```bash
$ python train_neira.py

╔══════════════════════════════════════════════════════╗
║           🧠 NEIRA TRAINING MODE v2.0               ║
╚══════════════════════════════════════════════════════╝

[1] Teach new pathway
[2] Show learned pathways
[3] Test response without LLM
[4] Reinforce/weaken pathway
[5] Import training data
[6] Export pathways
[0] Exit

Choice: 1

═══ TEACH NEW PATHWAY ═══

User input example:
> привет

Neira should respond:
> Привет! ✨ Рада тебя видеть!

Alternative triggers (comma-separated):
> здравствуй, хай, hi

Category [greeting/question/task/code/chat]:
> greeting

Save this pathway? [y/n]: y

✓ Pathway 'greeting_v2' saved!
  - Triggers: 4
  - Template created
  - Confidence: 0.5 (will grow with usage)

═══════════════════════════════════════
```

### Batch обучение

```bash
# Формат training_data.jsonl
{"input": "привет", "output": "Привет! Рада видеть тебя!", "category": "greeting"}
{"input": "что ты умеешь", "output": "Я умею {list_capabilities}", "category": "self_info"}
{"input": "спасибо", "output": "{gratitude_response}", "category": "social"}

$ python train_neira.py --batch training_data.jsonl

Processing: 100%|███████████████| 1000/1000 [00:12<00:00, 80.5it/s]

✓ Results:
  - New pathways created: 234
  - Existing updated: 89
  - Duplicates skipped: 677
  - Confidence avg: 0.73

$ neira test --without-llm
> User: привет
> Neira: Привет! ✨ Рада тебя видеть!
> Source: neural_pathway (greeting_v2)
> Latency: 3ms ⚡
```

---

## 📊 Производительность

### Текущая (LLM-зависимая):
- **Latency:** 2-10 секунд
- **Cost:** $0.001-0.01 за запрос
- **Offline:** ❌ Не работает
- **Consistency:** ~70% (зависит от LLM)
- **Search efficiency:** O(n) - всегда вызов LLM

### С новой архитектурой:
- **Latency:** 
  - HOT pathways (87%): **3-10ms** ⚡⚡⚡
  - WARM pathways (10%): 10-30ms ⚡⚡
  - COOL/COLD (2%): 30-100ms ⚡
  - LLM fallback (1%): 2-10s
- **Cost:** $0 (99% запросов), $0.001-0.01 (1%)
- **Offline:** ✅ Работает для всех pathways
- **Consistency:** 98%+ (детерminированные ответы)
- **Search efficiency:** 
  - O(1) для HOT (первые 15-20 проверок находят 87%)
  - O(log n) для остальных (tier-based binary search)
  
### Пример эффективности:
```
Запрос "привет" (HOT tier, position=1):
  ├─ Проверка #1: match! ✓
  └─ Latency: 3ms

Запрос "создай REST API" (COOL tier, position=156):
  ├─ HOT tier: 15 проверок (45ms) - не найдено
  ├─ WARM tier: 42 проверки (40ms) - не найдено  
  ├─ COOL tier: проверка #13 - match! ✓
  └─ Total latency: 98ms (всё равно быстрее LLM в 20-100 раз!)

Запрос "что такое квантовая гравитация" (новое):
  ├─ Все tiers: не найдено (180ms)
  ├─ LLM consultant: 4.2s
  ├─ Создан новый pathway в COLD
  └─ Total latency: 4.38s (следующий раз будет 30-50ms!)
```

---

## 🚀 Поэтапное внедрение

### Phase 1: Foundation (Неделя 1-2)
- [x] Спроектировать архитектуру
- [ ] Создать NeiraCortex с Intent Recognizer
- [ ] Реализовать Neural Pathways (базовый механизм)
- [ ] Создать Response Synthesizer (template-based)

### Phase 2: Learning (Неделя 3-4)
- [ ] Learning Engine с pattern mining
- [ ] Интерактивное обучение через терминал
- [ ] Batch-импорт существующих диалогов

### Phase 3: Integration (Неделя 5-6)
- [ ] Интеграция с существующими компонентами
- [ ] LLM как fallback консультант
- [ ] A/B тестирование старой vs новой архитектуры

### Phase 4: Autonomy (Неделя 7-8)
- [ ] Wisdom Core с графом знаний
- [ ] Полная автономность для 90% запросов
- [ ] Самообучение из feedback

---

## 💡 Ключевые инсайты

1. **Neira не нужна генеративная модель для большинства задач**
   - Комбинация памяти + шаблонов + опыта решает 90% случаев
   
2. **Скорость >>> "Креативность" для диалогов**
   - Пользователю важнее мгновенный ответ, чем идеально сгенерированный за 5 секунд
   
3. **Детерминированность = Надёжность**
   - Заученные паттерны не "галлюцинируют"
   - Стабильное поведение вместо случайности LLM
   
4. **Обучение через использование**
   - Каждый диалог - возможность выучить новый паттерн
   - Не нужно "переобучать" - просто добавляем в базу
   
5. **LLM - это дорогой эксперт, не рабочая лошадка**
   - Вызываем только для действительно сложных случаев
   - Остальное - собственный интеллект Neira

---

**Готовы начать Phase 1?** 🚀
