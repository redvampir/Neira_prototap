# 🌐 Web UI Нейры

> Веб-интерфейс для взаимодействия с Нейрой через браузер

## 📋 Содержание

- [Быстрый старт](#-быстрый-старт)
- [Проверка работы](#-проверка-работы)
- [Решение проблем](#-решение-проблем)
- [Ручной запуск](#-ручной-запуск)

---

## 🚀 Быстрый старт

### Windows
```bash
start_web_ui.bat
```

### Linux/Mac
```bash
chmod +x start_web_ui.sh  # Один раз
./start_web_ui.sh
```

Скрипт автоматически:
- ✅ Проверит и запустит LLM-провайдера
- ✅ Установит зависимости
- ✅ Запустит backend API
- ✅ Откроет frontend в браузере

---

## ✅ Проверка работы

### 1. Backend API

Открой в браузере: http://localhost:8000/

Ожидаемый ответ:
```json
{
  "status": "ok",
  "service": "Neira API",
  "version": "0.5.0"
}
```

### 2. WebSocket

В консоли браузера (F12):
```
✅ WebSocket connected
🧠 Neira Frontend loaded
```

### 3. Индикатор (правый верхний угол)

| Цвет | Статус |
|------|--------|
| 🟢 | Connected - всё работает |
| 🔴 | Disconnected - backend не запущен |

### 4. Кнопки реагируют

- **"Отправить"** → 🟠 Обработка... → Синяя
- **"🔄 Обновить"** → ⏳ Обновление... → ✅ Обновлено

---

## 🐛 Решение проблем

### Кнопка "Отправить" серая

**Причина:** WebSocket не подключен

```bash
# 1. Проверь backend
curl http://localhost:8000/

# 2. Если не отвечает:
cd backend
python api.py

# 3. Обнови страницу (F5)
```

### Кнопка "Обновить" показывает ❌

**Причина:** API /stats не отвечает

```bash
# 1. Проверь endpoint
curl http://localhost:8000/api/stats

# 2. Посмотри логи backend
# Должно быть: INFO: "GET /api/stats HTTP/1.1" 200 OK

# 3. Перезапусти backend при ошибках
```

### CORS блокирует запросы

```bash
# Запусти с явным разрешением CORS:
uvicorn api:app --reload --host 0.0.0.0 --port 8000
```

---

## 🖥️ Ручной запуск

### 1. Запусти LLM-провайдера

**LM Studio (рекомендуется):**
- Открой LM Studio
- Загрузи модель (например, `qwen2.5-coder-14b`, это опциональный пример)
- Запусти сервер на порту 1234

**Или Ollama:**
```bash
ollama pull nemotron-mini
ollama serve
```
Модель можно сменить через `NEIRA_LMSTUDIO_MODEL` или `NEIRA_OLLAMA_MODEL`.
Если переменные не заданы, используются дефолты из кода.

### 2. Запусти Backend API

```bash
cd backend
python api.py
```

Или через uvicorn:
```bash
uvicorn api:app --reload --host 0.0.0.0 --port 8000
```

### 3. Открой Frontend

```
file:///f:/Нейронки/prototype/frontend/index.html
```

Или через localhost:
```
http://localhost:8000/frontend/index.html
```

---

## 🔗 Связанные документы

- [Мобильный доступ](../mobile/README.md)
- [Руководство пользователя](../../guides/USER_MANAGEMENT.md)
- [Главная документация](../../README.md)
