# 📱 Мобильный доступ к Нейре

> Настройка доступа к Нейре с телефона или другого устройства

## 📋 Содержание

- [Локальная сеть WiFi](#-локальная-сеть-wifi)
- [Через Tailscale VPN](#-способ-1-tailscale-vpn)
- [Через ngrok](#-способ-2-ngrok)
- [Управление на телефоне](#-управление-на-телефоне)
- [Решение проблем](#-решение-проблем)

---

## 🏠 Локальная сеть WiFi

### Шаг 1: Запустить сервер

```bash
python start_multiplayer.py
```

Сервер покажет ваш IP:
```
📍 Локальный IP: 192.168.1.100
📍 Порт: 8003

📱 На телефоне используй:
   http://192.168.1.100:8003/
```

### Шаг 2: На телефоне

1. Подключиться к **той же WiFi сети**
2. Открыть браузер
3. Ввести адрес из консоли: `http://192.168.1.100:8003/`

### Узнать свой IP вручную

```powershell
ipconfig
```

Ищите `IPv4 Address` в разделе WiFi (обычно `192.168.X.X` или `10.0.X.X`)

---

## 🔐 Учётные данные

```
Логин:  0
Пароль: 11111111
```

> Захардкожены в коде для безопасности

---

## 🚀 Способ 1: Tailscale VPN

**Самый безопасный вариант** — приватная VPN-сеть между устройствами.

### Установка

1. **Компьютер:** https://tailscale.com/download → Установить → Войти
2. **Android:** Google Play → "Tailscale" → Войти в тот же аккаунт
3. **iPhone:** App Store → "Tailscale" → Войти в тот же аккаунт

### Запуск

```batch
.\start_tailscale.bat
```

Скрипт покажет Tailscale IP (например `100.64.0.1`)

### Подключение

1. Включить Tailscale на телефоне
2. Открыть: `http://100.64.0.1:8000`
3. Ввести логин `0` и пароль `11111111`

---

## 🌐 Способ 2: ngrok

**Публичный URL** для localhost — проще настроить, но менее безопасно.

### Установка

1. Регистрация: https://ngrok.com (бесплатно)
2. Скачать: https://ngrok.com/download
3. Распаковать `ngrok.exe`

### Настройка токена

```powershell
ngrok config add-authtoken YOUR_TOKEN_HERE
```

### Запуск

```batch
.\start_remote_secure.bat
```

Скрипт покажет URL (например `https://abc123.ngrok.io`)

---

## 🎮 Управление на телефоне

| Метод | Описание |
|-------|----------|
| Свайпы | В разные стороны для движения |
| Виртуальные кнопки | На экране |
| Bluetooth геймпад | Подключить внешний |

---

## 🐛 Решение проблем

### "Не могу подключиться"

- [ ] Телефон в той же WiFi сети?
- [ ] Правильный IP (из консоли сервера)?
- [ ] Firewall открыт?

```powershell
# Открыть порт в firewall (от администратора):
New-NetFirewallRule -DisplayName "Neira Game" -Direction Inbound -LocalPort 8003 -Protocol TCP -Action Allow
```

### Проверка что сервер слушает

```powershell
netstat -an | findstr 8003
```

Должно быть: `TCP 0.0.0.0:8003 LISTENING`

### Порт занят

```powershell
# Освободить порт 8003:
Get-NetTCPConnection -LocalPort 8003 -ErrorAction SilentlyContinue | 
    Select-Object -ExpandProperty OwningProcess | 
    ForEach-Object { Stop-Process -Id $_ -Force }
```

---

## 🔒 Безопасность

- 🔑 Используйте уникальные Room ID как пароли
- ⏹️ Запускайте сервер только когда играете
- 🚫 Не открывайте порт в роутере наружу

---

## 🔗 Связанные документы

- [Web UI](../web-ui/README.md)
- [Руководство пользователя](../../guides/USER_MANAGEMENT.md)
- [Главная документация](../../README.md)
