# 🔧 Решение проблем

> Типичные проблемы и их решения

## 📋 Содержание

- [Русские пути в Windows](#-русские-пути-в-windows)
- [Зацикливание памяти](#-зацикливание-памяти)
- [LLM провайдеры](#-llm-провайдеры)
- [Telegram бот](#-telegram-бот)
- [Web UI](#-web-ui)

---

## 🔤 Русские пути в Windows

**Проблема:** Многие инструменты не работают с кириллицей в путях

### Решение 1: Junction (рекомендуется)

```batch
# Запустить от администратора
fix_russian_path.bat
```

После этого:
```powershell
cd /d C:\neira_work  # Без кириллицы
```

**Преимущества:**
- ✅ Не копирует файлы
- ✅ Автосинхронизация
- ✅ 0 байт дополнительно

### Решение 2: Subst (виртуальный диск)

```batch
subst N: F:\Нейронки\prototype
cd /d N:\
```

Удалить:
```batch
subst N: /D
```

---

## 🔄 Зацикливание памяти

**Проблема:** Neira записывает одни и те же факты многократно

### Защита (v2.3+)

Автоматически активна:

| Защита | Описание |
|--------|----------|
| Дубликаты | Проверка последних 100 записей |
| Зацикливание | >5 похожих за 5 минут → блок |
| Схожесть | Jaccard >80% = дубликат |

### Ручная очистка

```bash
python clean_memory_duplicates.py
```

Создаст бэкап и удалит дубликаты.

---

## 🤖 LLM провайдеры

### Не отвечает на запросы

**Фактический дефолт приоритета (если `LLM_PROVIDER_PRIORITY` не задан):**

1. Mistral.rs (OpenAI-совместимый локальный сервер)
2. LM Studio
3. Ollama
4. llama.cpp server
5. Groq
6. OpenAI
7. Claude

Источник истины — `llm_providers._DEFAULT_PROVIDER_PRIORITY` в коде. Если переменная
`LLM_PROVIDER_PRIORITY` отсутствует, приоритет берётся именно оттуда.

**Поддерживаемые провайдеры (реально реализованы в `llm_providers.py`):**
- Mistral.rs (OpenAI-совместимый локальный сервер)
- LM Studio
- Ollama
- llama.cpp server
- Groq
- OpenAI
- Claude

**Проверить приоритет:**
```env
LLM_PROVIDER_PRIORITY=mistralrs
```

**Проверить mistral.rs:**
```bash
curl http://127.0.0.1:8080/health
```

**Проверить LM Studio (опционально):**
```bash
curl http://localhost:1234/v1/models
```

**Проверить Ollama (опционально):**
```bash
ollama list
```

### Переключение провайдера

```env
# Отключить Ollama
NEIRA_DISABLE_OLLAMA=true

# Использовать только mistral.rs
LLM_PROVIDER_PRIORITY=mistralrs
```

---

## 📱 Telegram бот

### Бот не отвечает

1. **Проверить токен:**
   ```bash
   python check_bot_status.py
   ```

2. **Проверить LLM (mistral.rs):**
   ```bash
   curl http://127.0.0.1:8080/health
   ```

3. **Перезапустить:**
   ```bash
   python telegram_bot.py
   ```

### Таймаут сообщений

```env
# Увеличить таймаут (сек)
NEIRA_TELEGRAM_TIMEOUT=60
```

---

## 🌐 Web UI

### Кнопки не работают

1. **Проверить backend:**
   ```bash
   curl http://localhost:8000/
   ```

2. **Перезапустить:**
   ```bash
   cd backend
   python api.py
   ```

3. **Обновить страницу (F5)**

### WebSocket не подключается

В консоли браузера (F12):
```
❌ Failed to create WebSocket
```

**Решение:** Запустить backend API

### CORS ошибки

```bash
uvicorn api:app --reload --host 0.0.0.0 --port 8000
```

---

## 🔗 Связанные документы

- [Web UI](../integrations/web-ui/README.md)
- [Telegram](../integrations/telegram/README.md)
- [Главная документация](../README.md)
