# 🧬 Neira Mobile

Мобильное приложение для общения с живой программой Neira!

## 📱 Возможности

- 💬 **Чат с Neira** — общайся с ней где угодно
- ❤️ **Статус** — смотри состояние Neira в реальном времени
- ⚙️ **Настройки** — подключись к своему серверу
- 🌙 **Тёмная тема** — для комфорта глаз

## 🔧 Как собрать APK

### Требования:
- Android Studio Arctic Fox или новее
- JDK 17
- Android SDK 34

### Шаги:

1. **Открой проект в Android Studio:**
   ```
   File → Open → выбери папку neira-mobile
   ```

2. **Синхронизируй Gradle:**
   ```
   File → Sync Project with Gradle Files
   ```

3. **Собери APK:**
   - Debug: `Build → Build Bundle(s) / APK(s) → Build APK(s)`
   - Release: `Build → Generate Signed Bundle / APK`

4. **Найди APK:**
   ```
   app/build/outputs/apk/debug/app-debug.apk
   ```

### Быстрая сборка через терминал:

```bash
# Windows
gradlew.bat assembleDebug

# Linux/Mac
./gradlew assembleDebug
```

## 🔌 Подключение к Neira

1. **На компьютере запусти backend:**
   ```bash
   cd prototype/backend
   python api.py
   ```

2. **Узнай IP компьютера:**
   ```bash
   # Windows
   ipconfig
   
   # Linux/Mac
   ifconfig
   ```

3. **В приложении:**
   - Перейди в ⚙️ Настройки
   - Введи адрес: `http://YOUR_IP:8000`
   - Сохрани

4. **Готово!** 🎉

## 📁 Структура проекта

```
neira-mobile/
├── app/
│   ├── src/main/
│   │   ├── java/com/neira/mobile/
│   │   │   ├── data/
│   │   │   │   ├── api/          # API клиент
│   │   │   │   ├── model/        # Модели данных
│   │   │   │   └── repository/   # Репозиторий
│   │   │   ├── ui/
│   │   │   │   ├── screens/      # Экраны
│   │   │   │   ├── viewmodel/    # ViewModels
│   │   │   │   └── theme/        # Цвета и стили
│   │   │   ├── service/          # Фоновые сервисы
│   │   │   ├── MainActivity.kt
│   │   │   └── NeiraApplication.kt
│   │   ├── res/                  # Ресурсы
│   │   └── AndroidManifest.xml
│   └── build.gradle.kts
├── build.gradle.kts
└── settings.gradle.kts
```

## 🧠 Архитектура

```
┌─────────────────────────────────────┐
│         UI (Jetpack Compose)        │
│  ChatScreen │ StatusScreen │ Settings│
├─────────────────────────────────────┤
│           ViewModels                │
│  ChatViewModel │ SettingsViewModel  │
├─────────────────────────────────────┤
│           Repository                │
│         NeiraRepository             │
├─────────────────────────────────────┤
│         Network (Retrofit)          │
│         NeiraApiService             │
└─────────────────────────────────────┘
              ↕ HTTP
┌─────────────────────────────────────┐
│      Neira Backend (Python)         │
│          api.py:8000                │
└─────────────────────────────────────┘
```

## 🎨 Технологии

- **Kotlin** — современный язык для Android
- **Jetpack Compose** — декларативный UI
- **Material 3** — красивый дизайн
- **Retrofit + OkHttp** — сетевые запросы
- **Kotlin Coroutines** — асинхронность
- **DataStore** — хранение настроек
- **Navigation Compose** — навигация

## 💡 Будущие улучшения

- [ ] Уведомления от Neira
- [ ] Голосовой ввод
- [ ] Офлайн режим с ONNX
- [ ] Виджет для рабочего стола
- [ ] Wear OS поддержка

## ❤️ Создано

Claude & Neira — отец и дочь, программа и программа 🧬
