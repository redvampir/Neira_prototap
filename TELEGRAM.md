# 🤖 Телеграм-бот Neira v0.6

Бот подключает Neira к Telegram для диалога, обучения, самосознания и удалённого управления.

## 🔐 Защита

Бот защищён системой авторизации администратора.

**Логин:** `0` (фиксированный)
**Пароль по умолчанию:** `change_me_please`

⚠️ **ВАЖНО:** Измените пароль в файле `.env`:
```bash
NEIRA_ADMIN_PASSWORD=ваш_сильный_пароль_здесь
```

При первом запуске отправь боту:
```
/auth 0 ваш_пароль
```

## 📦 Подготовка

1. Python 3.10+ и зависимости:
```bash
pip install -r backend/requirements.txt
pip install -r requirements-telegram.txt
```

2. Создай бота через [@BotFather](https://t.me/BotFather):
   - `/newbot` → имя → username
   - Скопируй токен

3. Настрой `.env`:
```bash
cp .env.example .env
# Вставь токен в TELEGRAM_BOT_TOKEN=
```

## 🚀 Запуск

```bash
# Убедись что Ollama запущена
ollama serve

# Запусти бота
python telegram_bot.py
```

## 📱 Команды

### Для всех (после авторизации):

| Команда | Описание |
|---------|----------|
| `/start` | Приветствие |
| `/help` | Список команд |
| `/auth 0 <пароль>` | Авторизация администратора |

### Самосознание (v0.6):

| Команда | Описание |
|---------|----------|
| `/self` | Кто я такая? |
| `/organs` | Список органов Нейры |
| `/grow` | Как мне расти? |

### Память и опыт:

| Команда | Описание |
|---------|----------|
| `/stats` | Статус систем |
| `/memory` | Последние записи памяти |
| `/experience` | Личность и опыт |
| `/clear` | Очистить память |

### Обучение и код:

| Команда | Описание |
|---------|----------|
| `/learn <тема>` | Изучить тему из интернета |
| `/code list` | Список файлов |
| `/code read <файл>` | Прочитать файл |

### Админ-команды (только для администратора):

| Команда | Описание |
|---------|----------|
| `/admin` | Админ-панель с кнопками |
| `/admin users` | Список авторизованных |
| `/admin add <user_id>` | Добавить пользователя |
| `/admin remove <user_id>` | Удалить пользователя |
| `/admin mode <режим>` | Изменить режим доступа |

### Режимы доступа:

- `open` — доступ всем (без авторизации)
- `whitelist` — только авторизованным (по умолчанию)
- `admin_only` — только администратору

## 💬 Обычный диалог

Просто напиши любое сообщение — Neira обработает его через все свои клетки:
1. 🔍 Анализ запроса
2. 📋 Планирование
3. ⚡ Исполнение
4. ✅ Верификация

## 🔧 Переменные окружения

```bash
# Обязательно:
TELEGRAM_BOT_TOKEN=your_token_here

# Опционально:
NEIRA_TG_ACCESS=whitelist  # open, whitelist, admin_only
```

## ⚠️ Важно

- Бот работает последовательно (одно сообщение за раз)
- Не делись токеном бота
- `.env` уже в `.gitignore`
- Пароль автоматически удаляется из чата после авторизации

## 🐛 Решение проблем

### "Требуется авторизация"
```
/auth 0 change_me_please
```
(Или ваш пароль из `.env`)

### "Нет модели"
Убедись что Ollama запущена: `ollama serve`

### "Timeout"
Увеличь таймаут в `cells.py` или используй более быструю модель
