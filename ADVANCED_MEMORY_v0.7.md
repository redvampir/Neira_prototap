# 🆕 Новые функции v0.7 — Подробно

## Все новые команды памяти

### 🔍 Поиск и фильтры

```bash
# Текстовый поиск
/memory search Python
→ Находит все записи со словом "Python"

# Семантический поиск (умный)
/memory semantic "как создать орган"
→ Находит записи по смыслу, даже без точных слов
→ Требует Ollama с nomic-embed-text

# Фильтр по уверенности
/memory filter confidence <0.5
→ Показывает ненадёжные записи (галлюцинации?)

/memory filter confidence >0.8
→ Только проверенные факты

# Фильтр по источнику
/memory filter source telegram
→ Только из Telegram
/memory filter source web
→ Только из веб-обучения

# Фильтр по времени
/memory filter recent 1h    # За последний час
/memory filter recent 24h   # За сутки
/memory filter recent 168h  # За неделю
```

---

### 📌 Закрепление важных записей

```bash
# Сначала найти запись
/memory
→ 1. [fact] Павел — создатель Neira
→ 2. [preference] Не использовать эмодзи
→ 3. [skill] Умею создавать органы

# Закрепить важную запись
/memory pin 1
→ 📌 Запись #1 закреплена
→ Защищена от удаления

# Посмотреть все закреплённые
/memory pinned
→ Список защищённых записей

# Открепить
/memory unpin 1
→ 📍 Запись #1 откреплена
```

**Закреплённые записи:**
- ✅ Не удаляются командами `/memory delete`
- ✅ Не удаляются при `/memory dedupe`
- ✅ Защищены от автоочистки по времени
- ❌ Можно удалить только через `/memory unpin` + delete

---

### 💾 Восстановление из бэкапов

```bash
# Создать бэкап
/memory backup
→ 💾 Бэкап создан: memory_backup_20241212_153045.json

# Посмотреть все бэкапы
/memory backups
→ 💾 Доступные бэкапы (5):
→ 1. memory_backup_20241212_153045.json
→    📅 12.12.2024 15:30 | 📦 247 записей | 💾 128 KB
→ 2. memory_backup_20241211_120000.json
→    📅 11.12.2024 12:00 | 📦 230 записей | 💾 115 KB

# Восстановить из бэкапа
/memory restore memory_backup_20241211_120000.json
→ ✅ Память восстановлена
→ Используй /memory stats для проверки

# Можно без .json
/memory restore memory_backup_20241211_120000
→ Работает!
```

**Что сохраняется в бэкапе:**
- Все типы памяти (долгосрочная, краткосрочная, эпизодическая, семантическая)
- Метаданные (timestamp, confidence, источники)
- Закрепления и теги
- Статистика (для справки)

---

### 📄 Экспорт в читаемый формат

```bash
# Экспорт всей памяти в текст
/memory export txt
→ 📄 Экспорт создан: memory_export_20241212_153045.txt
→ Сохранён в папку backups/

# Экспорт конкретной категории
/memory export txt fact
→ Только факты

/memory export txt preference
→ Только предпочтения
```

**Формат экспорта:**
```markdown
# Экспорт памяти Neira
# Дата: 2024-12-12 15:30

## [fact] 2024-12-10 14:25
Павел — создатель Neira
_Источник: telegram | Уверенность: 95%_

## 📌 [preference] 2024-12-11 09:15
Не использовать эмодзи в ответах
_Источник: conversation | Уверенность: 100% [теги: important]_
```

---

### 🔧 Примеры использования

#### Пример 1: Поиск и удаление галлюцинаций

```bash
# Найти ненадёжные записи
/memory filter confidence <0.5
→ Найдено 12 записей с низкой уверенностью

# Просмотреть
/memory search "квантовый"
→ 8 записей про квантовые процессоры (галлюцинация!)

# Удалить
/memory delete text "квантовый"
→ Удалено 8 записей
```

---

#### Пример 2: Защита важных данных

```bash
# Найти критически важное
/memory search "создатель"
→ 1. [fact] Павел — создатель Neira

# Защитить от случайного удаления
/memory pin 1
→ Запись защищена

# Теперь можно безопасно чистить память
/memory delete old 30
→ Удалено 47 старых записей
→ Но запись про создателя сохранена!
```

---

#### Пример 3: Регулярное обслуживание

```bash
# Еженедельная процедура
/memory backup
→ Сначала бэкап на всякий случай

/memory stats
→ Всего: 347 записей

/memory dedupe
→ Удалено дубликатов: 23

/memory delete old 60
→ Удалено старых: 89

/memory filter confidence <0.3
→ 5 ненадёжных записей найдено

/memory delete ... (по решению)

/memory stats
→ Всего: 230 записей (⬇️ 117)
→ Память чище и быстрее!
```

---

#### Пример 4: Поиск по контексту

```bash
# Что Neira помнит про Python?
/memory search Python
→ 15 записей найдено

# Более точно — семантический поиск
/memory semantic "программирование на Python"
→ Находит связанные записи:
→ - "Разработка на Python"
→ - "asyncio и асинхронность"
→ - "FastAPI framework"
→ Даже без слова "Python" в тексте!
```

---

#### Пример 5: Работа с источниками

```bash
# Что Neira узнала из Telegram?
/memory filter source telegram
→ 89 записей

# Что из веб-обучения?
/memory filter source web
→ 34 записи

# Что из разговоров?
/memory filter source conversation
→ 156 записей
```

---

#### Пример 6: Временной анализ

```bash
# Что запомнилось за последний час?
/memory filter recent 1h
→ 12 записей (активная сессия!)

# За сутки?
/memory filter recent 24h
→ 47 записей

# Удалить только свежие ошибки
/memory delete last 5
→ Откатываем последние 5 записей
```

---

## Технические детали

### Защита от удаления

**Приоритет защиты:**
1. Закреплённые записи (`pinned=True`) — **МАКСИМАЛЬНАЯ ЗАЩИТА**
2. Все остальные записи

**Что НЕ удаляет закреплённые:**
- `/memory delete text`
- `/memory delete old`
- `/memory delete category`
- `/memory delete last` (пропускает закреплённые)
- `/memory dedupe` (оставляет первую закреплённую копию)

**Как удалить закреплённую запись:**
```bash
/memory unpin <N>    # Сначала открепить
/memory delete ...   # Потом удалить
```

---

### Семантический поиск

**Требования:**
- Ollama должна быть запущена (`http://localhost:11434`)
- Модель `nomic-embed-text` должна быть скачана
- Записи должны иметь эмбеддинги (генерируются автоматически)

**Преимущества:**
- Находит записи по смыслу, не только по словам
- Ранжирует результаты по релевантности
- Работает на русском и английском

**Пример:**
```bash
/memory semantic "как обучаться"
→ Находит:
→ - "Система непрерывного обучения"
→ - "Изучение новых тем из интернета"
→ - "Создание органов для новых навыков"
```

---

### Форматы фильтров

**Уверенность (confidence):**
```bash
<0.5    # Меньше 50%
>0.8    # Больше 80%
<=0.3   # Не больше 30%
>=0.9   # Минимум 90%
==0.5   # Ровно 50%
```

**Время (recent):**
```bash
1h      # 1 час
24h     # Сутки
168h    # Неделя (7*24)
720h    # Месяц (30*24)
```

**Источники (source):**
- `telegram` — из Telegram
- `conversation` — из разговора
- `web` — из веб-обучения
- `user_input` — от пользователя
- `system` — системные записи

---

## FAQ

**Q: Можно ли восстановить удалённую запись?**  
A: Только из бэкапа! Делайте `/memory backup` перед экспериментами.

**Q: Сколько записей можно закрепить?**  
A: Неограниченно! Но рекомендуется <50 для производительности.

**Q: Как работает дедупликация с закреплёнными?**  
A: Если обе копии закреплены — оставляет обе. Если одна — оставляет закреплённую.

**Q: Можно ли экспортировать только закреплённые?**  
A: Пока нет, но планируется. Временное решение:
```bash
/memory pinned  # Скопировать список
```

**Q: Семантический поиск не работает?**  
A: Проверьте Ollama:
```bash
curl http://localhost:11434/api/tags
→ Должна быть модель nomic-embed-text
```

---

## Roadmap

Планируется в v0.8:
- [ ] `/memory tag <N> <тег>` — добавление тегов
- [ ] `/memory filter tag important` — фильтр по тегам
- [ ] `/memory export pinned` — экспорт только закреплённых
- [ ] `/memory autopurge` — автоматическая очистка по правилам
- [ ] `/memory history` — история изменений памяти
- [ ] `/memory compare <backup1> <backup2>` — сравнение бэкапов

---

**Версия:** 0.7  
**Дата:** 12 декабря 2024  
