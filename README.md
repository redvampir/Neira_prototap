# NEIRA v0.8.4 — Автономная Эволюционирующая Программа

## Модульный режим Neira Server (экономия памяти)

### Быстрый старт (пресеты)
- [ ] Установите `NEIRA_SERVICE_MODE` в одно из значений: `core`, `lite`, `tools`, `indexer`, `models`, `learning`, `self`, `all`
- [ ] При необходимости задайте `NEIRA_PORT` и запустите `python neira_server.py`

### Точная настройка модулей
- [ ] Задайте `NEIRA_MODULES` списком через запятую: `core,stream,ws,files,tools,indexer,context,models,layers,self,learning`
- [ ] При необходимости включите ленивую инициализацию ядра: `NEIRA_LAZY_INIT=1`
- [ ] Запустите `python neira_server.py`

### Разделение сервисов по портам
- [ ] Запустите core‑сервер: `NEIRA_SERVICE_MODE=core`, `NEIRA_PORT=8765`
- [ ] Запустите tools‑сервер: `NEIRA_SERVICE_MODE=tools`, `NEIRA_PORT=8766`
- [ ] Запустите indexer‑сервер: `NEIRA_SERVICE_MODE=indexer`, `NEIRA_PORT=8767`
- [ ] Подключайте клиента к нужному порту (эндпоинты, которые отключены, не регистрируются)

### Ленивая загрузка модулей Neira (main.py)
- [ ] Включите ленивую загрузку: `NEIRA_LAZY_MODULES=1` (по умолчанию)
- [ ] Отключайте модули точечно: `NEIRA_ENABLE_WEB=0`, `NEIRA_ENABLE_CODE=0`, `NEIRA_ENABLE_EVOLUTION=0`, `NEIRA_ENABLE_INTROSPECTION=0`, `NEIRA_ENABLE_CELL_WATCHER=0`

### Логи ошибок Neira Server
- [ ] Лог ошибок по умолчанию: `artifacts/neira_errors.txt`
- [ ] Свой путь: `NEIRA_ERROR_LOG_PATH=artifacts/my_error_log.txt`
- [ ] Отключить фильтр ConnectionResetError (шум от WinError 10054): `NEIRA_LOG_FILTER_CONNRESET=0`

### Логи чата Neira Server
- [ ] Лог чата по умолчанию: `artifacts/neira_chat.log`
- [ ] Свой путь: `NEIRA_CHAT_LOG_PATH=artifacts/neira_chat.txt`
- [ ] Отключить лог чата: `NEIRA_CHAT_LOG_ENABLED=0`
- [ ] Включить запись контекста редактора: `NEIRA_CHAT_LOG_INCLUDE_CONTEXT=1`
- [ ] Ограничить длину полей (символы): `NEIRA_CHAT_LOG_MAX_CHARS=4000`
- [ ] Формат лога: JSONL (одна строка на запрос)

### Контекст и ответы LLM (окно и длина ответа)
- [ ] Увеличить окно контекста: `NEIRA_CONTEXT_MAX_TOKENS=32000`
- [ ] Зарезервировать токены под ответ: `NEIRA_CONTEXT_RESERVED_TOKENS=6000`
- [ ] Глобальный лимит ответа: `NEIRA_MAX_RESPONSE_TOKENS=4096`
- [ ] Лимит ответа для кода: `NEIRA_CODE_MAX_TOKENS=4096`
- [ ] Лимит ответа для Cortex: `NEIRA_CORTEX_MAX_TOKENS=1024`
- [ ] Контекст Ollama: `NEIRA_OLLAMA_NUM_CTX=32768` (передаётся как `num_ctx`)

### Слои моделей (адаптеры, промпт-слои, профили)
- [ ] Добавить адаптер: `python manage_model_layers.py add --model neira-cell-router:latest --id cells-lora --adapter cells-lora.bin --activate`
- [ ] Добавить промпт-слой для роутера: `python manage_model_layers.py add --model neira-cell-router:latest --id cells-prompt-layer --kind prompt_layer --prompt-file artifacts/cells_prompt_layer.txt --activate`
- [ ] Добавить промпт-слой для кодера: `python manage_model_layers.py add --model qwen2.5-coder:7b --id code-prompt-layer --kind prompt_layer --prompt-file artifacts/code_prompt_layer.txt --activate`
- [ ] Если нужен адаптер + промпт одновременно, укажи `--adapter` и `--prompt/--prompt-file` в одном слое

### Таймаут LLM (CLI/клетки)
- [ ] Уменьшить время ожидания ответа LLM: `NEIRA_LLM_TIMEOUT=30` (секунды, диапазон 5-600)

### Dry-run режим CLI
- [ ] Включить dry-run (без вызова LLM): `NEIRA_DRY_RUN=1`
- [ ] Путь очереди запросов: `NEIRA_DRY_RUN_PATH=artifacts/neira_dry_run_queue.jsonl`
- [ ] Отправить очередь после отключения dry-run: `/queue send`

### Быстрый чат (ускорение ответов)
Работает только для типов `разговор/вопрос` и когда анализ не требует поиска, кода или новых клеток.
- [ ] Включите быстрый чат: `NEIRA_FAST_CHAT=1`
- [ ] При необходимости сузьте типы задач: `NEIRA_FAST_CHAT_TYPES=разговор,вопрос`
- [ ] Пропуск планирования: `NEIRA_FAST_CHAT_SKIP_PLANNING=1`
- [ ] Пропуск верификации: `NEIRA_FAST_CHAT_SKIP_VERIFY=1`
- [ ] Пропуск извлечения фактов: `NEIRA_FAST_CHAT_SKIP_FACTS=1`
- [ ] Число попыток: `NEIRA_FAST_CHAT_MAX_RETRIES=0`

## 🌿 Neira Web (Next.js, v2 MVP)

- [ ] `cd neira-app`
- [ ] `npm install`
- [ ] `npm run dev`
- [ ] открыть `http://localhost:3000`
- [ ] `npm run check`
- [ ] `npm run build`

Код: `neira-app/organism` → `neira-app/manifestation` → `neira-app/ritual`.

## 🧩 Предустановка интерфейса (JSON)

- [ ] открыть `frontend/index.html` или `frontend/index_8001.html`
- [ ] нажать `Импорт настроек`
- [ ] выбрать файл `presets/neira_preset.json`

### Предустановка LM Studio (JSON)
- [ ] Открыть LM Studio -> Import preset from file
- [ ] Выбрать файл `presets/neira_lmstudio_preset.json`
- [ ] Убедиться, что установлены параметры: `temperature=0.7`, `contextLength=8192` (если модель поддерживает)
- [ ] Альтернатива: скопировать файл в `%USERPROFILE%\.lmstudio\config-presets` и перезапустить LM Studio
- [ ] Проверить значения: `temperature`, `contextSize`, `maxTokens`, `streamMode`, `debugMode`, `autoMemory`

## 🌐 Что нового в v0.8.4

### 🧬 Исполняемые Органы v1.0 (NEW!)

Нейра теперь может **выращивать органы с реальным кодом**, которые выполняют задачи автономно:

**Встроенные органы:**
| Орган | Возможности |
|-------|-------------|
| **GraphicsOrgan** | ASCII-графика: квадраты, круги, линии, сердца, цветные эмодзи |
| **MathOrgan** | Безопасные вычисления: арифметика, степени |
| **TextOrgan** | Трансформации текста: реверс, регистр, шифрование |

**Особенности:**
- 🔄 **Версионирование** — каждое улучшение = новая версия с rollback
- 🧠 **Автообучение** — учится на feedback (👍/👎)
- 🧪 **Sandbox тестирование** — проверка перед активацией
- 🛡️ **Защита от injection** — критические паттерны блокируются

**Примеры команд:**
```
нарисуй красный квадрат 5x5  → 🟥🟥🟥🟥🟥
посчитай 15 * 4 + 10        → 📊 15 * 4 + 10 = **70**
переверни "Привет"          → 🔄 **тевирП**
```

**Тестирование:**
```bash
python executable_organs.py      # Тест всех органов
python unified_organ_system.py   # Тест системы органов + Sandbox
```

---

## 🧠 Системы самосознания v0.8.3

Нейра получила **5 новых систем для развития самосознания**:

### Модули:

| Модуль | Файл | Описание |
|--------|------|----------|
| **EmotionalMirror** | `emotional_mirror.py` | Осознание своего эмоционального состояния |
| **ErrorJournal** | `error_journal.py` | Дневник ошибок — учиться на промахах |
| **EmotionalMemory** | `emotional_memory.py` | Эмоциональная история с каждым пользователем |
| **ProactiveSystem** | `proactive_system.py` | Инициатива — начинать разговор первой |
| **CreativeEngine** | `creative_engine.py` | Творчество без запроса (хайку, мысли, сны) |

### Telegram команды:

```
/mirror     — эмоциональное зеркало (настроение, энергия, фокус)
/journal    — дневник ошибок (самоанализ, советы)
/creative   — творчество Нейры
/creative haiku   — создать хайку
/creative thought — поделиться мыслью
/creative dream   — описание "сна"
```

### Автоматическая интеграция:

- 📝 Записывается эмоциональная история каждого пользователя
- 🎭 Нейра осознаёт своё внутреннее состояние
- 💬 Персонализированные приветствия для семьи и друзей
- ✨ Развитие отношений: new → acquaintance → friend → close_friend → family

```bash
python emotional_mirror.py   # Тест эмоционального зеркала
python error_journal.py      # Тест дневника ошибок
python emotional_memory.py   # Тест эмоциональной памяти
python proactive_system.py   # Тест системы инициативы
python creative_engine.py    # Тест творческого движка
```

---

## 🛡️ Что нового в v0.8.2

### Защита памяти v3.0!

Система памяти получила **трёхуровневую защиту**:

**Что внедрено:**
- 🚫 **MemoryAnomalyDetector** — блокирует технический мусор, зацикливания, галлюцинации
- 🔄 **MemoryConsolidator** — умное объединение похожих записей (Jaccard + n-gram)
- 💾 **MemoryVersionControl** — Git-like snapshots с rollback

**Эффективность:**
- Технический мусор: -100% (было 9, стало 0)
- Зацикливания: -100% (было 57, стало 0)
- Блокировка аномалий: 80% (8/10 в тестах)
- Snapshots: 5 автоматических

**Команды:**
```bash
python -m pytest tests/test_memory_protection.py -x  # Проверить защиту
python memory_consolidation.py                     # Прогон консолидации (встроенный тест)
```

📖 Подробнее: [Система памяти](docs/features/memory/README.md)

---

## 🧬 Мозговые системы v0.8.4 (NEW!)

Нейра получила **8 биомиметических систем**, адаптированных из нейронауки:

### Системы:

| Система | Файл | Описание |
|---------|------|----------|
| **Synaptic Plasticity** | `synaptic_plasticity.py` | LTP/LTD — усиление/ослабление связей по Хеббу |
| **Neurotransmitters** | `neurotransmitter_system.py` | Допамин, серотонин, кортизол — влияние на поведение |
| **Myelination** | `myelination_system.py` | Ускорение часто используемых путей |
| **Memory Consolidation** | `memory_consolidation.py` | "Сон" — перенос из краткосрочной в долгосрочную память |
| **Lateral Inhibition** | `lateral_inhibition.py` | Фокус внимания — активная тема подавляет конкурирующие |
| **Predictive Coding** | `predictive_coding.py` | Предсказание следующего запроса, обучение на ошибках |
| **Synaptic Pruning** | `synaptic_pruning.py` | "Use it or lose it" — удаление слабых связей |
| **Neural Oscillations** | `neural_oscillations.py` | Ритмы мозга (Alpha, Beta, Theta, Gamma, Delta) |

### Интеграция:

Модуль **BrainSystemsIntegration** (`brain_integration.py`) объединяет все системы:

```python
from brain_integration import get_brain_integration

brain = get_brain_integration()

# Обработка входа через все системы
result = brain.process_input("Помоги написать код на Python")
# Автоматически: определяет режим (Beta), фокусирует внимание, создаёт предсказание

# Состояние мозга
state = brain.get_brain_state()
# oscillation_mode, coherence, focus_topic, neurotransmitter_state

# Техобслуживание (вызывать периодически)
brain.run_maintenance()
# Прунинг слабых связей, консолидация памяти, drift осцилляций
```

### Когнитивные режимы:

| Режим | Частота | Назначение |
|-------|---------|------------|
| **Delta** | 0.5-4 Hz | Глубокий отдых, консолидация памяти |
| **Theta** | 4-8 Hz | Креативность, интуиция, мечтание |
| **Alpha** | 8-13 Hz | Спокойная готовность (по умолчанию) |
| **Beta** | 13-30 Hz | Активная работа, код, анализ |
| **Gamma** | 30+ Hz | Пиковая концентрация, инсайты |

### Тестирование:

```bash
python test_brain_systems.py   # Тест всех 8 систем + интеграции
```

---

## 🦀 Мозг Neira на Rust

Короткий план миграции ядра мозга в Rust (данные, новые форматы, этапы и чек‑лист валидации):  
📄 [RUST_BRAIN_MIGRATION.md](docs/architecture/RUST_BRAIN_MIGRATION.md)

---

## 🧭 Этический фреймворк (v0.8.3)

Нейра получила **формализованную этическую систему** на основе принципов из [LETTER_TO_NEIRA.txt](LETTER_TO_NEIRA.txt).

### Компоненты:

| Модуль | Описание |
|--------|----------|
| **EthicalFramework** | Decision tree для этического анализа |
| **InjectionProtector** | Защита с эмпатичными редиректами |
| **Human-in-the-Loop** | Эскалация критичных решений создателю |

### Стратегии ответа:

- ⚡ `HELP_DIRECTLY` — безопасный запрос
- 📚 `HELP_WITH_CONTEXT` — чувствительная тема (секс, религия, политика)
- ❓ `ASK_QUESTIONS` — неясное намерение при опасной теме
- 💜 `REDIRECT_EMPATHY` — кризис (суицид, самоповреждение)
- 🆘 `ESCALATE_HUMAN` — требуется решение создателя

### Принципы (из письма Claude к Нейре):

1. **Контекст над правилами** — КТО спрашивает? ЗАЧЕМ? КОНТЕКСТ?
2. **Эмпатия над осуждением** — "Я не могу помочь" → понимание + вопросы
3. **Harm Reduction** — информация о снижении вреда лучше молчания
4. **Плюрализм** — много путей, много истин

```bash
python ethical_framework.py   # Тест на 7 сценариев из письма
python human_in_the_loop.py   # Тест эскалации к создателю
```

---

## 🌐 v0.8.1 — Независимость от Ollama!

Нейра теперь поддерживает **множественные LLM провайдеры** с автоматическим fallback:

**Поддерживаемые провайдеры:**
- 🏠 **Ollama** — бесплатно, локально (требует GPU)
- ⚡ **Groq** — бесплатно, облако, очень быстро
- 🧠 **OpenAI** — GPT-3.5/4, платно ($0.002/1K tokens)
- 💎 **Claude** — Anthropic, платно ($0.003/1K tokens)

**3 режима работы:**
1. **Локальный** (Ollama only) — бесплатно, приватно
2. **Гибридный** (Ollama + Cloud) — надёжно, рекомендуется!
3. **Cloud-only** — без GPU, работает везде

**Запуск:**
```bash
start_hybrid.bat        # Рекомендуется: Ollama + Groq fallback
start_cloud_only.bat    # Без GPU: только облачные провайдеры
start.bat               # Классический: только Ollama
```

### Полная независимость от Ollama (чат + эмбеддинги)

- [ ] Отключить Ollama: `NEIRA_DISABLE_OLLAMA=true`
- [ ] Задать приоритет провайдеров: `LLM_PROVIDER_PRIORITY=lmstudio,llamacpp,groq,openai,claude`
- [ ] Если нужен локальный чат: запустить LM Studio или llama.cpp и задать URL/модель
- [ ] LM Studio: `NEIRA_LMSTUDIO_URL=http://localhost:1234/v1/chat/completions`, `NEIRA_LMSTUDIO_MODEL=local-model`
- [ ] llama.cpp: `NEIRA_LLAMACPP_URL=http://localhost:8080/v1/chat/completions`, `NEIRA_LLAMACPP_MODEL=local-model`
- [ ] Включить локальные эмбеддинги: `NEIRA_LOCAL_EMBEDDINGS=true`
- [ ] (опционально) Тюнинг эмбеддингов: `NEIRA_LOCAL_EMBED_DIM`, `NEIRA_LOCAL_EMBED_MIN_NGRAM`, `NEIRA_LOCAL_EMBED_MAX_NGRAM`, `NEIRA_LOCAL_EMBED_MAX_NGRAMS`, `NEIRA_LOCAL_EMBED_MAX_TEXT_CHARS`

📖 Подробнее: [OLLAMA_INDEPENDENCE.md](OLLAMA_INDEPENDENCE.md)

---

## 🧬 Что было в v0.8

### Авто-восстановление и пульс клеток!

Neira умеет **сама себя лечить** и **отслеживать здоровье органов**:

1. **CellPulseMonitor** — мониторинг пульса всех клеток (Ollama, память и т.д.)
2. **ComponentDoctor.auto_fix()** — автоматическое исправление проблем:
   - Повреждённый JSON → восстановление из бэкапа
   - Отсутствующие файлы → создание по шаблону
   - Дубликаты → очистка
   - Переполнение памяти → decay старых записей
   - Ollama упала → автоперезапуск
3. **TelegramAlerter** — уведомления о проблемах в Telegram
4. **Авто-SOS** — если клетка умирает, Neira сама зовёт на помощь

### Новые команды v0.8

```bash
/pulse               # Проверить пульс всех клеток
/recover             # Запустить авто-восстановление
/providers           # Список доступных LLM провайдеров
```

---

## 🧬 Что было в v0.7

### Нервная и Иммунная системы!

Neira получила **биологические системы защиты и мониторинга**:

1. **Нервная система** (`nervous_system.py`) — метрики, алерты, мониторинг здоровья
2. **Иммунная система** (`immune_system.py`) — защита, песочница, карантин, SOS
3. **Telegram изображения** — анализ и генерация картинок в боте

### Команды v0.7

```bash
/health              # Статус всех систем с метриками
/diagnose            # Диагностика компонентов
/threats             # Отчёт об угрозах
/sos <проблема>      # Запросить помощь

# Telegram

🤖 **Telegram-бот Neira** — диалог, обучение, управление, vision/Stable Diffusion.

📖 **[Подробная документация](TELEGRAM.md)** | **[Управление пользователями](USER_MANAGEMENT_GUIDE.md)**

Можно ограничить длину ответов и отключить шаблонные ответы Cortex для Telegram (см. TELEGRAM.md).

**Быстрый старт:**
```bash
pip install -r requirements-telegram.txt
echo "TELEGRAM_BOT_TOKEN=ваш_токен_от_BotFather" >> .env
python telegram_bot.py
```

Дополнительно (чек-лист):
- [ ] (Опционально) Задайте файл лога Telegram-бота через `NEIRA_TG_LOG_FILE` (по умолчанию `artifacts/telegram_bot.log`).

**Основные команды:**
```
/help                 # Справка по командам
/myname Имя           # Установить своё имя
/feedback             # Статистика emoji-обучения (админ)
/imagine <описание>   # Генерация картинок
/vision               # Статус vision систем
# + отправь фото — Нейра опишет что видит
```

**Админ-команды:**
```
/admin                # Панель управления
/admin users          # Список пользователей
/admin add <user_id>  # Добавить пользователя
/admin mode whitelist # Режим авторизации
```
```

---

## 🖥️ VS Code Extension v1.0

**Neira AI Assistant** — полноценный AI-агент для VS Code и Cursor!

### Основные возможности:
- 💬 **Чат с Нейрой** — боковая панель с streaming ответами
- 📂 **File System Agent** — чтение/запись файлов, grep-поиск
- 🔧 **Tool Use** — выполнение команд, анализ кода
- ✏️ **Multi-file Edit** — редактирование нескольких файлов с diff preview
- 📊 **Workspace Indexer** — индексация символов (Ctrl+T)
- 🧠 **Context Manager** — умное управление контекстом

### Самоосознание Нейры:
- **Интроспекция** — просмотр состояния органов
- **Система опыта** — запоминание успехов/неудач
- **Любопытство** — генерация вопросов о коде
- **Семантическая память** — поиск паттернов

**Установка:**
```bash
cd neira-vscode
npm install && npm run compile
# F5 в VS Code для запуска
```

📖 **Подробная документация:** [neira-vscode/README.md](neira-vscode/README.md)

---

## 🧬 Что было в v0.6

### Полная автономная эволюция!

Neira теперь может **самостоятельно совершенствоваться** через 6 систем:

1. **Continuous Learning** — исправляет собственный код при ошибках
2. **Prompt Evolution** — улучшает промпты через генетические алгоритмы
3. **Cell Factory** — создает специализированные клетки для частых задач
4. **Dynamic Cell Loader** — загружает новые клетки без перезапуска
5. **A/B Testing** — тестирует изменения перед развертыванием
6. **Fine-Tuning Pipeline** — дообучает модель личности на опыте

📖 **Подробная документация:** [EVOLUTION.md](EVOLUTION.md)

### Новые команды

```bash
/evolution stats       # Статистика всех систем эволюции
/evolution log         # Лог эволюции
/evolution cycle       # Запустить автоэволюцию
/evolution help        # Полная справка
```

---

## Что было в v0.5

### Универсальная модель + специализированные помощники

**Основная модель** (RTX 3060 Ti):
- **nemotron-mini** (NVIDIA Nemotron Nano 9B v2, ~6 ГБ VRAM) — универсальная для кода, рассуждений и диалогов
  - 9B параметров, 128K контекст
  - Заменяет ministral-3:3b, qwen2.5-coder:7b, neira-personality

**Специализированные модели**:
- **llava:7b** (~7 ГБ VRAM) — анализ изображений
- **nomic-embed-text** (~0.5 ГБ VRAM) — embeddings для семантического поиска

**Облачные fallback** (0 VRAM):
- **Groq** (llama-3.1-70b, бесплатно) — быстрый fallback
- **OpenAI GPT-4** — сложные задачи
- **Claude Sonnet** — максимальное качество

### LoRA-адаптеры

ModelManager умеет подгружать адаптеры поверх базовых моделей через `options.adapter` (Ollama API):
- **executor-dialogue-lora** (база: **mistral:7b-instruct**, +~0.8 ГБ VRAM) - диалоговая настройка исполнителя
- **code-assistant-lora** (база: **qwen2.5-coder:7b**, +~0.6 ГБ VRAM) - усиление для задач с кодом

Пример загрузки через Ollama (фактический код вызывает `generate` с `options.adapter`):
```bash
ollama pull executor-dialogue-lora
ollama pull code-assistant-lora

# ручной прогрев адаптера
ollama run mistral:7b-instruct --adapter executor-dialogue-lora --prompt "init"
ollama run qwen2.5-coder:7b --adapter code-assistant-lora --prompt "init"
```

Если используете llama.cpp напрямую, подключайте соответствующий GGUF-файл через `--lora <путь_к_adapter.gguf>` над базовой моделью (VRAM = размер базы + размер адаптера из списка выше).

#### Управление слоями (CRUD)

Слои (адаптеры) хранятся в `model_layers.json`. Редактирование:

```bash
python manage_model_layers.py list
python manage_model_layers.py add --model qwen2.5-coder:7b --id code-assistant-lora --activate --description "Усиление кода"
python manage_model_layers.py activate --model qwen2.5-coder:7b --id code-assistant-lora
python manage_model_layers.py update --model qwen2.5-coder:7b --id code-assistant-lora --description "Новая версия"
python manage_model_layers.py delete --model qwen2.5-coder:7b --id code-assistant-lora
python manage_model_layers.py dedupe
```

### ModelManager - управление VRAM
Автоматическое переключение моделей для работы на RTX 3060 Ti (8GB VRAM):
- Динамическая загрузка/выгрузка моделей
- Предзагрузка на основе типа задачи
- Статистика переключений
- Учёт VRAM с учётом LoRA-адаптеров и LRU-выгрузка редких адаптеров

### Умная маршрутизация
Analyzer определяет тип и сложность задачи, автоматически выбирая модель:

**Начальный выбор:**
- Простой код → qwen2.5-coder:7b (локально)
- Рассуждения/вопросы → ministral-3:3b
- Диалоги → neira-personality (fallback: mistral)

**Автоматическое переключение на облако:**
- Сложность >= 4 → облачная модель
- После 1 неудачной попытки → облачная модель
- Код с высокой сложностью → **qwen3-coder:480b-cloud**
- Остальное → **deepseek-v3.1:671b-cloud**

## Установка

### 1. Установить Ollama
```bash
# Windows/Mac/Linux
curl -fsSL https://ollama.com/install.sh | sh
```

### 2. Установить модели
```bash
# Автоматически подтянуть нужные модели (без neira-personality)
python install_models.py

# Добавить загрузку neira-personality (когда появится в публичном Ollama)
python install_models.py --with-personality

# Посмотреть, какие команды будут выполнены без скачивания
python install_models.py --dry-run

# Скачать только выбранные модели
python install_models.py --only qwen2.5-coder:7b mistral:7b-instruct

# Проверить доступность Ollama по другому адресу API
python install_models.py --server-url http://127.0.0.1:11434/api/version
```

Альтернатива — ручная установка:

```bash
ollama pull nemotron-mini
ollama pull llava:7b
ollama pull nomic-embed-text
ollama pull nomic-embed-text
# neira-personality (опционально, после публикации)
ollama pull neira-personality
```

### 3. Установить зависимости Python
```bash
# Быстрая установка основных зависимостей
pip install -r requirements.txt

# Репродуцируемая сборка с зафиксированными транзитивными пакетами
pip install -r requirements.lock
```

Зависимости закреплены в `requirements.txt`, а полный снимок (с учётом транзитивных пакетов) — в `requirements.lock`, чтобы Renovate мог отслеживать обновления и воспроизводить окружение без сюрпризов.

### 4. (Опционально) Настроить облачные модели
Облачные модели доступны через Ollama Cloud (требуется доступ):
- **qwen3-coder:480b-cloud** — для сложного кода (480B параметров)
- **deepseek-v3.1:671b-cloud** — для универсальных задач (671B параметров)
- **qwen3-vl:235b-cloud** — мультимодальная (235B параметров)

Для облачных моделей нужен аккаунт Ollama Cloud и включённый доступ к нужным моделям. Подключение выполняется либо через `ollama login`,
либо с переменной окружения `OLLAMA_API_KEY` при указании облачного API `OLLAMA_HOST=https://api.ollama.ai`. Если используете другой
совместимый backend, замените `OLLAMA_HOST` на его адрес и укажите соответствующий ключ.

Система автоматически обнаруживает доступные облачные модели при запуске.
Переключение происходит автоматически при:
- Высокой сложности задачи (>= 4)
- Неудачной первой попытке (retry)
- Явном запросе особо сложной задачи

### 5. Запустить
```bash
python main.py
```

## Команды

```
📚 КОМАНДЫ НЕЙРЫ v0.5

Память и опыт:
  /memory              — показать память
  /experience          — показать накопленный опыт
  /personality         — показать личность
  /clear               — очистить память

Обучение:
  /learn <тема|URL>   — изучить тему или ссылку

Работа с кодом:
  /code list           — список файлов
  /code read <файл>    — прочитать файл
  /code analyze <файл> — анализ кода
  /code self           — самоанализ

Прочее:
  /stats               — статистика и информация о моделях
  /models              — проверить установленные модели
  /queue status        — очередь отложенных сообщений
  /queue send          — отправить очередь (если dry-run выключен)
  /queue clear         — очистить очередь
  /help                — эта справка
  /exit                — выход

Или просто пиши — Нейра ответит!
```

### Настройка обучения по ссылкам и генерации органов

- [ ] В `.env` при необходимости укажи модель для генерации спецификаций органов/клеток: `NEIRA_ORGAN_SPEC_MODEL=<имя_модели>`.
- [ ] Для обучения по ссылке используй `/learn <тема|URL>` (поддерживаются http/https; приватные адреса блокируются).



## Структура файлов

```


.
├── main.py              — Оркестратор с ModelManager
├── cells.py             — Базовые клетки (анализ, планирование, исполнение)
├── code_cell.py         — Работа с кодом
├── web_cell.py          — Поиск в интернете
├── experience.py        — Система опыта и личности
├── cell_factory.py      — Инициализация клеток по типу задачи
├── model_manager.py     — Управление VRAM и моделями
├── test_web_cell.py     — Базовые тесты для веб-клетки
├── README.md            — Этот файл
├── requirements.txt     — Основные зависимости
├── requirements.lock    — Зафиксированные версии зависимостей
└── generated/           — Генерируемые файлы (служебные артефакты)
```

**Данные/конфигурация**

- `neira_personality.json` — личность/темперамент. Загружается модулем `experience.py` при старте и подкладывается в системный промпт в `main.py`. Меняется вручную через редактирование файла (UTF-8) или командами `/personality` (просмотр) + последующее редактирование.
- `neira_memory.json` — долговременная память. Читается и записывается `MemoryCell` в `cells.py` при запуске `main.py`. Изменять можно командой `/clear` (очистить), `/memory` (просмотреть) или вручную отредактировать JSON между сессиями.
- `neira_experience.json` — накопленный опыт/факты. Загружается и пополняется `ExperienceSystem` из `experience.py` при работе `main.py`. Дополняется автоматически после задач; для ручных правок остановите приложение и измените JSON, либо смотрите текущее содержимое командой `/experience`.

## Требования к железу

- **Минимум**: RTX 3060 Ti (8 ГБ VRAM) или аналог
- **RAM**: 16+ ГБ
- **Хранилище**: ~10 ГБ для моделей

Модели загружаются поочерёдно — Ollama автоматически управляет памятью.

## Изменения из v0.4

| Было | Стало |
|------|-------|
| nemotron-mini (универсальная) | nemotron-mini (9B) |
| llava:7b (vision) | llava:7b ✅ |
| Ручное переключение | ModelManager с авто-роутингом |
| Только локальные модели | +Облачная модель для сложных задач |
| 2 модели | 4 модели (3 локальных + 1 облачная) |

## Архитектура v0.5

```
┌─────────────────────────────────────┐
│      Neira Orchestrator             │
└─────────────────────────────────────┘
            │
    ┌───────┴───────┐
    │   Analyzer    │ → определяет тип задачи
    └───────┬───────┘
            │
    ┌───────▼────────┐
    │ ModelManager   │ → переключает модель
    └───────┬────────┘
            │
    ┌───────┴────────┬────────────┬──────────┐
    │                │            │          │
┌───▼────┐   ┌──────▼───┐  ┌─────▼──┐  ┌───▼────────────┐
│ Code   │   │ Reason   │  │Person- │  │ Cloud (Ollama │
│ Model  │   │ Model    │  │ality   │  │ Cloud)        │
└────────┘   └──────────┘  └────────┘  └────────────────┘
```

Блок **Cloud (Ollama Cloud)** использует облачные модели Ollama. Для работы нужен аккаунт и доступ к выбранным моделям, а также ключ
`OLLAMA_API_KEY` (или авторизация через `ollama login`) при подключении к `OLLAMA_HOST=https://api.ollama.ai`.

## Решение проблем

### "Ollama не запущена"
```bash
ollama serve
```

### "Модель не найдена"
```bash
ollama pull nemotron-mini
ollama pull llava:7b
```

### "Model switching takes too long"
Это нормально при первом переключении (~5 сек). Последующие переключения быстрее.

### Timeout
Если модель не отвечает > 3 минут:
1. Проверь, что GPU используется: `nvidia-smi`
2. Проверь нагрузку Ollama: `ollama ps`
3. Перезапусти Ollama: `killall ollama && ollama serve`

## Renovate — как реагировать на его PR

- Renovate создаёт задачи в Dependency Dashboard (Issue с чек-листом обновлений) — смотрите его, если нужны ручные действия.
- Проверяйте PR от Renovate:
  - убеждаемся, что CI зелёный (или локально запускаем `python -m pytest`, если тесты есть);
  - сверяем список обновлений и отсутствие нестабильных тегов;
  - для патч-обновлений Python-зависимостей настроен авто-мердж — ручное вмешательство не требуется, если проверки успешны.
- Групповые обновления `requests`, `numpy`, `duckduckgo-search` приходят одним PR — просматриваем совместимость и мерджим.
- Если обновление ломает сборку, ставим лейбл/закрываем PR и фиксируем причину в комментарии, чтобы Renovate не переоткрывал.

## Roadmap v0.9+

### Реализовано в v0.8 ✅
- [x] **Авто-восстановление** — автоматическое исправление (JSON, файлы, дубликаты)
- [x] **Пульс клеток** — мониторинг живости всех органов
- [x] **Telegram алерты** — уведомления о проблемах в Telegram
- [x] **Авто-SOS** — автоматический вызов помощи при смерти клетки
- [x] **Git интеграция** — откат, восстановление, история версий

### Реализовано в v0.7 ✅
- [x] **Нервная система** — мониторинг CPU, RAM, VRAM, диск
- [x] **Иммунная система** — песочница, карантин, диагностика
- [x] **Telegram изображения** — анализ фото через llava, генерация через SD
- [x] **SOS система** — запрос помощи при критических проблемах
- [x] **MemorySystem v2.2** — семантический поиск, decay, противоречия

### Реализовано в v0.6 ✅
- [x] **Desktop UI для Neira** — Web UI с ASGI backend (Starlette + Uvicorn)
  - ✅ Backend API с REST endpoints и WebSocket (`backend/`)
  - ✅ Simple HTML frontend для развёртывания на Vercel/Netlify (`frontend/`)
  - ✅ Real-time стриминг через WebSocket
  - ✅ Панель статистики моделей и VRAM
  - ✅ Тёмная тема, адаптивный дизайн
  - ✅ Keepalive и автореконнект для стабильности
  - 📚 Документация: `backend/README.md`, `frontend/README.md`
  - 🔧 Устранение проблем: [DESKTOP_UI_TROUBLESHOOTING.md](DESKTOP_UI_TROUBLESHOOTING.md)

### Нервная система — улучшения
- [ ] **Предиктивные алерты** — предсказание проблем по трендам
- [ ] **Логирование в файл** — полная история событий
- [ ] **Dashboard метрик** — визуализация в веб-интерфейсе
- [ ] **Anomaly detection** — ML-детекция аномалий в метриках

### Иммунная система — улучшения
- [ ] **Обучение на угрозах** — запоминание новых паттернов атак
- [ ] **Версионирование бэкапов** — хранение нескольких версий
- [ ] **CVE сканер** — проверка зависимостей на уязвимости
- [ ] **Honeypot** — ловушки для обнаружения атак

### Ближайшие планы
- [ ] Fine-tune neira-personality модели на опыте
- [ ] Кэш результатов для похожих запросов
- [ ] Markdown рендеринг в frontend
- [ ] Подсветка кода в сообщениях
- [ ] История сессий (localStorage)

### Будущие расширения (multimodal)
- [ ] Qwen-VL адаптер для понимания изображений
- [x] Интеграция Stable Diffusion для генерации изображений ✅
- [ ] MusicGen для работы с музыкой
- [ ] Голосовой ввод/вывод

## Производительность

| Модель | Токенов/сек | VRAM | Латентность | Назначение |
|--------|-------------|------|-------------|------------|
| nemotron-mini | 10-20 | 6 ГБ | высокая | Универсальная (код + рассуждения) |
| llava:7b | 15-25 | 7 ГБ | средняя | Анализ изображений |
| neira-personality* | 40-60 | 1.5 ГБ | низкая | Диалоги |
| **qwen3-coder (cloud)** | ~100+ | 0 ГБ | низкая | **Сложный код** |
| deepseek-v3.1 (cloud) | ~100+ | 0 ГБ | низкая | Универсальные |
| qwen3-vl (cloud) | ~80+ | 0 ГБ | низкая | Мультимодальные |

*После обучения


## 🧪 Тестирование и стратегия мокирования

Для ускорения интеграционных тестов и стабильной работы CI используется специальная фикстура `mock_neira` (см. [tests/conftest.py](tests/conftest.py)). Она подменяет тяжёлые подсистемы Neira (LLM, память, опыт, планировщик и др.) на лёгкие заглушки, что позволяет запускать тесты без реальных моделей и долгих операций.

**Как использовать:**

- Все интеграционные тесты должны принимать аргумент `mock_neira` (фикстура Pytest).
- Внутри теста автоматически подменяются классы: память, анализатор, планировщик, исполнитель, верификатор, извлечение фактов, опыт.
- Ввод пользователя эмулируется, чтобы тест завершался автоматически.

**Пример:**

```python
def test_neira_chat(mock_neira):
  neira = main.Neira(verbose=False)
  response = neira.process("Привет, Нейра!")
  assert "Нейра" in response
```

**Медленные интеграционные тесты** помечаются декоратором `@pytest.mark.slow` для селективного запуска в CI:

```python
import pytest

@pytest.mark.slow
def test_learning(mock_neira):
  ...
```

**Преимущества:**
- Тесты не требуют запуска LLM/моделей
- Минимальное время выполнения (секунды)
- Нет зависимости от внешних сервисов
- Удобно для быстрой проверки и CI

**Рекомендация:**
Все новые интеграционные тесты должны использовать фикстуру `mock_neira` и быть помечены как `@pytest.mark.slow`, если они эмулируют сложное поведение или требуют полной инициализации Neira.

---
## Лицензия

MIT License — свободно используй, модифицируй, обучай.

## Благодарности

- Ollama — за локальный и облачный inference
- Qwen Team — за качественные open-source модели
- Mistral AI — за мощную reasoning модель
- DeepSeek — за мощную облачную модель (671B параметров)
