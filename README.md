# NEIRA v0.5 — Живая программа

## Что нового в v0.5

### Четыре модели с умной маршрутизацией
- **qwen2.5-coder:7b** (~5 ГБ VRAM) — для работы с кодом
- **mistral:7b-instruct** (~4.5 ГБ VRAM) — для рассуждений, планирования, верификации
- **neira-personality** (~1.5 ГБ VRAM) — для диалогов с личностью (будет обучена позже)
- **deepseek-v3.1:671b-cloud** (0 VRAM) — облачная модель Ollama для особо сложных задач (671B параметров)

### ModelManager — управление VRAM
Автоматическое переключение моделей для работы на RTX 3060 Ti (8GB VRAM):
- Динамическая загрузка/выгрузка моделей
- Предзагрузка на основе типа задачи
- Статистика переключений

### Умная маршрутизация
Analyzer определяет тип задачи и автоматически выбирает оптимальную модель:
- Код → qwen-coder
- Рассуждения/вопросы → mistral
- Диалоги → neira-personality (fallback: mistral)
- Сложные задачи → облачная модель

## Установка

### 1. Запустить Ollama
```bash
ollama serve
```

### 2. Установить модели
```bash
# Основные модели
ollama pull qwen2.5-coder:7b
ollama pull mistral:7b-instruct
ollama pull nomic-embed-text
```

### 3. Установить зависимости Python
```bash
pip install requests numpy duckduckgo-search
```

### 4. (Опционально) Настроить облачную модель
Облачная модель **deepseek-v3.1:671b-cloud** доступна через Ollama.
Если у вас есть доступ к облачным моделям Ollama, система автоматически их обнаружит.

Список доступных облачных моделей можно посмотреть через интерфейс Ollama.

### 5. Запустить
```bash
python main.py
```

## Команды

```
📚 КОМАНДЫ НЕЙРЫ v0.5

Память и опыт:
  /memory              — показать память
  /experience          — показать накопленный опыт
  /personality         — показать личность
  /clear               — очистить память

Обучение:
  /learn <тема>        — изучить тему из интернета

Работа с кодом:
  /code list           — список файлов
  /code read <файл>    — прочитать файл
  /code analyze <файл> — анализ кода
  /code self           — самоанализ

Прочее:
  /stats               — статистика и информация о моделях
  /models              — проверить установленные модели
  /help                — эта справка
  /exit                — выход

Или просто пиши — Нейра ответит!
```

## Структура файлов

```
neira_v05/
├── main.py              — Оркестратор с ModelManager
├── cells.py             — Базовые клетки (анализ, планирование, исполнение)
├── model_manager.py     — Управление VRAM и моделями
├── code_cell.py         — Работа с кодом
├── web_cell.py          — Поиск в интернете
├── experience.py        — Система опыта и личности
└── README.md            — Этот файл
```

## Требования к железу

- **Минимум**: RTX 3060 Ti (8 ГБ VRAM) или аналог
- **RAM**: 16+ ГБ
- **Хранилище**: ~10 ГБ для моделей

Модели загружаются поочерёдно — Ollama автоматически управляет памятью.

## Изменения из v0.4

| Было | Стало |
|------|-------|
| qwen2.5:3b (chat) | mistral:7b-instruct (reason) |
| qwen2.5-coder:7b | qwen2.5-coder:7b ✅ |
| Ручное переключение | ModelManager с авто-роутингом |
| Только локальные модели | +Облачная модель для сложных задач |
| 2 модели | 4 модели (3 локальных + 1 облачная) |

## Архитектура v0.5

```
┌─────────────────────────────────────┐
│      Neira Orchestrator             │
└─────────────────────────────────────┘
            │
    ┌───────┴───────┐
    │   Analyzer    │ → определяет тип задачи
    └───────┬───────┘
            │
    ┌───────▼────────┐
    │ ModelManager   │ → переключает модель
    └───────┬────────┘
            │
    ┌───────┴────────┬────────────┬──────────┐
    │                │            │          │
┌───▼────┐   ┌──────▼───┐  ┌─────▼──┐  ┌───▼────┐
│ Code   │   │ Reason   │  │Person- │  │ Cloud  │
│ Model  │   │ Model    │  │ality   │  │ (Groq) │
└────────┘   └──────────┘  └────────┘  └────────┘
```

## Решение проблем

### "Ollama не запущена"
```bash
ollama serve
```

### "Модель не найдена"
```bash
ollama pull mistral:7b-instruct
ollama pull qwen2.5-coder:7b
```

### "Model switching takes too long"
Это нормально при первом переключении (~5 сек). Последующие переключения быстрее.

### Timeout
Если модель не отвечает > 3 минут:
1. Проверь, что GPU используется: `nvidia-smi`
2. Проверь нагрузку Ollama: `ollama ps`
3. Перезапусти Ollama: `killall ollama && ollama serve`

## Roadmap v0.6+

### Ближайшие планы
- [ ] Fine-tune neira-personality модели на опыте
- [ ] Автоматическое переключение на cloud при retry >= 2
- [ ] Кэш результатов для похожих запросов

### Будущие расширения (multimodal)
- [ ] Qwen-VL адаптер для понимания изображений
- [ ] Интеграция Stable Diffusion для генерации изображений
- [ ] MusicGen для работы с музыкой
- [ ] Голосовой ввод/вывод

## Производительность

| Модель | Токенов/сек | VRAM | Латентность |
|--------|-------------|------|-------------|
| mistral:7b-instruct | 15-30 | 4.5 ГБ | средняя |
| qwen2.5-coder:7b | 12-25 | 5 ГБ | средняя |
| neira-personality* | 40-60 | 1.5 ГБ | низкая |
| deepseek-v3.1 (cloud) | ~100+ | 0 ГБ | низкая |

*После обучения

## Лицензия

MIT License — свободно используй, модифицируй, обучай.

## Благодарности

- Ollama — за локальный и облачный inference
- Qwen Team — за качественные open-source модели
- Mistral AI — за мощную reasoning модель
- DeepSeek — за мощную облачную модель (671B параметров)
