# Neira VS Code Extension

🧠 **Локальный AI-ассистент Нейра для VS Code и Cursor**

Работает через Ollama — полностью локально, без облачных зависимостей и санкционных ограничений.

## 🚀 Быстрый старт

### Автоматическое определение пути

При первом запуске расширение **автоматически найдёт** проект Neira в:
1. Открытых папках workspace
2. Рядом с расширением (для разработчиков)

Если не найдено — появится диалог выбора папки.

### Настройка пути вручную

Если нужно указать путь вручную:

```json
// Только для текущего проекта (.vscode/settings.json)
{
  "neira.projectPath": "F:\\Нейронки\\prototype"
}

// Или глобально для всех проектов (User Settings)
{
  "neira.projectPath": "/path/to/neira"
}
```

**💡 Рекомендация**: используйте **workspace settings** — так путь не будет конфликтовать между разными проектами.

### Сброс настроек

Если расширение перестало работать после смены проекта:

1. `Ctrl+Shift+P` → `Preferences: Open User Settings (JSON)`
2. Удалите строку с `"neira.projectPath"`
3. Перезапустите VS Code

## ✨ Возможности

### 💬 Чат и помощь
- **Чат с Нейрой** — общение в боковой панели
- **Объяснение кода** — выделите код → ПКМ → "Объяснить"
- **Генерация кода** — опишите что нужно, Нейра напишет
- **Вопросы о коде** — спросите о выделенном фрагменте
- **Streaming ответы** — ответы появляются по мере генерации (SSE)

### 📂 Работа с файлами
- **File System Agent** — чтение, запись, редактирование файлов
- **Поиск по проекту** — grep-подобный поиск в файлах
- **Структура проекта** — древовидное отображение файлов
- **Multi-file редактирование** — изменение нескольких файлов с предпросмотром diff

### 🔧 Инструменты и автоматизация
- **Tool Use / Function Calling** — выполнение команд, анализ кода
- **Выполнение Python** — запуск Python скриптов
- **HTTP запросы** — API-вызовы из чата
- **Системная информация** — данные об окружении

### 📊 Индексация и контекст
- **Workspace Indexer** — индексация всех символов (функции, классы, методы)
- **Поиск символов** — быстрый переход к определениям (Ctrl+T)
- **Context Manager** — умное управление контекстом для LLM
- **Подсчёт токенов** — отображение токенов в статус-баре
- **Автоматическое сжатие** — оптимизация длинных контекстов

### 🚀 Copilot-like фичи
- **Inline Completions** — ghost-text автодополнения
- **Code Actions** — Quick Fix с AI
- **Code Lens** — кнопки над функциями
- **Hover с документацией** - подсказки из индекса
- **Copilot Chat (@neira)** - Нейра как участник чата VS Code
 
### Интеграция с Copilot Chat

- Команда: `Neira: Сделать Neira моделью для Copilot Chat` — регистрирует Нейру как участника Copilot Chat и попытается открыть новый чат с Neira.
- Если ваш VS Code поддерживает Chat API, команда автоматически откроет чат с Neira; иначе — инструкция по ручному выбору участника в Chat UI.

Как использовать:

1. Откройте палитру команд: `Ctrl+Shift+P`.
2. Выполните `Neira: Сделать Neira моделью для Copilot Chat`.
3. Откройте Chat — в списке участников выберите `Neira` или начните новый чат (Neira будет доступна как участник).

### 🧠 Самоосознание Нейры
- **Интроспекция** — просмотр состояния всех органов (клеток) Нейры
- **Система опыта** — запоминание успехов/неудач, извлечение уроков
- **Любопытство** — генерация вопросов о коде, рефлексия
- **Семантическая память** — поиск релевантных паттернов и решений
- **Обратная связь** — обучение на предпочтениях пользователя (👍/👎)

## 🚀 Быстрый старт

### 1. Установите зависимости

```bash
# В папке проекта Neira
pip install aiohttp

# В папке расширения
cd neira-vscode
npm install
```

### 2. Настройте путь к проекту (один раз)

В настройках VS Code (`Ctrl+,` → поиск "neira"):
- `neira.projectPath` — путь к папке с `neira_server.py`

Или при первом запуске расширение предложит выбрать папку.

### 3. Запуск

**Автоматически**: Сервер запустится сам при открытии VS Code!

**Вручную**: Нажмите кнопку `$(debug-start) Нейра` в статус-баре или:
- `Ctrl+Shift+P` → "Neira: Запустить сервер"

### 4. Установите расширение

**Вариант A: Из исходников (для разработки)**

```bash
cd neira-vscode
npm run compile
```

Затем в VS Code: `F5` для запуска в режиме отладки.

**Вариант B: Сборка .vsix (для установки)**

```bash
cd neira-vscode
npm run package
```

Это создаст файл `neira-assistant-1.0.0.-vsix`.

Установка:
```bash
code --install-extension neira-assistant-1.0.0.vsix
```

Или в VS Code: `Ctrl+Shift+P` → "Extensions: Install from VSIX..."
-
### 5. Использование в Copilot Chat

Чек-лист:
- [ ] Обновите VS Code до версии 1.100+.
- [ ] Установите GitHub Copilot Chat (если используете его чат-интерфейс).
- [ ] Откройте чат VS Code и выберите участника `@neira`.
- [ ] Убедитесь, что сервер Нейры запущен (статус-бар: `$(brain) Neira`).

### 6. Панель действий (кнопки)

Чек-лист перед использованием:
- [ ] Сервер Нейры запущен.
- [ ] Открыт workspace проекта (для поиска/индекса/структуры).
- [ ] Выделен код (Объяснить/Исправить/Улучшить/Тесты/Документация).
- [ ] Выделен текст (Запомнить выделенное/Обучить из выделенного).

Примечания:
- [ ] Ответы приходят в чат, код автоматически не изменяется.
- [ ] Раздел "Слои" — заглушки, реализация в процессе.

## ⌨️ Горячие клавиши

| Команда | Windows/Linux | Mac |
|---------|---------------|-----|
| Открыть чат | `Ctrl+Shift+N` | `Cmd+Shift+N` |
| Объяснить код | `Ctrl+Shift+E` | `Cmd+Shift+E` |
| Спросить о выделенном | `Ctrl+Shift+Q` | `Cmd+Shift+Q` |

## 🧠 Команды самоосознания

| Команда | Описание |
|---------|----------|
| `Neira: Состояние Нейры` | Интроспекция — показать состояние органов |
| `Neira: Рефлексия Нейры` | Запросить рефлексию о последних действиях |
| `Neira: Вкл/Выкл любопытство` | Переключить режим любопытства |
| `Neira: Запомнить выделенное` | Сохранить код/паттерн в память |
| `Neira: Поиск в памяти` | Семантический поиск по памяти |
| `Neira: Быстрая обратная связь` | Оценить последний ответ 👍/👎 |

## 🎓 Команды обучения

Нейра может обучаться из различных источников, защищённых от интернет-шума:

| Команда | Описание |
|---------|----------|
| `Neira: Обучить Нейру` | Диалог выбора источника обучения |
| `Neira: Обучить из URL` | Загрузить статью/документацию |
| `Neira: Обучить из файла` | Изучить локальный файл (txt, md, pdf, code) |
| `Neira: Обучить из выделенного` | Изучить выделенный текст |
| `Neira: Статистика обучения` | Показать что изучено |

### Поддерживаемые источники:
- 📁 **Файлы**: txt, md, py, js, ts, pdf и др.
- 🌐 **Веб-страницы**: статьи, документация (с фильтрацией рекламы)
- ▶️ **YouTube**: автоматическое извлечение транскриптов

### Защита от шума:
- ❌ Удаление рекламы и баннеров
- ❌ Фильтрация навигации и сайдбаров
- ❌ Очистка от cookie-попапов
- ✅ Извлечение основного контента

## 🖥️ Управление сервером

Кнопка в статус-баре показывает состояние и позволяет управлять сервером:

| Состояние | Иконка | Действие по клику |
|-----------|--------|-------------------|
| Запущен | `$(hubot) Нейра` | Остановить сервер |
| Остановлен | `$(debug-start) Нейра` | Запустить сервер |
| Запускается | `$(loading~spin) Нейра...` | — |
| Ошибка | `$(error) Нейра` | Перезапустить |

### Команды:
| Команда | Описание |
|---------|----------|
| `Neira: Запустить сервер` | Запустить сервер Нейры |
| `Neira: Остановить сервер` | Остановить сервер |
| `Neira: Перезапустить сервер` | Перезапустить сервер |
| `Neira: Показать лог сервера` | Открыть Output Panel с логами |

## ⚙️ Настройки

В VS Code: `Ctrl+,` → Поиск "neira"

| Параметр | Описание | По умолчанию |
|----------|----------|--------------|
| `neira.serverUrl` | URL сервера Нейры | `http://127.0.0.1:8765` |
| `neira.autoStartServer` | Автозапуск сервера | `true` |
| `neira.pythonPath` | Путь к Python | `python` |
| `neira.projectPath` | Путь к проекту Neira (с neira_server.py) | — |
| `neira.showStatusBar` | Показывать статус | `true` |
| `neira.enableInlineCompletions` | Ghost-text автодополнение | `true` |
| `neira.enableCodeLens` | Кнопки над функциями | `true` |

## 🖥️ Cursor

Расширение полностью совместимо с **Cursor**!

Установка такая же:
```bash
cursor --install-extension neira-assistant-1.0.0.vsix
```

Или через GUI: `Ctrl+Shift+X` → три точки → "Install from VSIX..."

## 📁 Структура расширения

```
neira-vscode/
├── package.json          # Манифест расширения
├── tsconfig.json         # Конфигурация TypeScript
├── src/
│   ├── extension.ts      # Точка входа
│   ├── neiraClient.ts    # HTTP клиент
│   ├── chatViewProvider.ts   # UI чата
│   └── statusViewProvider.ts # UI статуса
└── media/
    └── neira-icon.png    # Иконка
```

## 🔧 Разработка

```bash
# Установка зависимостей
npm install

# Компиляция
npm run compile

# Компиляция в watch-режиме
npm run watch

# Запуск отладки
# В VS Code: F5

# Сборка .vsix
npm run package
```

## ❓ Частые вопросы

### Расширение не видит сервер

1. Проверьте, запущен ли `neira_server.py`
2. Откройте http://127.0.0.1:8765 в браузере
3. Проверьте настройку `neira.serverUrl`

### Нейра не отвечает

1. Проверьте, запущен ли Ollama: `ollama serve`
2. Есть ли нужные модели: `ollama list`
3. Смотрите логи в терминале с сервером

### Как обновить расширение?

1. Пространение нового `.vsix`
2. `code --install-extension neira-assistant-X.X.X.vsix --force`

## 📄 Лицензия

MIT
