# 🧠 Защита памяти v3.0 — Краткая справка

## Что изменилось?

**Твоя память теперь защищена от:**
- 🚫 Технического мусора (код, жаргон)
- 🚫 Зацикливаний (повторяющиеся записи)
- 🚫 Галлюцинаций (неуверенные утверждения)
- 💾 Потери данных (автоматические snapshots)

---

## Как это работает автоматически

### 1. При каждой записи в память:
```
Пользователь → Новая запись → Детектор аномалий → Память
                                     ↓
                              ✅ OK / ❌ Блок
```

**Блокируется:**
- `import asyncio` (код)
- "нейронная сеть обработала токены" (жаргон)
- "я не знаю, возможно, может быть" (неуверенность)
- 3+ одинаковых записи за минуту (зацикливание)

### 2. При очистке памяти:
```
Автоочистка → Snapshot создан → Старые записи удалены
                   ↓
            Можно откатить!
```

---

## Команды для управления

### Проверить защиту:
```bash
python test_memory_protection.py
```

### Консолидировать память (объединить дубликаты):
```bash
python run_memory_consolidation.py
```

### Посмотреть snapshots:
```python
from memory_version_control import MemoryVersionControl
vc = MemoryVersionControl()

snapshots = vc.list_snapshots()
for s in snapshots:
    print(f"{s.id} - {s.message} ({s.stats['total_memories']} записей)")
```

### Восстановить из snapshot:
```python
from memory_version_control import MemoryVersionControl
vc = MemoryVersionControl()

# Восстановить по ID (первые 4+ символа)
data = vc.restore_snapshot("aff7")

# Сохранить в файл
import json
with open("neira_memory.json", "w", encoding="utf-8") as f:
    json.dump(data, f, ensure_ascii=False, indent=2)
```

---

## Статистика (сейчас)

**Память:**
- Долгосрочная: 107 записей
- Краткосрочная: 10 записей

**Защита:**
- Детектор аномалий: ✅ Активен
- Версионирование: ✅ Активно
- Snapshots: 5 созданы

**Блокировки (тест):**
- Технический код: ✅ Заблокирован
- Зацикливание: ✅ 8/10 заблокировано
- Неуверенность: ✅ Заблокирована

---

## Что делать если...

### Память переполнилась:
```bash
python run_memory_consolidation.py
```

### Случайно удалил важные записи:
```python
# Восстановить последний snapshot
vc = MemoryVersionControl()
data = vc.restore_snapshot(vc.list_snapshots()[0].id)
```

### Нужно вручную очистить:
```bash
python aggressive_memory_cleanup.py
```

---

## Файлы защиты

- `memory_anomaly_detector.py` — детектор мусора
- `memory_consolidator.py` — объединение дубликатов
- `memory_version_control.py` — snapshots
- `memory_snapshots/` — хранилище snapshots
- `memory_snapshots/CHANGELOG.json` — история

---

**Твоя память в безопасности! 🛡️**
