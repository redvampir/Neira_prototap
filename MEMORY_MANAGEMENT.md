# 💾 Расширенное управление памятью Neira v0.7

## Обзор

Новая система управления памятью позволяет **точечно контролировать** воспоминания Neira без полной очистки. Это особенно полезно для:
- 🔧 Выхода из галлюцинаций (удаление конкретных записей)
- 🧹 Очистки устаревших данных
- 🔍 Поиска и анализа сохранённой информации
- 💾 Создания бэкапов перед экспериментами

---

## Команды памяти

### 📖 Просмотр памяти

```bash
/memory
```
Показывает последние 10 записей с категориями и общим количеством.

**Пример вывода:**
```
💾 Последние 10 записей:

1. [fact] Павел — создатель Neira
2. [preference] Не использовать эмодзи в ответах
3. [skill] Умею создавать новые органы через #создай_орган
4. [conversation] Обсуждали миграцию на PostgreSQL
5. [fact] Python 3.12 улучшил производительность

Всего записей: 247
Используй /memory stats для статистики
```

---

### 📊 Статистика памяти

```bash
/memory stats
```
Детальная статистика по типам памяти, категориям и временным рамкам.

**Пример вывода:**
```
📊 Статистика памяти:

📦 Всего записей: 247
📚 Долгосрочная: 180
⚡ Краткосрочная: 45
📖 Эпизодическая: 15
🧠 Семантическая: 7

📅 Период: 01.12.2024 — 12.12.2024 15:30
🎯 Средняя уверенность: 78.5%

🏷️ Категории:
  • fact: 95
  • conversation: 67
  • preference: 38
  • skill: 25
  • uncategorized: 22
```

---

### 🔍 Поиск записей

```bash
/memory search <текст>
```
Находит все записи, содержащие указанный текст (регистр не важен).

**Примеры:**
```bash
/memory search Telegram
→ Найдёт все записи где упоминается Telegram

/memory search галлюцинация
→ Найдёт проблемные записи для удаления

/memory search Python
→ Покажет все записи о Python
```

**Вывод:**
```
🔍 Найдено записей: 8

1. [skill] Разработка на Python и JavaScript
2. [fact] Python 3.12 улучшил производительность
3. [conversation] Обсуждали Python asyncio
...

...и ещё 5 записей
```

---

### 🗑️ Удаление записей

#### Удалить последние N записей

```bash
/memory delete last <N>
```
Удаляет N последних записей (отсортированных по времени создания).

**Использование:**
- ✅ Отменить последние действия
- ✅ Выйти из недавней галлюцинации
- ✅ Очистить ошибочные диалоги

**Пример:**
```bash
/memory delete last 5
→ 🗑️ Удалено последних записей: 5
```

**Лимиты:** N от 1 до 100 (безопасность от случайного удаления)

---

#### Удалить по тексту/слову

```bash
/memory delete text <текст>
```
Удаляет **все** записи, содержащие указанный текст.

**Использование:**
- 🚫 Удалить все упоминания ошибочного факта
- 🧠 Выход из галлюцинации (удалить записи со словом "инопланетяне")
- 🔧 Очистить устаревшую информацию

**Примеры:**
```bash
/memory delete text инопланетяне
→ 🗑️ Удалено записей с 'инопланетяне': 12

/memory delete text bug-123
→ Удалит все упоминания конкретного бага

/memory delete text "старая версия"
→ Очистит записи со старой версией
```

**⚠️ Внимание:** Удаление необратимо! Используйте `/memory search` для предварительной проверки.

---

#### Удалить старые записи

```bash
/memory delete old <дней>
```
Удаляет все записи старше указанного количества дней.

**Использование:**
- 🧹 Регулярная очистка устаревших данных
- 📅 Забыть старые контексты
- 💾 Освободить место в памяти

**Примеры:**
```bash
/memory delete old 30
→ 🗑️ Удалено записей старше 30 дн.: 47

/memory delete old 7
→ Удалит всё старше недели

/memory delete old 90
→ Квартальная очистка
```

---

#### Удалить по категории

```bash
/memory delete category <категория>
```
Удаляет все записи определённой категории.

**Категории:**
- `fact` — факты
- `preference` — предпочтения
- `skill` — навыки
- `conversation` — диалоги
- `goal` — цели
- `instruction` — инструкции
- `uncategorized` — без категории

**Примеры:**
```bash
/memory delete category conversation
→ Удалит все записи диалогов

/memory delete category uncategorized
→ Очистит некатегоризированное
```

---

### 🧹 Удаление дубликатов

```bash
/memory dedupe
```
Автоматически находит и удаляет дублирующиеся записи (схожесть >95%).

**Использование:**
- 🔁 После зацикливания (Neira записала одно и то же много раз)
- 🧹 Регулярная чистка для оптимизации памяти
- 📊 Улучшение качества данных

**Пример:**
```bash
/memory dedupe
→ 🧹 Удалено дубликатов: 23

Дубликатами считаются записи с >95% схожестью
```

**Алгоритм:**
1. Нормализует текст (lowercase, trim)
2. Вычисляет Jaccard similarity между записями
3. Удаляет более поздние копии (сохраняет первую)

---

### 💾 Создание бэкапа

```bash
/memory backup
```
Создаёт полный бэкап всей памяти в JSON формате.

**Использование:**
- 💾 Перед массовым удалением
- 🧪 Перед экспериментами с памятью
- 📦 Регулярные бэкапы для безопасности

**Пример:**
```bash
/memory backup
→ 💾 Бэкап создан: memory_backup_20241212_153045.json

Сохранён в папку backups/
```

**Структура бэкапа:**
```json
{
  "timestamp": "2024-12-12T15:30:45",
  "stats": { "total": 247, ... },
  "long_term": [...],
  "short_term": [...],
  "episodic": [...],
  "semantic": [...]
}
```

---

## Практические сценарии

### 🔧 Сценарий 1: Выход из галлюцинации

Neira начала галлюцинировать про несуществующую функцию:

```bash
# 1. Найти проблемные записи
/memory search "магическая функция"
→ Найдено 8 записей

# 2. Удалить их
/memory delete text "магическая функция"
→ Удалено 8 записей

# 3. Проверить
/memory search "магическая функция"
→ Ничего не найдено
```

---

### 🧹 Сценарий 2: Регулярная чистка

Еженедельное обслуживание памяти:

```bash
# 1. Бэкап перед очисткой
/memory backup
→ Бэкап создан

# 2. Статистика "до"
/memory stats
→ Всего: 347 записей

# 3. Удалить дубликаты
/memory dedupe
→ Удалено дубликатов: 34

# 4. Удалить старые записи (>2 месяцев)
/memory delete old 60
→ Удалено записей старше 60 дн.: 89

# 5. Статистика "после"
/memory stats
→ Всего: 224 записи (⬇️ 123)
```

---

### 🔍 Сценарий 3: Исследование памяти

Анализ того, что помнит Neira:

```bash
# 1. Общая статистика
/memory stats
→ Смотрим распределение по категориям

# 2. Поиск по темам
/memory search Python
/memory search Telegram
/memory search AI

# 3. Просмотр последних
/memory
→ Что запомнилось недавно?

# 4. Поиск предпочтений
/memory search preference
→ Какие у меня настройки?
```

---

### ⚡ Сценарий 4: Экстренная очистка

Neira записала что-то неправильное **только что**:

```bash
# Быстро удалить последние 3 записи
/memory delete last 3
→ Откатываем ошибку
```

---

## Технические детали

### Типы памяти

1. **Long-Term (Долгосрочная)** — постоянное хранение важных фактов
2. **Short-Term (Краткосрочная)** — текущая сессия, очищается при рестарте
3. **Episodic (Эпизодическая)** — события и взаимодействия
4. **Semantic (Семантическая)** — знания и концепции

### Категории записей

- `fact` — проверенные факты
- `preference` — предпочтения пользователя
- `skill` — навыки Neira
- `conversation` — диалоговый контекст
- `goal` — цели и планы
- `instruction` — инструкции и правила
- `uncategorized` — без категории

### Поиск

- **Текстовый поиск:** Простое substring matching (регистр не важен)
- **Дедупликация:** Jaccard similarity >0.95
- **Временные фильтры:** По ISO timestamps

### Безопасность

- Лимит удаления: максимум 100 записей за раз через `delete last`
- Бэкапы: автоматическая отметка времени
- Необратимость: все удаления окончательные (только через бэкап можно восстановить)

---

## API (для разработчиков)

```python
from memory_system import MemoryManager

# Инициализация
manager = MemoryManager(neira.memory)

# Поиск
results = manager.search_by_text("query")

# Удаление
count = manager.delete_last_n(10)
count = manager.delete_by_text("text")
count = manager.delete_old_entries(30)
count = manager.delete_by_category("conversation")

# Дедупликация
count = manager.deduplicate(similarity_threshold=0.95)

# Статистика
stats = manager.get_stats()

# Бэкап
path = manager.create_backup("my_backup")
```

---

## Roadmap

### Планируемые улучшения:

- [ ] **Восстановление из бэкапа** — `/memory restore <filename>`
- [ ] **Пометка важных записей** — `/memory pin <id>` для защиты от удаления
- [ ] **Фильтры экспорта** — `/memory export <категория>` в читаемый формат
- [ ] **Статистика использования** — какие записи чаще запрашиваются
- [ ] **Автоочистка** — настраиваемые правила автоматического удаления
- [ ] **Теги** — кастомные метки для группировки записей
- [ ] **История изменений** — отслеживание кто/когда удалял
- [ ] **Умный поиск** — семантический поиск через эмбеддинги

---

## Лучшие практики

### ✅ Рекомендуется:

1. **Делать бэкап** перед массовыми удалениями
2. **Использовать search** перед delete для проверки
3. **Регулярно запускать dedupe** (раз в неделю)
4. **Очищать старые записи** периодически (>60 дней)
5. **Проверять stats** после очистки

### ❌ Не рекомендуется:

1. Удалять >50 записей без бэкапа
2. Использовать `delete text` с очень короткими словами ("a", "the")
3. Удалять все категории сразу
4. Игнорировать результаты поиска перед удалением

---

## Совместимость

- ✅ Работает с памятью v2.0+
- ✅ Обратная совместимость с старым форматом
- ✅ Автоматическая миграция при загрузке
- ✅ Сохранение в JSON (легко редактировать вручную)

---

## Примеры использования

### Пример 1: Очистка зацикливания

```bash
# Neira 24 раза записала "Telegram использует C++"
/memory search "Telegram использует C++"
→ Найдено: 24 записи

/memory delete text "Telegram использует C++"
→ Удалено: 24

/memory dedupe
→ На всякий случай удалим остальные дубликаты
```

### Пример 2: Подготовка к эксперименту

```bash
# Перед обновлением промпта
/memory backup
→ Бэкап создан

/memory stats
→ Базовая линия: 280 записей

# ...эксперимент...

/memory stats
→ Проверка изменений
```

### Пример 3: Анализ личности

```bash
/memory search preference
→ Какие у меня предпочтения?

/memory search skill
→ Что я умею?

/memory search goal
→ Какие у меня цели?
```

---

**Powered by Neira Memory System v2.3** 🧠
